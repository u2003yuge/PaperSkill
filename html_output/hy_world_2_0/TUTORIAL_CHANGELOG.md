# HY-World 2.0 教程修改记录

本文档统一记录教程的人工优化版本。后续每次产生实质修改时，都应在顶部追加一个新版本，说明修改范围、依据、验证结果和待审查事项。

每个版本必须包含“模块变更明细”：逐项写出模块编号、模块名称、组件文件、新增或调整的具体内容，以及需要审查的状态变化。若没有修改运行模块，也必须明确写“无模块变更”，不能只写笼统的功能概述。

## V0.50「万象镜检·烟火全绿姬」

日期：2026-08-16

### 修改目标

- 让论文原图、官方架构图和 GIF 全部从 `html_output/hy_world_2_0/public/images/` 读取，并改用相对路径，避免部署在 `papers/hy_world_2_0/` 子路径时错误请求站点根目录。
- 将 5.2 重排为“左侧竖排输入证据 -> 中央共享骨干 -> 右侧竖排五步检验”，并让每次执行随机出现多种正确或错误候选。
- 统一 7.2 的 HY 谱系和其它谱系矩阵格式；统一 7.3 的 Table 14 与其它模型公开记录操作格式。
- 增加五项候选全部通过时的隐藏烟花彩蛋，同时保持教学巡检与论文真实架构之间的证据边界。

### 模块变更明细

- 调整全部直接静态图片引用，涉及 `src/modules/hy-architecture.tsx`、`hy-creation-pipeline.tsx`、`hy-composition.tsx`、`hy-innovation-map.tsx`、`hy-mission-planner.tsx`、`hy-panorama.tsx`、`hy-trajectory.tsx` 与 `hy-worldlens-lab.tsx`。
- 将上述模块中的 `/images/<file>` 全部改为 `./images/<file>`；引用的九个 PNG、WebP、GIF 均已核对存在于 `public/images/`，没有新增远程图片地址或本地绝对路径。
- 重做模块 5.2“一次重建，五类几何产物”，组件为 `src/modules/hy-architecture.tsx`，样式为 `src/styles/paper.css`。
- 输入区固定竖排 RGB、Pose、K、Depth；RGB 始终必需，未勾选先验保持灰色缺省，已勾选先验按播放顺序接入中央 Shared Transformer。
- 中央只保留一个共享 Global-local 跨视图理解核心，明确“一次共享前向”产生共同对应关系与几何上下文；右侧再竖排 Camera、Pointmap、Depth、Normal、3DGS 五步检验。
- 为每个输出定义两种正确候选和两种错误候选：Camera 覆盖回投影/尺度与位姿漂移/焦距错配，Pointmap 覆盖连续点面/薄结构与离群点/薄片断裂，Depth 覆盖层级/遮挡与空洞/近远翻转，Normal 覆盖平滑/棱角与翻转/噪声，3DGS 覆盖贴合/收敛与漂浮/重复高斯。
- 每个候选使用独立小 Canvas 绘制症状；点击“执行重建”会重新随机抽取五类候选，巡检按 Camera -> Pointmap -> Depth -> Normal -> 3DGS 依次进行，错误候选退回，正确候选直接保留。
- 设置约 20% 的隐藏“全通过”轮次；五项全部为正确候选且巡检结束时，结构图上层 Canvas 释放三组烟花，并显示“镜界烟花已释放”。再次执行会重新抽取，不保证继续全对。
- 调整模块 7.2“模型能力进化图鉴”，组件为 `src/modules/hy-model-evolution.tsx`。
- HY 谱系与其它谱系现在共用 `evolution-matrix lineage` 的列宽、表头、日期、状态单元格、详情区和窄屏内部滚动规则；删除“外部谱系压缩为一屏”的特殊格式说明。
- “未报告”状态仍保持灰色、不可点击；统一格式不改变能力证据，也不把不同任务模型压成统一排名。
- 重做模块 7.3“协议内分簇图与跨论文工程记录”，组件为 `src/modules/hy-performance-compare.tsx`。
- “本文 Table 14”和“其它模型公开记录”现在都使用同一套模型多选、指标多选、分簇柱状图、零基线比例和图例；外部记录不再切换到另一种横条组件。
- 外部视图可选 WorldMirror 2.0、Fast3R、VGGT、CUT3R、π³，可选峰值显存、秒级墙钟和 FPS；没有报告的条目显示“未报告”，Table 14 的无可运行配置仍显示 OOM，两者不混写。
- 在统一柱状图下增加“公开记录条件卡”，逐条保留硬件、输入规模、分辨率、GB/GiB 原单位、骨干/完整模型范围和论文链接；统一界面只减少操作差异，不消除协议差异。
- 更新模块 8.2“版本星轨与完整更新日志”，组件为 `src/modules/hy-update-log.tsx`。
- 最近三个大型版本更新为 V0.50、V0.49、V0.48，默认选中 V0.50；版本卡明确列出资源路径、5.2、7.2、7.3 与本日志同步。
- 更新 `src/data/tutorial.ts` 中 5.2、7.2、7.3 的模块描述，使章节入口准确说明竖排结构、随机候选、统一矩阵和统一分簇图。

### 事实与教学边界

- 小 Canvas 中的候选类型、随机概率和巡检顺序是教学设计，不是论文报告的坏样本比例、自动质量检查网络或真实部署通过率。
- 烟花只表示本轮教学候选五项均为绿色通过状态，不表示 WorldMirror 2.0 在真实输入上必然一次生成无错误结果。
- Figure 12 支撑 Any-Modal 输入、共享骨干和多输出头；Table 11 只支撑仅图像与全部先验两个报告端点，部分先验组合仍不插值数字。
- 7.2 的统一矩阵只统一视觉语法；7.3 的统一分簇图只统一操作语法。跨论文硬件、输入、单位和任务范围继续保持不可直接排名。

### 验证结果

- 教程 `npm run build` 通过，TypeScript 与 Vite 共转换 69 个模块。
- 九个静态图片/GIF 引用均使用 `./images/` 相对路径，并逐项核对存在于 `public/images/`；未发现缺失文件。
- 5.2 已在桌面端实际展开检查：RGB、Pose、K、Depth 从左侧顶部竖排接入，中央仅保留一个 Shared Transformer，右侧 Camera、Pointmap、Depth、Normal、3DGS 从顶部竖排巡检；未再出现顶部大片空白。
- 5.2 已在 390px 窄屏实际检查：三列按“输入 -> 共享骨干 -> 五步检验”折叠为单列，页面 `scrollWidth` 与内容宽度一致，没有横向溢出。
- 5.2 已重复运行并观察到正确/错误候选变化；错误候选会退回。重复运行至全绿后，“镜界烟花已释放”提示与 Canvas 烟花均正常触发。
- 7.2 已实际切换 HY 谱系和其它谱系：两者均使用同一矩阵结构，外部谱系的“未报告”单元格保持禁用。
- 7.3 已实际切换本文与外部公开记录：两种视图均保留模型选择、指标选择、分簇柱状图与图例；外部记录额外保留五张协议条件卡。
- 仓库正式构建产物已从 `/papers/hy_world_2_0/` 子路径启动验证；Figure 12 与官方重建 GIF 分别正确解析到 `./images/figure-12-worldmirror.png` 和 `./images/official-reconstruction.gif`，自然尺寸有效，控制台无错误或警告。
- paper-skill `validate-output.js` 已通过：教程共 8 章、19 个主动模块、21 个组件 ID。
- 仓库级 `npm run validate`、`npm run catalog`、`npm run catalog:check` 均通过；以当前 `upstream/main` 建立等价 PR 基准后，范围检查通过，共涉及 67 个已跟踪文件且只包含 `hy_world_2_0` 一篇教程与目录索引。
- `npm run build:paper -- hy_world_2_0` 通过，正式构建共转换 69 个模块并输出到 `site/papers/hy_world_2_0/`；仅保留 Vite 对主脚本超过 500 kB 的性能提示，不影响构建或资源加载。

### 审查重点

- 资源路径：在正式 `papers/hy_world_2_0/` 子路径展开所有论文图与 GIF，确认请求不会落到站点根目录 `/images/`。
- 5.2：分别运行多次，确认五类候选会改变且正确/错误画布与文字一致；只有错误候选显示退回，正确候选显示可交付。
- 5.2 彩蛋：重复执行直至出现五项全绿，确认烟花覆盖结构图但不拦截按钮，动画结束后页面布局不跳动。
- 7.2：切换两个谱系，确认列宽、表头、日期、状态和滚动提示完全一致，未报告单元格仍不可点击。
- 7.3：切到其它模型公开记录，确认仍显示模型与指标选择器、分簇柱状图和图例；切换显存/秒数/FPS 后，未报告状态与条件卡同步保留。

## V0.49「镜界巡检·星图映写姬」

日期：2026-08-16

### 修改目标

- 调整首章动画标签与任务卡，避免标签遮挡主体，并让四种输入卡具有更明确的可点击感和选中状态。
- 修正 2.1 的焦距错误表达、放大 2.2 的坐标与场景标签，并把 2.3 的平面路线底图升级为具有地面透视、立体障碍和远端高度的三维 NavMesh 场景。
- 把 5.2 重做为一张从左到右、再进入下一行的大结构图；输出头使用各自的小型视觉预览，只有问题候选被退回，正常候选通过检查。
- 把 6.1 重做为“主场景 + 墙面放大镜 + 天空放大镜”，让体素降采样、普通增密、掩码剪枝与非天空增密的职责可以直接从画面读出。
- 让 7.2 的外部谱系矩阵在一屏内完成比较，把 7.3 的 Table 14 与跨论文记录合并为同一位置的双视图，并在第 8 章新增可演示的完整更新日志模块。

### 模块变更明细

- 调整模块 1.2“四阶段造物管线”的动画标签，样式文件为 `src/styles/paper.css`。
- 将“独立场景”标签从画布左上角移动到右下角，将动画状态提示移到左下角，避免标签覆盖阶段标题、路径说明或关键帧连线；移动端同步使用相同角落分工。
- 美化模块 1.3“生成与重建双路线总图”，组件为 `src/modules/hy-mission-planner.tsx`，样式为 `src/styles/paper.css`。
- 为文本、单图、多视图和视频加入 `T / ▣ / ▦ / ▶` 识别符号；输入任务卡增加稳定高度、悬停抬升、选中描边与生成/重建颜色区分，卡片文字不再只靠普通标题表达可点击性。
- 调整模块 2.1“全景故障擦除实验”，组件为 `src/modules/hy-panorama.tsx`。
- “相机参数不可靠”不再用类似鱼眼镜头的红色弧线泛化表达，而是在同一 ERP 地标上叠加正确建筑落点与错误 FoV 梯形投影；宽化轮廓、四个局部位移箭头和“轮廓与 ERP 地标错位”共同说明错误焦距如何放大投影宽度。
- 调整模块 2.2“全景数据二维分布地图”，样式为 `src/styles/paper.css`。
- 纵轴文字取消反向旋转；地图高度由 330px 增至 370px，标题、轴标签、十二类场景标签、污染提示、图例和说明文字均增大，避免坐标含义只能靠近距离辨认。
- 调整模块 2.3“WorldNav 五层规划回放”，组件为 `src/modules/hy-trajectory.tsx`。
- 规划画布新增斜俯地面、透视网格、半透明 NavMesh 区域、两个有顶面和侧面的立体障碍、椭圆深度节点与远端目标高度辅助线；候选路线和相机仍使用原规划状态，不以新的三维装饰替代路径证据。
- 重做模块 5.2“一次重建，五类几何产物”，组件为 `src/modules/hy-architecture.tsx`。
- 输入行固定按 `RGB -> Pose -> K -> Depth -> Shared Transformer` 从左到右播放，随后用向下转折进入下一行；共享骨干再横向连接 Camera、Pointmap、Depth、Normal 与 3DGS 五个输出头，形成一张连续结构图。
- 五个输出头新增小型视觉预览：Camera 显示回投影框与漂移轮廓，Pointmap 显示连续点面与离群点，Depth 显示连续深度层，Normal 显示朝向块与局部翻转，3DGS 显示房间轮廓和高斯点。
- Camera、Pointmap、Normal 使用教学坏候选，检查时标记问题并退回；Depth 与 3DGS 使用正常候选，绿色扫描后显示“可交付”，不再让所有输出都暗示需要丢弃。
- 保留醒目边界：巡检是解释各输出应检查什么的教学动画，不是论文新增的自动质量检查网络，也不代表论文报告了上述坏候选比例。
- 重做模块 6.1“3DGS 配方逐步重建实验”，组件为 `src/modules/hy-composition.tsx`。
- 新增五步因果进度条：画质上限、先减体积、找回细节、删除冗余、限制错误增密；点击 Table 9 配方时，已完成、当前和未到达步骤使用不同状态。
- Canvas 改为斜俯三维建筑主场景，右侧同时提供墙面细节和天空区域两个放大镜；体素降采样减少墙面网格，普通增密恢复网格并增加天空漂浮点，MaskGaussian 减少冗余，完整配置在保留细节的同时把天空漂浮点压到最低教学状态。
- 五次切换加入 620ms 渐入动画；Table 9 的高斯数量、PSNR、SSIM 与 LPIPS 数值保持不变，画面中的纹理密度和漂浮点数量继续只承担机制解释。
- 调整模块 7.2“模型能力进化图鉴”，组件为 `src/modules/hy-model-evolution.tsx`。
- “对比其它谱系”固定为 HY-World 2.0、GenEx、video2world、Marble 1.0 四列，外部视图取消横向滚动并压缩表头、日期和状态字体；HY 谱系在窄屏仍允许必要的内部滑动。
- 调整模块 7.3“协议内分簇图与跨论文工程记录”，组件为 `src/modules/hy-performance-compare.tsx`。
- 在“显存与时间”协议顶部新增“本文 Table 14 / 其它模型公开记录”分段控件；本文视图保留视图数选择、配置分簇柱状图和原表，外部视图在相同位置显示显存、秒级墙钟与 FPS 三组横条，不再在 Table 14 下方重复追加整块内容。
- 两个视图继续严格隔离协议：跨论文 GB/GiB 不换算，FPS 不倒数，VGGT 仍注明只覆盖骨干，未报告显存保持未报告。
- 新增模块 8.2“版本星轨与完整更新日志”，组件为 `src/modules/hy-update-log.tsx`，并注册到 `src/modules/registry.tsx`、写入 `src/data/tutorial.ts`。
- 最近三个大型版本以 V0.49、V0.48、V0.47 卡片展示，点击后切换别名、修改焦点、具体模块列表和演示说明；完整 `TUTORIAL_CHANGELOG.md` 通过 Vite raw import 直接读取，默认折叠，需要追溯时展开，不再维护一份容易过期的复制文本。

### 事实与教学边界

- 2.1 的正确落点、错误 FoV 梯形和位移箭头是投影错误的教学示意，不是论文报告的相机标定误差样本或角度数值。
- 2.2 的类别位置仍是教学分布地图；字体放大不改变其“不是论文样本统计”的边界。
- 2.3 的透视地面与立体障碍用于说明 NavMesh、障碍和远端目标存在高度关系，不声称论文输出了本教程绘制的网格或具体三维坐标。
- 5.2 的通过/退回状态只用于区分质量检查结果；论文 Figure 12 证明共享骨干与多输出头，不证明教程中的统一巡检流程或坏候选类型频率。
- 6.1 的放大镜严格复述 Table 9 配方因果，纹理格数和漂浮点个数不是论文测量值；真实定量结果仍以可展开 Table 9 为准。
- 7.2 的一屏矩阵描述能力覆盖与证据状态，不产生跨任务排名；7.3 的双视图只合并界面位置，不混合不兼容协议。

### 验证结果

- TypeScript 检查与 Vite 生产构建通过，69 个模块完成转换。
- 浏览器逐项检查 5.2、6.1、7.2、7.3 与 8.2：桌面端结构、动画状态和文字均可辨认，外部谱系矩阵无需横向滑动，效率双视图正常切换。
- 390px 移动端检查中，文档、5.2 结构图、7.2 外部矩阵与 8.2 更新日志的 `scrollWidth` 均未超过容器宽度；浏览器控制台无警告或错误。
- paper-skill 输出结构校验通过：8 个章节、19 个主动模块、8 个双模块章节、21 个组件 ID 全部注册。
- 仓库 `npm run validate`、`npm run catalog`、`npm run validate:pr -- main` 与 `npm run build:paper -- hy_world_2_0` 全部通过；单篇教程已重新构建到 `site/papers`。
- 单篇构建前精确停止了占用 `esbuild.exe` 的 4174 Vite 预览进程；构建完成后已在 `http://127.0.0.1:4174/` 恢复预览并再次确认页面标题与访问状态。

### 审查重点

- 1.2/1.3：确认“独立场景”位于右下角且不遮挡说明，四种输入卡的可点击性和当前选择足够明确。
- 2.1：选择“相机参数不可靠”并从 0% 拖到 100%，确认错误区域表达的是 FoV 宽化与 ERP 地标错位，而不是无关的鱼眼变形。
- 2.2/2.3：确认坐标轴方向、标签字号与透视 NavMesh 可直接读懂，路线和障碍不会被三维底图遮挡。
- 5.2：播放完整流程，确认 RGB/先验按行接入，Depth 和 3DGS 通过，Camera/Pointmap/Normal 才退回，并且边界提示始终可见。
- 6.1：依次切换五个配方，确认墙面和天空放大镜分别回应细节与漂浮物问题，指标值没有被动画视觉量替换。
- 7.2/7.3：确认外部谱系一屏完成，Table 14 与其它模型记录通过分段按钮互斥显示。
- 8.2：确认三个版本卡可以切换，完整 Markdown 日志默认折叠且展开后可滚动审查。

## V0.48「万象归航·星图验收姬」

日期：2026-08-16

### 修改目标

- 统一整篇教程的初始状态、章节总结图标、下拉框边框、展开提示、焦点状态与移动端排布。
- 把 2.2 从单维度散点示意改成真正的二维数据分布地图，并减少 2.3 漫游场景中大色块对路线的遮挡。
- 把 5.2 从一次点击后静态解锁输出，重做为“输入证据逐项接入 -> 共享空间理解 -> 五头分流 -> 逐项验收”的连续动画。
- 把 7.3 的跨论文工程记录改成显存、秒级墙钟时间与 FPS 三组严格比例条形图，同时保留不可直接排名的协议警告。
- 扩充第 8 章的最终总结、外部观点、轻量边界快问与隐藏式资料库；重做首屏动画，让核心矛盾在进入正文前即可读懂。

### 模块变更明细

- 调整全局滑动条初始状态，涉及 `src/modules/hy-panorama.tsx`、`src/modules/hy-resolution.tsx` 与 `src/modules/exampleSlider.tsx`。
- 模块 2.1 的扫描线由 50% 改为 0%；模块 5.1 的相对位置由 70% 改为控件最小值 5%；示例滑动条由 50% 改为 0%。“最左边”按每个控件允许的最小值解释，不把 5.1 强行改成其定义域之外的 0%。
- 调整全部章节结尾总结，数据文件为 `src/data/tutorial.ts`：24 个字母或数字图标全部替换为与内容对应的 emoji，例如视频、测量、路线、记忆、显式资产、证据与未知边界；总结标题与正文不变。
- 重做首屏新旧方法动画，组件为 `src/modules/hy-hero.tsx`。
- 传统方法侧改为三帧主体/背景逐渐错位，并在右侧绘制带缺口的红色线框房间，分别表达“视频想象会漂移”与“重建未见区域会留白”。
- 本文方法侧从一张稀疏观察逐步展开四张补充视图，再通过绿色共享节点凝结为带高斯点的完整线框房间；动画不再复用正文中常见的相机沿路线移动逻辑。
- 重做模块 2.2“全景数据二维分布地图”，组件为 `src/modules/hy-panorama-curation.tsx`。
- 横轴明确为“常见现实 -> 稀有/想象”，纵轴明确为“真实采集主导 -> 可控合成主导”；不再使用一条意义不清的分隔线表示上下区域。
- 场景类别由 6 类扩展为 12 类：客厅、街景、自然地貌、夜景人群、工业空间、古城遗迹、水下世界、航拍峡谷、科幻舱、浮空遗迹、巨构室内与极端视点。
- 切换真实、合成、清洗双源与未清洗双源时，点位按来源显示、灰化或恢复；未清洗状态额外标出“接缝被学成场景边界”和“支架被学成固定物体”。所有坐标与百分比继续标为教学映射，不是论文数据统计。
- 调整模块 2.3“WorldNav 五层规划回放”，组件为 `src/modules/hy-trajectory.tsx`。
- 删除漫游状态覆盖画面的黄色走廊矩形，改用三条低透明度透视引导线、递减门框和远端消失点表达“沿可走区域望向远端”；路线、障碍和候选淘汰过程保持可见。
- 重做模块 5.2“一次重建，五类几何产物”，组件为 `src/modules/hy-architecture.tsx`。
- 点击执行后，三张 RGB 先接入；用户勾选的 Pose、K、Depth 再按顺序接入，未选择先验始终显示为灰色“本次缺省”，随后全部已知 token 汇入共享 Global-local Transformer。
- 共享特征建立后，同时激活 Camera、Pointmap、Depth、Normal、3DGS 五个输出头；系统自动逐项移动检查焦点，并以小动画展示漂移位姿、离群点、无效像素、翻转法线与漂浮点被移出交付结果。
- 新增醒目边界说明：输出验收动画用于解释每类产物应该检查什么，不声称论文在共享骨干后新增了统一自动验收网络；Table 11 仍只展示“仅图像”和“全部先验”两个真实端点。
- 调整模块 7.3“协议内分簇图与跨论文工程记录”，组件为 `src/modules/hy-performance-compare.tsx`。
- 论文内 Table 14 的配置分簇柱状图保持不变；跨论文区域新增三组横向条形图：WorldMirror/Fast3R/VGGT 的峰值显存、三者的秒级墙钟记录，以及 CUT3R/π³ 的 FPS 吞吐记录。
- 每根条严格使用当前组原始报告值与组内最大打印数值的比例；GB 与 GiB 不互相换算，FPS 不倒数成秒，VGGT 明确标为骨干成本，CUT3R 与 π³ 的未报告显存继续显示为虚线空槽。
- 重做模块 8.1“结论地图与继续阅读资料库”，组件为 `src/modules/hy-evidence-court.tsx`。
- 新增四张全文总结：系统价值、技术抓手、现实边界和审慎判断；汇总论文、官方资料与两篇知乎文章中可共同支持的观点，同时把体验评价和应用判断保留为第三方意见。
- 保留论文报告、官方展示、第三方解读、未报告四层证据仪表盘；新增两道不计分边界快问，只检查“运行时实时不等于完整生成实时”和“生成补观察、重建拉回几何”的限定条件。
- 新增四组隐藏资料栏：论文与官方入口，权重/体验/许可证，相关文章与中文讲解，相关论文与比较模型。每组都带灰色点击提示、用途说明与外部链接。
- 调整 `src/styles/paper.css`：新增二维坐标轴、token 接入、输出验收、工程条形图、全文总结、边界快问和参考资料库样式；统一模块内所有 `details` 的 4px 圆角、边框、打开状态、悬停和键盘焦点，并补充 900/620/420px 三档响应式布局。

### 事实与教学边界

- 2.2 的十二类场景、二维坐标和四项百分比均是教学抽象；论文 Section 3.1 只明确真实/UE 双源数据、domain gap，以及过滤明显接缝和拍摄设备入镜。
- 5.2 的先验接入顺序用于播放可选条件如何进入共享骨干；五项验收动画不是论文训练或推理模块，也不产生新的质量分数。
- 7.3 的条形长度只对应各论文直接打印的数值，不能消除硬件、分辨率、输入规模、任务范围和 GB/GiB 单位差异。
- 第 8 章链接继续遵守“数字看论文、开放状态看官方仓库、产品能力看官方页面、体验判断看署名文章、许可证看全文”的证据分工。

### 验证结果

- 项目 TypeScript 检查与 Vite 生产构建通过，67 个模块完成转换。
- paper-skill 输出结构校验通过：8 个章节、18 个主动模块、7 个双模块章节、20 个组件 ID 全部注册。
- 仓库 `npm run validate`、`npm run catalog`、`npm run validate:pr -- main` 与 `npm run build:paper -- hy_world_2_0` 全部通过；单篇教程已重新构建到 `site/papers`。
- 浏览器完成新版首屏实际截图检查，确认传统侧的漂移/留白与本文侧的扩展观察/显式资产能够直接区分，文字未遮挡画面。
- 后续全页浏览器自动检查因浏览器控制环境重置而中断，重复初始化仍返回本地运行时路径错误；因此本版本不把桌面全页、移动端宽度与新控制台结果记为已通过。4174 预览服务已在仓库构建后恢复。

### 审查重点

- 模块 2.2：依次切换四种配方，确认纵横轴含义清楚、十二类标签没有把真实与合成误写成论文固定分布。
- 模块 2.3：切到“漫游”，确认透视引导线不会覆盖候选路线、障碍、远端目标和相机回放。
- 模块 5.2：分别以无先验、部分先验和全部先验播放；确认缺省 token 不会被错误接入，五个输出依次验收，动画结束后仍可手动点击复查。
- 模块 7.3：确认显存、秒数和 FPS 从不进入同一坐标组；短柱只表示原始数值更小，不自动解释为总体更优。
- 模块 8.1：检查两道快问不计分，四组资料默认折叠，展开后所有链接用途和证据层级均清楚。

## V0.47「星轨校准·镜界巡游使」

日期：2026-08-16

### 修改目标

- 修复 1.2 WorldStereo 相机没有沿曲线运行的问题，并把四个曲线取景点与四张目标关键帧建立可见映射。
- 让 2.1 的三种全景故障在同一场景中具有明确、局部、可追踪的视觉症状，消除重复绘制场景造成的背景素材泄漏。
- 深化 2.2 数据策展因果链，规范 2.3 候选路线从全虚线到逐条淘汰再到最终实线的状态顺序。
- 正式定义 SSM++，重做 5.2 的主操作，并调研其它前馈三维重建论文的显存与时间记录。
- 跨论文效率数据只作为带完整条件的工程记录；不同硬件、分辨率、视图数、测量范围和单位不进入同一比例图。

### 模块变更明细

- 修复模块 1.2“四阶段造物管线”，组件 `src/modules/hy-creation-pipeline.tsx`。
- 新增三次贝塞尔曲线位置与切线计算；WorldStereo 相机的位置使用曲线点，朝向使用曲线导数，不再以起点到终点的直线插值代替曲线运动。
- 在曲线上设置 K1-K4 四个关键帧取景点；每个点使用虚线与箭头连接到对应目标帧，动画推进时依次变为紫色，明确“路径上的相机位姿 -> 目标关键帧”的对应关系。
- 四张目标帧继续使用同一运动主体的横向位移表达视差；曲线、相机、取景点与帧卡使用同一动画进度。
- 重做模块 2.1“全景故障擦除实验”，组件 `src/modules/hy-panorama.tsx`。
- 场景底图现在只绘制一次；扫描线左侧叠加修复机制提示，右侧只叠加故障层，不再分别绘制两套完整素材，因此不会在背景露出第二套山体、建筑或主体。
- “相机参数不可靠”增加两侧几何拉伸、弯曲边缘、焦距误差文字和局部位移箭头；症状集中在输入映射层。
- “潜空间左右断开”增加左右边缘不同颜色/地形、红色周期断边和跨边界关系线；明确 ERP 两端本应相邻。
- “解码后亮度跳变”增加冷暗/暖亮色块、双侧亮度带与边界竖线；明确 Pixel Blending 只处理像素空间可见接缝。
- 扫描线继续完整支持 0%-100%；0% 完整显示故障，100% 完整显示修复，中间状态在同一场景中局部比较。
- 丰富模块 2.2“全景数据配方显微镜”，组件 `src/modules/hy-panorama-curation.tsx`。
- 在四项相对指标之外新增“来源门 -> 质量门 -> 学到的先验 -> 生成时表现”四段因果链。
- 每种数据配方现在都具体说明哪些来源进入、污染是否拦截、模型会学到什么，以及推理时可能出现什么结果。
- 清洗双源状态突出真实外观、稀有语义和几何标签的互补；未清洗双源状态突出接缝、支架和设备轮廓会成为错误捷径。
- 所有百分比继续明确为教学抽象；论文只报告双源策展、domain gap 与过滤明显 stitching artifacts / 相机设备入镜，没有公开固定配比。
- 修正模块 2.3“WorldNav 五层规划回放”，组件 `src/modules/hy-trajectory.tsx`。
- 均匀采样阶段三条候选路线统一使用虚线，主候选不再提前变成实线或宣布胜出。
- Ray-casting 阶段按动画进度先后标红候选 B 与 C，增加“穿过障碍”“碰撞边界”原因和红色 X；被淘汰路线逐渐降低透明度。
- 双向连接阶段幸存路线仍为双色虚线；只有最终尾部修剪与相机回放阶段才绘制灰色底线和任务色实线。
- 补全模块 3.2“GGM × SSM++ 双记忆实验”，组件 `src/modules/hy-memory.tsx`。
- 新增 `SSM++ = Improved Spatial-Stereo Memory` 正式定义，与 GGM 并列解释，不再只在下拉词条中一笔带过。
- 新增“目标相机查询 -> 3D FoV 评分 -> 选择性检索 -> 空间配对”四步说明。
- Canvas 中增加三张历史视角候选与 `31% / 82% / 18%` 教学相关性排序，选中的中间帧进入检索-目标横向配对，形成 `2W` 并共享时间索引 `t` 后送入主 DiT。
- 上述百分比只表达“选择最相关关键帧”，不是论文阈值或模型输出；论文事实锚定 Section 5.2.2、Figure 10-11 与 Table 8。
- 完全重做模块 5.2“一次重建，五类几何产物”，组件 `src/modules/hy-architecture.tsx`。
- 删除“相机估计 / 表面恢复 / 资产交付”三个抽象任务页签，改为一个固定、可理解的真实委托：把三张室内照片交付为可进入 WorldLens 的几何资产。
- 多视图 RGB 始终必需；用户勾选当前已有的 Pose、K、Depth 后点击“执行重建”，主路径依次显示统一编码、共享 Global-local Transformer 和专用解码。
- 一次执行会同时解锁 Camera、Pointmap、Depth、Normal、3DGS 五类产物；用户点击产物卡查看每个输出头为何需要独立格式、监督或训练阶段。
- 无先验与全部先验状态分别高亮 Table 11 的两个真实端点；部分先验只说明约束作用，不插值虚构 Acc. / Comp. 数字。
- 扩展模块 7.3“协议内分簇图与跨论文工程记录”，组件 `src/modules/hy-performance-compare.tsx`。
- 论文内 Table 4、Table 12、Table 14 继续按原始数值和零基线绘制；进入效率页时默认切换到 32 视图，便于与 Fast3R 的 32 视图记录并列阅读。
- 新增 HY-World 2.0：H20、32 视图、518×378、BF16 单卡，15.10 GB/卡、2.11 s。
- 新增 Fast3R：A100、32 视图、512×384，13.25 GiB、0.509 s；单位保留 GiB。
- 新增 VGGT：H100 + FlashAttention v3、20 帧、336×518，特征骨干 5.58 GB、0.31 s；明确 DPT 头平均另需 0.03 s 与 0.2 GB/帧。
- 新增 CUT3R：A100、KITTI 视频深度、512×144，16.58 FPS，峰值显存未报告。
- 新增 π³：A800、KITTI 视频深度，57.4 FPS；表中未单列分辨率和峰值显存，保持未报告。
- 跨论文记录不绘制共同柱高，不把 FPS 倒数为单场景秒数，不把骨干成本冒充完整多头重建成本。
- 更新 `src/data/tutorial.ts`：重写 1.2、2.1、2.2、2.3、3.2、5.2、7.3 的标题或描述，以及第 7 章协议边界结论。
- 更新 `src/styles/paper.css`：新增数据因果链、双记忆定义与流程、固定重建委托、执行路径、五输出产物、跨论文工程表及 900/620/420px 响应式样式。
- 更新 `REFERENCES.md`：新增四篇外部论文链接、表号、原始效率记录和不可比协议说明。

### 验证结果

- 项目 TypeScript 与 Vite 生产构建通过，67 个模块完成转换。
- 桌面端 1.2 截图确认：K1-K4 位于橙色贝塞尔曲线上，分别连接四张目标帧；相机位于曲线末端并按曲线切线转向。
- 2.1 的 range 仍为 `min=0`、`max=100`；50% 潜空间断边状态中左侧只显示修复提示，右侧只显示断边故障，没有第二套背景素材泄漏。
- 2.3 候选阶段截图确认三条路线全部为虚线；Ray-casting 中两条失败路线按顺序标红并显示碰撞原因。
- 3.2 切换到 GGM + SSM++ 后，界面同时显示 GGM 粗点云、三候选相关性、检索/目标 `2W` 配对、主 DiT 和稳定门框/砖纹结果。
- 5.2 接入 Pose + Depth 并执行后，五个输出头全部解锁；反馈明确部分先验没有 Table 11 插值数值。
- 7.3 效率页显示五条跨论文记录；页面外层无横向溢出，工程表在自身区域内提供横向滚动。
- `390×844` 视口下文档 clientWidth / scrollWidth 均为 `375 / 375`，所有已展开模块均无模块级横向溢出；5.2 与 3.2 的流程自动改为单列。
- 全新浏览器标签页重新加载后控制台无 error、warning 或 warn；编辑过程中删除再重建 5.2 文件产生的旧 Vite HMR 记录不在新页面复现。

### 审查重点

- 审查 1.2：相机必须始终贴合曲线，朝向在弯曲处发生变化；K1-K4 与目标帧的连接不能错位。
- 审查 2.1：三种故障必须能直接对应输入映射、潜空间周期边界、解码像素空间，不得重新出现全背景叠画。
- 审查 2.3：阶段 2 与阶段 4 的幸存路线仍应为虚线，只有阶段 5 可以显示实线执行路线。
- 审查 3.2：SSM++ 的选择性检索、直接进入主 DiT、横向 2W 拼接和共享时间索引必须与论文原文一致。
- 审查 5.2：用户应能回答“为什么一次前向需要共享骨干，又为什么仍保留五个输出头”。
- 审查 7.3：任何外部显存/时间数字都必须同时显示硬件、输入、分辨率或未报告状态和测量范围，不得产生跨论文总排名。

---

## V0.46「万象重铸·真意显形领域」

日期：2026-08-16

### 修改目标

- 对用户点名的 1.2、1.3、2.1、2.2、2.3、3.2、4.2、5.1、5.2、6.1、7.3 做教学目的重构，不再以“有动画或有按钮”作为完成标准。
- 为 11 个核心模块加入统一学习契约：明确“为什么学”“本次操作”“应得判断”，让交互前就能知道它要证明什么。
- 所有模块继续遵守论文证据边界：教程视觉用于解释因果，论文原值、协议、指标方向和未报告状态不由动画计算或补写。

### 模块变更明细

- 重做模块 1.2“四阶段造物管线”，组件 `src/modules/hy-creation-pipeline.tsx`。
- WorldNav 场景新增多条候选路线、红色穿墙淘汰线、逐段移动相机和动态视锥，动画过程从“沿一条线移动”改为“铺开候选 -> 淘汰碰撞 -> 沿互补路线补盲区”。
- WorldStereo 场景删除四张近似静态缩略图；改为同一橙色运动主体在四个目标视角中的横向位置从 18% 变化到 82%，同时保留固定地标产生视差，四帧区别无需阅读文字即可辨认。
- 保留每阶段官方图片抽屉与论文边界；四步仍只指 WorldStereo 关键帧生成器。
- 调整模块 1.3“生成与重建双路线总图”，组件 `src/modules/hy-mission-planner.tsx`。
- 新增高对比度“输入任务卡可点击”入口，可依次预览四条输入路径；输入卡改为紧凑 2×2 布局，减少空白。
- 收窄路径图最小宽度、阶段卡和共享核心尺寸；桌面与手机端均取消外层横向溢出。
- WorldMirror 2.0 继续作为两条路线共同汇合核心，共享落点独立放在下方，不再遮挡路径画面。
- 完全重做模块 2.1“全景故障擦除实验”，组件 `src/modules/hy-panorama.tsx`。
- 删除抽象三节点方框图；在同一 ERP 场景中构造“错误相机投影、潜空间左右断开、解码后像素亮度跳变”三种可见故障。
- 扫描线范围由 8%-92% 改为完整 0%-100%；0% 显示完整故障，100% 显示完整修复，中间状态同步比较主体和左右边界。
- 明确隐式映射、Circle Padding、Pixel Blending 分别位于输入映射、扩散潜空间和解码像素空间，不能互相替代。
- 完全重做模块 2.2“全景数据配方显微镜”，组件 `src/modules/hy-panorama-curation.tsx`。
- 删除由通用方框拼出的伪照片和六个样本判断卡；改为真实单源、合成单源、清洗双源、未清洗双源四种训练分布配方。
- 同一操作联动语义覆盖、现实外观、几何标签和污染风险，并显式显示接缝捷径与设备入镜污染。
- 教学百分比明确标为相对关系示意；论文事实只保留双源数据、domain gap 与质量过滤，不冒充公开配比。
- 强化模块 2.3“WorldNav 五层规划回放”，组件 `src/modules/hy-trajectory.tsx`。
- 常规轨迹增加中心放射覆盖；环绕轨迹突出绑定对象和闭环；重建感知轨迹突出覆盖空洞与多次回访；漫游突出长走廊远端；航拍突出高度与俯视覆盖。
- 五种模式不再只是折线形状和颜色略有不同；最终相机仍按每段真实长度移动并在拐点转向。
- 完全重做模块 3.2“GGM × SSM++ 双记忆实验”，组件 `src/modules/hy-memory.tsx`。
- 新增术语定义：GGM = Global-Geometric Memory，是从目标相机渲染的 360° 粗点云几何锚点，不是通用 token 缓存。
- 交互改为无记忆、仅 GGM、仅 SSM++、GGM + SSM++ 四态消融；同一房间中的门框漂移、墙角位置、砖纹和门牌细节随状态改变。
- 动画结论直接对应两种记忆分工：GGM 守全局坐标与粗结构，SSM++ 检索相关历史帧补局部纹理。
- Table 8 的 16.13、20.94、19.83、21.63 等数值继续作为论文配置证据，不由四态动画计算。
- 调整模块 4.2“从多步教师到四步学生”，组件 `src/modules/hy-dmd-lab.tsx`。
- 在分布曲线实验前新增蒸馏动机和过程：成熟教师定义目标分布、少步学生产生偏差样本、`s_real - s_fake` 校准学生。
- 明确为什么 DMD 必须在控制与记忆训练之后，以及“WorldStereo 四步”与“完整系统 712 秒”之间的边界。
- 完全重做模块 5.1“同一物体的跨分辨率坐标实验”，组件 `src/modules/hy-resolution.tsx`。
- 让同一橙色物体始终处于画面相对横向位置，用户可切换 L/M/H 并移动物体；左右同步显示标准整数索引与 `[-1,1]` 归一化位置。
- 高分辨率状态明确标出标准索引的训练外风险，并联动 Table 12 的 AUC@30：WorldMirror 1.0 为 66.29，2.0 为 86.89。
- 保留 Table 11 与 Table 12 的低分辨率尺寸差异提示，避免跨表混写。
- 完全重做模块 5.2“任务驱动的 Any-Modal 重建台”，组件 `src/modules/hy-architecture.tsx`。
- 删除从左到右点击抽象架构节点的操作；改为相机估计、表面恢复、3DGS 资产交付三个具体问题。
- 用户按当前可用条件接入 Pose、K、Depth；多视图 RGB 始终必需，统一 token、共享 Transformer 和对应任务头形成一条可解释路径。
- 解释共享骨干为何复用跨视图空间特征、多个头为何保留不同输出格式与监督，以及 3DGS 头为何在第二阶段单独训练。
- Table 11 只展示“仅图像”和“全部先验”两个真实端点，部分先验组合不插值虚构结果。
- 完全重做模块 6.1“3DGS 配方逐步重建实验”，组件 `src/modules/hy-composition.tsx`。
- 删除最大高斯预算和最低 PSNR 的抽象阈值寻优；改为按 Table 9 五个真实配置逐步查看 6M 基线、体素降采样、自适应增密、MaskGaussian 和完整配置。
- 主画面同时表现墙面高频细节与天空漂浮物：降采样导致砖缝消失，普通增密恢复细节但产生天空漂浮物，最终非天空增密与 MaskGaussian 同时控制两类问题。
- 每一步固定显示高斯数、PSNR、SSIM、LPIPS 原值；完整配置明确为减少约 77% 高斯、PSNR 下降 0.153 dB、LPIPS 增加 0.006 的有损取舍。
- 修正模块 7.3“严格零基线模型分簇图”，组件 `src/modules/hy-performance-compare.tsx`。
- 删除最低 18% 视觉保底和低优指标反向映射；所有柱高严格等于“原始数值 / 当前指标最大已报告值”。
- AUC、PSNR、SSIM 等高优指标仍是长柱更好；AbsRel、LPIPS、显存、时间保持原始方向，短柱更好。
- OOM 继续使用灰色纹理，不映射为任何数值；柱顶保留论文原值，协议间不构造总分。
- 调整 `src/data/tutorial.ts`：重写上述模块标题、描述、第 4 章蒸馏桥接和第 7.3 图表口径。
- 调整 `src/styles/paper.css`：新增统一学习契约、故障擦除、数据配方、双记忆、DMD 动机、坐标实验、任务架构、3DGS 配方及严格比例图样式；补充 900/620/420px 响应式规则。

### 验证结果

- TypeScript 与 Vite 生产构建通过，67 个模块完成转换。
- 浏览器桌面端页面宽度为 1265px，页面 scrollWidth 同为 1265px；模块 1.3 外框宽 949px，scrollWidth 同为 949px。
- 390×844 手机视口下页面 clientWidth / scrollWidth 均为 375px；1.3 外框为 279/279px，2.1、2.2、3.2、5.2、6.1、7.3 均无模块级横向溢出。
- 2.1 的 range 元素明确为 min=0、max=100；手机截图确认三层故障按钮、同场景擦除线和百分比控件均可读。
- 1.3 切到视频后正确显示“视频 -> 世界重建”，并保持多视图/视频跳过前三阶段的分流解释。
- 3.2 切到 GGM + SSM++ 后反馈为“全局几何锚点与局部相关视角分工协作”。
- 5.2 切到资产交付后激活 3DGS head，并说明先冻结几何参数再训练该头。
- 6.1 切到完整配置后显示 1.381M、25.023、0.747、0.215 及有损取舍说明。
- 7.3 相机与深度页签实测：AUC@30 61.68 / 86.48 对应 71.3228% / 100%；AbsRel 0.151 / 0.167 / 0.445 对应 33.9326% / 37.5281% / 100%，严格与原值成比例且未反向。
- 新建浏览器标签页重新加载后控制台无 error 或 warning；编辑过程中产生的 Vite 热更新历史报错不在新页面复现。

### 审查重点

- 重点审查 1.2 WorldStereo：四帧中的橙色运动主体必须有明显横向位移，固定建筑地标不能同步平移。
- 重点审查 2.1：扫描线应能到达 0% 和 100%，三种故障的修复层级必须彼此不同。
- 重点审查 3.2：仅 GGM 应结构稳定但细节偏软，仅 SSM++ 应细节清晰但位置可能漂移，双记忆才同时稳定。
- 重点审查 5.1：同一相对位置在高分辨率时标准整数索引增大，归一化坐标保持不变。
- 重点审查 6.1：体素降采样、普通增密、MaskGaussian 与完整配置的视觉症状和 Table 9 原值必须逐项一致。
- 重点审查 7.3：低优指标不得反向，任何柱都不得存在非数值保底高度。

---

## V0.45「星海折叠·八章秘典再编」

日期：2026-08-16

### 修改内容

- 全文阅读知乎文章《HY-World 2.0：生成辅助重建的完整开源》，采用其“背景技术缺陷 -> 生成辅助重建 -> A/B/C/D 模块 -> 实验边界”的讲解顺序重新组织教程。
- 将原 10 章压缩为 8 章，减少章节切换，同时保持论文从系统动机、生成路线、WorldStereo、训练、WorldMirror、3DGS、实验到边界的顺序。
- 重做三个交互价值最低的模块：原 6.1 的阶段点选、原 8.2 的创新筛选卡、原 10.1 的八题判题。
- 重做原 10.3 性能图：每个模型现在是一簇，同一簇内显示多个已选指标；用户可分别多选模型与指标。
- 未增加第三方动画包。继续遵守 paper-skill 仅允许 React 与 ReactDOM 运行时依赖的限制。

### 八章重排明细

| 新章节 | 合并的旧内容 | 调整后的教学作用 |
| --- | --- | --- |
| 第 1 章“为什么需要生成辅助重建” | 原第 1 章 | 先比较视频生成、3DGS、Forward 3DGS 的能力边界，再看双路线系统总图 |
| 第 2 章“A+B：世界种子与观察路线” | 原第 2、3 章 | 把 HY-Pano 与 WorldNav 连成“先看全、再补盲区”的连续任务 |
| 第 3 章“关键帧与双记忆” | 原第 4、5 章 | 把 Keyframe-VAE、GGM 与 SSM++ 放在同一世界扩展阶段 |
| 第 4 章“训练课程” | 原第 6 章 | 保留三阶段训练与 DMD，但把静态点选改成顺序编排 |
| 第 5 章“WorldMirror 前馈重建” | 原第 7、8 章的架构部分 | 合并 Normalized RoPE、Any-Modal、共享骨干与多输出头 |
| 第 6 章“资产与运行时” | 原第 9 章 | 连续讲深度对齐、3DGS 压缩和 WorldLens 运行时 |
| 第 7 章“创新、代际与实验” | 原第 8 章创新模块、原第 10 章模型/性能模块 | 先连接问题与机制，再看谱系和兼容协议数据 |
| 第 8 章“证据边界与使用” | 原第 10 章结论模块 | 用非答题式仪表盘收束论文、官方、第三方与未报告边界 |

### 模块变更明细

- 调整模块 1.1“世界模型概念实验室”，组件 src/modules/hy-world-model-basics.tsx：位置保留，但新桥接文案先引入视频生成、传统 3DGS 与 Forward 3DGS 的互补缺陷。
- 调整模块 1.2“四阶段造物管线”，组件 src/modules/hy-creation-pipeline.tsx：由旧 1.1 顺延，作为“生成辅助重建”总览后的第一条生成路线。
- 调整模块 1.3“生成与重建双路线总图”，组件 src/modules/hy-mission-planner.tsx：由旧 1.2 顺延，继续保证 WorldMirror 2.0 是两条路线共享落点。
- 合并原第 2、3 章为新第 2 章：2.1 hy-panorama.tsx、2.2 hy-panorama-curation.tsx、2.3 hy-trajectory.tsx 依次呈现全景映射、数据策展和五类轨迹。
- 合并原第 4、5 章为新第 3 章：3.1 hy-keyframes.tsx 与 3.2 hy-memory.tsx 连续解释关键帧表示、GGM 全局骨架与 SSM++ 局部检索。
- 完全重做模块 4.1“训练课程编排器”，组件 src/modules/hy-training-stages.tsx：用户从领域适配、记忆中训、DMD 后蒸馏三张卡中构造顺序；先蒸馏、先记忆、控制后立即蒸馏等错误顺序分别显示教师不成熟、参考关系不稳和压缩过早的症状；正确顺序逐项点亮相机控制、跨轨迹记忆与四步采样能力。
- 调整模块 4.2“DMD 分布校准仪”，组件 src/modules/hy-dmd-lab.tsx：由旧 6.2 迁入新训练章，继续保留噪声与更新渐变动画。
- 合并原第 7、8 章的重建部分为新第 5 章：5.1 hy-resolution.tsx 先讲跨分辨率坐标，5.2 hy-architecture.tsx 再讲 Any-Modal 与多输出头。
- 合并资产处理为新第 6 章：6.1 hy-composition.tsx 讲 3DGS 数量/画质取舍，6.2 hy-worldlens-lab.tsx 讲生成后运行时。
- 完全重做模块 7.1“创新证据链工作台”，组件 src/modules/hy-innovation-map.tsx：从“分类筛选贡献卡”改为“选择旧问题 -> 切换连接机制 -> 闭合结果与证据”；错误连接不会计分，只解释为什么规划、记忆、重建与运行时不能互相替代；匹配后显示论文定位、证据、边界和相关图片/GIF。
- 调整模块 7.2“模型能力进化图鉴”，组件 src/modules/hy-model-evolution.tsx：由旧 10.2 迁入实验章，继续保持 HY-World 2.0 在首列、历代在右、外部谱系单独切换、未报告项不可点击。
- 完全重做模块 7.3“可筛选模型分簇性能图”，组件 src/modules/hy-performance-compare.tsx：模型是横轴簇，指标是簇内竖柱；模型和指标均支持多选且至少保留一项；全景、几何、NVS、效率四组协议分别呈现；低优指标在归一化时反向处理，使更高柱统一表示协议内相对更优；柱顶保留原始论文数值；FP32 256 视图 OOM 使用灰色纹理。
- 完全重做模块 8.1“证据边界仪表盘”，组件 src/modules/hy-evidence-court.tsx：删除八案得分、逐题判决和称号；改为论文报告、官方展示、第三方解读、未报告四层页签，再选择协议性能、局部效率、Marble、运行时、知乎直觉或许可证切片，直接查看结论能说到哪里。
- 调整公共类比组件 src/modules/hy-analogy.tsx：由 10 个章节动作改为 8 个，分别对应任务分流、全景与路线、双记忆、训练顺序、归一化坐标、资产压缩、协议比较和证据标签。
- 调整 src/data/tutorial.ts：重写章节 ID、模块编号、桥接文字、洞察、公式与 takeaway，使页面顺序与论文管线和从浅入深学习顺序一致。
- 调整 src/styles/paper.css：新增训练编排器、证据链、边界仪表盘、模型分簇图及 760/520px 响应式布局；新增元素圆角不超过 6px，窄屏改为单列。
- 调整 REFERENCES.md：记录知乎文章本轮采用的讲解顺序，并把模块映射更新为 8 章编号。

### 参考资料采用与降级

- 采用第三方文章对三类背景技术的输入、输出、优势和局限的讲解方式，以及“视频生成补观察、Forward 3DGS 拉回显式几何”的系统直觉。
- 采用文章对 A/B/C/D 数据流的分段顺序，用于教程章节重排。
- HY-Pano、WorldNav、WorldStereo、WorldMirror、3DGS 的结构和训练陈述继续回到论文与官方资料核验。
- 文章作者对 Marble 的观感、对工程完整度的评价、对累计误差的担忧均保留为署名第三方观点，不升级为论文结论。

### 验证结果

- paper-skill 结构校验通过：8 章、18 个主动模块、7 个双模块章节、20 个组件全部注册。
- TypeScript 与 Vite 生产构建通过，67 个模块完成转换。
- 浏览器完成全部 8 章渐进展开，章节标题顺序与新教程数据一致。
- 模块 4.1 实测“先蒸馏”会显示“教师尚未成熟”；重新编排为领域适配 -> 记忆中训 -> DMD 后蒸馏后显示“课程闭环完成”，三段能力与详细继承说明全部出现。
- 模块 7.1 将 HY-Pano + WorldNav 问题错误连接到 WorldStereo 时显示“机制与问题错位”；切回正确机制后显示“证据链已闭合”并展开论文支持与不能外推。
- 模块 7.3 初始全景图显示 3 个模型簇、每簇 3 个指标共 9 根柱；取消 CubeDiff 与 Q-Align 美学后变为 2 个模型簇、每簇 2 个指标共 4 根柱。
- 模块 7.3 的低优指标方向核对通过：AbsRel 0.151 的 pi3 柱高为 100%，0.167 的 WorldMirror 2.0 柱高约 95.54%；论文原值同时显示。
- 模块 7.3 至少保留一个指标；效率页切到 256 视图时，FP32 的显存和时间两项均显示 OOM 灰色纹理，完整 FSDP 配置显示 78.78 GB 与 17.52 s。
- 模块 8.1 不再包含得分、下一案、称号或连续判题；第三方页签显示知乎来源与降级说明，未报告页签显示 Marble 无统一定量协议。
- 浏览器控制台无 error 或 warning。
- 390x844 手机视口下，页面 clientWidth=375px、scrollWidth=375px；训练编排器、创新证据链、模型分簇图、证据仪表盘内部均为 clientWidth=284px、scrollWidth=284px，无横向溢出。
- 仓库 validate、catalog、validate:pr -- main 全部通过；单篇 build:paper -- hy_world_2_0 在停止占用 esbuild 的预览进程后通过，67 个模块完成转换。
- 构建完成后已恢复 http://127.0.0.1:4174/，HTTP 状态为 200，浏览器重新加载后标题与首章入口正常。

### 审查重点

- 重点审查 4.1：任意错误顺序都应有明确失败症状，正确顺序必须点亮三段能力。
- 重点审查 7.1：错配机制时结果与证据不得被错误显示为成立。
- 重点审查 7.3：切换模型和指标时簇数量与柱数量同步变化，至少保留一个模型和一个指标；AbsRel、LPIPS、显存和时间的柱高方向必须正确。
- 重点审查 8.1：页面不再出现连续答题、得分或称号。

---

## V0.44「镜湖止闪·秘卷显影结界」

日期：2026-08-16

### 修改内容

- 修复模块 2.1、7.1、9.1 等滑动或连续调节 Canvas 时的闪烁：状态变化不再重新初始化画布尺寸和观察器，而是在同一个 Canvas 上更新绘图函数并重绘当前帧。
- 新增统一“证据媒体抽屉”，以明显的灰色小字提示用户点击展开；论文原图、官方架构图与官方 GIF 默认收起，避免长图打断主交互。
- 把论文 Figure 2、Figure 12，以及全景、WorldNav、重建、Mesh、角色交互素材分配到最相关模块，不再只在页面中直接显示或放在不显眼的末尾。

### 模块变更明细

- 调整模块 2.1“全景三层修复扫描仪”，组件文件为 `src/modules/hy-panorama.tsx`：扫描线拖动只触发当前 Canvas 重绘，不再销毁和重新建立绘图上下文；新增 HY-Pano 2.0 官方架构图抽屉。
- 调整模块 7.1“跨分辨率位置探针”，组件文件为 `src/modules/hy-resolution.tsx`：分辨率与探针状态切换复用既有 Canvas，避免连续调整时出现白闪。
- 调整模块 9.1“高斯预算帕累托实验”，组件文件为 `src/modules/hy-composition.tsx`：最大高斯预算、最低 PSNR 两个滑动条改为持久 Canvas 重绘；官方轻量 Mesh GIF 改为明显的可展开抽屉。
- 调整模块 1.2“四任务共享路径总图”，组件文件为 `src/modules/hy-mission-planner.tsx`：新增论文 Figure 2 整体架构原图抽屉，用于核对生成与重建两条路径的真实连接。
- 调整模块 3.1“WorldNav 五层规划回放”，组件文件为 `src/modules/hy-trajectory.tsx`：新增官方五类轨迹三维场景图抽屉。
- 调整模块 8.1“Any-Modal 架构配线盘”，组件文件为 `src/modules/hy-architecture.tsx`：将原本直接显示的 Figure 12 改为论文原图抽屉，并新增官方多图/视频重建 GIF 抽屉。
- 调整模块 9.2“WorldLens 运行时实验”，组件文件为 `src/modules/hy-worldlens-lab.tsx`：官方角色漫游与碰撞 GIF 改为可展开抽屉。
- 调整公共论文证据组件 `src/modules/hy-paper-evidence.tsx`：新增 `EvidenceMediaDrawer`，统一媒体类型、标题、灰色提示、来源链接、图片说明和展开状态。
- 调整 `src/styles/paper.css`：新增证据媒体抽屉的桌面与移动端布局，抽屉圆角为 7px，展开后保留来源和证据边界说明。
- 调整 `src/data/tutorial.ts`：更新 1.2、2.1、3.1、8.1、9.1、9.2 的模块描述，明确原图/GIF 的展开位置；移除 1.3 直接渲染 Figure 2 与 8.1 直接渲染 Figure 12 的旧配置，避免同图重复出现。

### 验证结果

- TypeScript 与 Vite 生产构建通过，67 个模块完成转换。
- 浏览器对 9.1 最大高斯预算执行 20 次连续状态更新，数值从 1.5M 更新到 3.5M，Canvas 全程保持 `is-ready`，控制台无错误。
- 浏览器确认页面共有 7 个统一媒体抽屉：论文 Figure 2、论文 Figure 12、HY-Pano 官方图、WorldNav 官方图、重建 GIF、Mesh GIF、角色交互 GIF。
- 7 个抽屉收起时均显示“灰色提示：点击展开图片或 GIF”；逐一点击后全部进入打开状态，7 个媒体元素均可见。
- 桌面页面 `clientWidth=1265px`、`scrollWidth=1265px`；手机页面 `clientWidth=375px`、`scrollWidth=375px`，媒体抽屉内部 `clientWidth=283px`、`scrollWidth=283px`，无横向溢出。
- `paper-skill` 输出校验通过：10 章、19 个主动模块、6 个双模块章节、21 个组件全部注册。
- 仓库结构验证、目录索引生成、PR 范围检查和单篇生产构建全部通过；单篇构建转换 67 个模块。
- 单篇构建完成后已恢复 `http://127.0.0.1:4174/`，HTTP 状态为 200。

### 审查重点

- 连续拖动 2.1 和 9.1 的滑动条时，Canvas 应保持同一 DOM 与绘图上下文，画面连续更新且不出现白色清空帧。
- 每个媒体抽屉都应在收起状态显示“灰色提示：点击展开图片或 GIF”，点击后才加载可见媒体区域。
- 论文原图、官方架构图和官方 GIF 必须标明素材类型与来源；GIF 只展示官方案例，不替代论文指标。
- 8.1 的 Figure 12 只能在抽屉中出现一次，不能同时在模块顶部和底部重复渲染。

---

## V0.43「星轨勘界·万象量衡觉醒」

日期：2026-08-16

### 修改内容

- 依据知乎文章对 WorldNav 工程流程的拆解，完全重做模块 3.1：把原来的“选择盲区并组合两条路线”改为五层规划回放，降低答题感，增加机制解释与连续动画。
- 新增模块 9.2“WorldLens 运行时实验”，把 IBL 环境光照、碰撞代理和角色漫游拆成三种不同运动逻辑，并把官方角色交互 GIF 从 9.1 移到该模块。
- 新增模块 10.3“协议安全性能条形图”，覆盖全景生成、相机与深度、新视角合成、每卡显存和墙钟时间；所有数字来自论文表 4、表 12、表 14。
- 收窄模块 10.2 的外部比较范围：“对比其它谱系”只显示 HY-World 2.0、GenEx、video2world、Marble 1.0，不再混入 HY-World 1.5、HY-World 1.0 或 WorldMirror 1.0。
- 在 `REFERENCES.md` 新增“知乎文章模块细节采用方式”表，明确文章解释、论文证据、官方展示和教程动画之间的边界。

### 模块变更明细

- 调整模块 3.1“WorldNav 五层规划回放”，组件文件为 `src/modules/hy-trajectory.tsx`。
- 第 1 层显示场景语义、障碍和 NavMesh；第 2 层铺开均匀采样候选；第 3 层用 Ray-casting 命中标记淘汰穿墙路径；第 4 层用蓝/绿两端显示双向贪心连接；第 5 层执行尾部修剪后的最终折线回放。
- 五类任务仍保留论文表 1 的常规 9、环绕 5、重建感知 10、漫游 3、航拍 8 条最大数量，并为每类任务补充用途和执行条件。
- 最终相机不再从起点直线插值到终点：使用每段欧氏长度计算全路径累计距离，在拐点切换当前线段角度，视锥随朝向同步旋转。
- 新增模块 9.2“WorldLens 运行时实验”，组件文件为 `src/modules/hy-worldlens-lab.tsx`。
- IBL 页签让太阳、建筑明暗覆盖和地面阴影同步变化；碰撞页签同时显示被边界截停的角色与穿模红色虚影；角色页签让角色沿折线与台阶移动，并由第三人称相机持续跟随。
- 将 `/images/official-interactive.gif` 从模块 9.1 移到 9.2，避免同一素材重复出现；保留 9.1 的轻量 Mesh 官方 GIF。
- 调整模块 10.2“模型能力进化图鉴”，组件文件为 `src/modules/hy-model-evolution.tsx`。
- 新增严格的 `external` 范围集合；切换外部比较时若当前选中 HY 1.5、HY 1.0 或 WorldMirror 1.0，会自动回到 HY-World 2.0，外部矩阵固定为四个模型列。
- 新增模块 10.3“协议安全性能条形图”，组件文件为 `src/modules/hy-performance-compare.tsx`。
- 全景页签提供 CLIP-I、Q-Align 透视质量、Q-Align ERP 美学；相机/深度页签提供 AUC@30 与 AbsRel，并保留 π3 的 AbsRel 优于 WorldMirror 2.0 的非领先结果。
- NVS 页签提供 PSNR、SSIM、LPIPS；效率页签可切换 32/64/128/256 视图、每卡显存与墙钟时间，FP32 256 视图 OOM 使用灰色纹理而非虚构数值。
- 调整 `src/modules/registry.tsx`：注册 WorldLens 与性能条形图；调整 `src/data/tutorial.ts`：更新 3.1 描述，并新增 9.2、10.3 章节映射与桥接文字。
- 调整 `src/styles/paper.css`：新增五层规划、WorldLens、性能图、外部四列矩阵及 900/760/560px 响应式规则；所有工具卡圆角不超过 8px。

### 参考资料采用与降级

- 采用知乎文章中的 WorldNav 工程路径、WorldLens IBL/碰撞/角色模式，以及对 HY-Pano、WorldStereo、WorldMirror 模块关系的讲解视角。
- HY-Pano 的 Circle Padding / Pixel Blending、WorldStereo 的三阶段训练/GGM/SSM++/DMD、WorldMirror 的 Any-Modal/共享骨干/多输出头已由现有 1.1、2.1、4.1、5.1、6.1、6.2、8.1、8.2 覆盖，本版本不重复创建同逻辑组件。
- “完美重建”“物理正确”“彻底解决结构崩塌”“24GB 必然可跑完整管线”“瞬间生成”等说法均未升级为论文事实。
- 所有性能数字回到论文表 4、表 12、表 14；知乎文章只帮助解释模块用途，不替代实验协议。

### 验证结果

- TypeScript 与 Vite 生产构建通过，67 个模块完成转换。
- `git diff --check` 通过。
- 浏览器确认模块 3.1 的五个规划阶段均可直接切换；最终画面显示按折线分段长度移动、在拐点同步转向的相机与视锥。
- 浏览器确认 WorldLens 的 IBL、碰撞代理、角色漫游三个页签均能切换并完成 Canvas 动画，画布进入 `is-ready` 状态。
- 浏览器确认性能图四个协议入口依次显示 CLIP-I、AUC@30、NVS PSNR、每张 GPU 显存；256 视图 FP32 正确显示 OOM。
- 浏览器确认外部谱系只显示 HY 2.0、GenEx、video2world、Marble 1.0；切回历代谱系显示 HY 2.0、HY 1.5、HY 1.0、WM 1.0。
- 桌面端页面 `clientWidth=1265px`、`scrollWidth=1265px`；手机视口页面 `clientWidth=375px`、`scrollWidth=375px`，3.1、9.2、10.2、10.3 模块内部均无横向溢出。
- 浏览器控制台未记录错误。
- `paper-skill` 输出校验通过：10 章、19 个主动模块、6 个双模块章节、21 个组件全部注册。
- 仓库结构验证、目录索引生成、PR 范围检查和单篇构建全部通过；单篇生产构建转换 67 个模块。
- 单篇构建前关闭了占用 `esbuild.exe` 的本地预览服务，构建完成后已恢复 `http://127.0.0.1:4174/`，HTTP 状态为 200。

### 审查重点

- 模块 3.1 的最终相机必须逐段沿折线移动，并在每个拐点改变朝向；不应再出现起终点直线插值。
- WorldNav 的均匀采样、Ray-casting、双向贪心、尾部修剪必须继续标记为第三方工程解读，不能写成论文证明的全局最优规划算法。
- 模块 9.2 的“实时”只指已生成资产的运行时渲染与交互，不代表论文表 10 的 712 秒完整世界生成已实时化。
- 模块 10.2 的外部视图只能出现 HY-World 2.0 与三个外部参照；HY 1.5、HY 1.0、WorldMirror 1.0 仅保留在历代谱系视图。
- 模块 10.3 的条形图不得跨页签求和或平均；AbsRel、LPIPS、显存和时间均为越低越好，OOM 不是“模型不支持”。

---

## V0.42「四幕重铸·独景幻演觉醒」

日期：2026-08-16

### 修改内容

- 完全重做模块 1.1 的视觉叙事：不再让四阶段在同一张画布上机械叠加，而是将 HY-Pano 2.0、WorldNav、WorldStereo 2.0、WorldMirror 2.0 设计成四幕独立功能动画；切换阶段时先清空 Canvas，再从零绘制当前阶段。
- 为四幕动画分别补入与功能对应的对象、状态变化和即时结论，避免四阶段只替换少量文字或复用同一种运动逻辑。
- 从腾讯混元官方项目主页新增四张阶段说明图，在对应阶段下方提供明显的可展开入口，让读者先操作动画，再按需查看真实案例和官方架构映射。
- 重排模块 1.2 的窄屏布局，取消依赖固定宽度内部画布的做法；在平板和手机上将输入、分流、两条路径、共享核心和输出栏逐层堆叠，处理组件越界问题。
- 复核动画依赖方案：`paper-skill` 规定运行时依赖只能是 React 与 ReactDOM，因此本版本没有增加 GSAP、Framer Motion、Three.js 或其它动画包，使用原生 Canvas、requestAnimationFrame 与 CSS 完成效果。

### 模块变更明细

- 调整模块 1.1“四阶段造物管线：从世界种子到三维资产”，组件文件为 `src/modules/hy-creation-pipeline.tsx`。
- HY-Pano 2.0 幕：从窄视场透视输入出发，以隐式映射曲线逐步展开 ERP 全景；左右边界用红色接缝提示，动画完成后显示循环填充与像素融合完成闭环。
- WorldNav 幕：绘制俯视障碍物、NavMesh 连接、三个覆盖盲区与碰撞约束；相机严格沿折线路线逐段移动，视锥随当前线段转向，盲区按抵达进度依次从红色变为绿色。
- WorldStereo 2.0 幕：左侧展示全景骨架与相关视角记忆，中部用四个独立去噪步显示图像由噪声变清晰，下方同步更新目标相机轨迹，右侧输出跨视角关键帧并明确“四步仅属于生成器”。
- WorldMirror 2.0 幕：多视图图像、位姿、内参和深度先验进入 Any-Modal token 网格；扫描线依次激活共享特征，经 WorldMirror 2.0 Backbone 后同时解锁点图、相机、深度、法线和 3DGS 输出。
- 四幕均使用独立绘制函数；每次阶段切换重置进度并调用 `clearRect` 清空 760×390 Canvas，动画时长统一为 1180ms；系统开启“减少动态效果”时直接显示最终状态。
- 修复 Canvas 首帧时间戳可能早于 `performance.now()`、进而产生负展开宽度并中断动画的问题：动画进度统一钳制在 `0–1`，圆角绘制工具也会拒绝负宽高与负半径。
- HY-Pano 2.0 的接缝状态由“红色缺陷始终保留”改为“红色接缝逐渐消退、绿色闭环边界接管”，使完成帧与“左右边界已闭环”的结论一致。
- 新增四个阶段证据下拉框：默认收起，摘要处用灰色小字提示“点击展开”，展开后显示对应官方说明图、中文图注和腾讯项目主页来源链接。
- 新增本地素材 `public/images/official-stage-pano.webp`、`official-stage-nav.webp`、`official-stage-stereo.webp`、`official-stage-mirror.webp`；README 与 `REFERENCES.md` 分别登记来源、用途和证据边界。
- 调整模块 1.2“四任务共享路径总图”的响应式样式，组件文件仍为 `src/modules/hy-mission-planner.tsx`：小于 760px 时改为单列流程，四输入先变两列、两条支路纵向排列、共享输出改为单列；小于 520px 时输入与每条阶段链均改为单列，箭头旋转为纵向。
- 调整 `src/data/tutorial.ts`：模块 1.1 描述改为“四个独立功能场景、切换即清空、可展开官方图”，不再使用“同一世界累积四层能力”的旧表述。
- 调整 `src/styles/paper.css`：新增动画运行指示、证据下拉框、官方图与图注样式，并重写模块 1.2 的平板/手机响应式规则。

### 证据边界

- 四幕 Canvas 是对论文机制的教学抽象：几何形状、场景纹理、盲区位置、token 网格和输出建筑体都不是论文真实中间特征或实验样本。
- 四张阶段图来自腾讯混元官方项目主页，承担“系统如何解释和展示这些阶段”的辅助说明；方法、四步采样条件、实验数字和速度仍以论文为准。
- WorldNav 五类轨迹是论文报告的互补启发式组合，动画中的单条路线只演示覆盖逻辑，不能解读为论文给出的唯一轨迹或全局最优规划。
- 统一 1180ms 动画时长是教程交互节奏，不代表任一模型的真实运行耗时；WorldStereo 的四步去噪也不代表端到端世界生成只需四步或实时完成。
- 模块 1.2 的窄屏堆叠只改变教程布局，不改变文本/单图走生成支路、多视图/视频直接进入共享 WorldMirror 2.0 的系统关系。

### 验证结果

- TypeScript 检查与 Vite 生产构建通过，65 个模块完成转换。
- 四张官方说明图均以本地 WebP 文件落库，避免运行时依赖远端图片；文件扩展名与服务器实际返回格式一致。
- 浏览器逐幕检查 HY-Pano 2.0、WorldNav、WorldStereo 2.0 和 WorldMirror 2.0：四个完成帧的场景对象、颜色语义和结论均不同，页签切换后旧幕内容不会残留。
- WorldNav 中间帧确认相机位于当前折线段上，朝向随线段斜率改变；不再从总起点直线插值到总终点。
- 四个证据槽均能切换到正确本地图片：Pano `2400×889`、Nav `1200×556`、Stereo `3200×962`、Mirror `2400×716`；展开状态下图片宽度受模块容器约束。
- 干净重载后 HY-Pano 动画正常结束为“动画完成，可继续探索”，控制台无警告或错误；红色接缝已转为绿色闭环边界。
- 桌面视口 `clientWidth=1265px`、`scrollWidth=1265px`，模块 1.2 任务图内部 `scrollWidth=clientWidth`；手机视口 `clientWidth=360px`、`scrollWidth=360px`，共享核心与输出栏均位于容器边界内。
- `paper-skill` 结构校验通过：10 章、17 个主动模块、5 个双模块章节、19 个组件全部注册。
- 仓库结构验证、目录生成、PR 范围检查和单篇构建全部通过；单篇构建转换 65 个模块。
- `git diff --check` 通过。

### 审查重点

- 点击四个页签或上一步/下一步时，旧阶段图形必须立即消失；当前画布只能包含当前阶段的对象和状态。
- WorldNav 相机必须沿每一段折线移动并随线段转向，不能从整条路线起点直线插值到终点。
- 四幕动画的主要动作必须分别是全景展开闭环、折线覆盖盲区、四步去噪与记忆合成、多模态 token 聚合与几何输出，不能退化为同一种动画模板。
- 四个证据下拉框应一一对应当前阶段，默认收起、提示清楚，展开后图片不应超出模块宽度。
- 模块 1.2 在 760px 与 520px 以下不应依赖横向滚动，也不能出现输入卡、路径节点、共享核心或输出栏越界。

---

## V0.41「四阶造物·双流归镜之门」

日期：2026-08-16

### 修改内容

- 在第一章前部新增统一的“四阶段造物管线”，通过同一世界画面的连续完善，依次解释 HY-Pano 2.0、WorldNav、WorldStereo 2.0 与 WorldMirror 2.0 分别接收什么、执行什么、增加什么能力。
- 重构原四任务共享路径图：删除生成支路与重建支路中重复出现的 WorldMirror 2.0，改成两条支路共同汇入唯一的共享重建核心。
- 将“共享落点”从右侧独立大卡改为主路径下方的横向资产输出栏，避免遮挡或挤压支路图形，并让“共享核心 → 显式资产 → WorldLens”关系更清楚。
- 将模块 10.2 的“查看全部参照”改为“对比其它谱系”，明确外部模型是不同技术谱系的参照，而不是同一代际序列中的更多成员。

### 模块变更明细

- 新增模块 1.1“四阶段造物管线：从世界种子到三维资产”，组件文件为 `src/modules/hy-creation-pipeline.tsx`，组件 ID 为 `hy-creation-pipeline`。
- 模块 1.1 提供四个可直接点击的阶段页签，以及“上一步 / 下一步 / 从头再看”控制；默认从 HY-Pano 2.0 开始，点击下一步依次进入 WorldNav、WorldStereo 2.0 和 WorldMirror 2.0。
- Canvas 使用同一场景累积四层变化：第一步展开 360° 全景，第二步绘制互补折线路径并让相机沿路径移动，第三步加入跨视角关键帧与记忆连线，第四步叠加几何轮廓、共享重建核心与可漫游资产输出。
- 每次阶段切换使用 820ms 缓动动画；系统减少动态效果时直接显示完成状态。阶段状态栏同步显示“已接入、正在形成、等待”。
- 每个阶段详情新增“接收什么、执行什么、新增什么、为什么需要”四栏说明，并附论文章节、图表或协议边界；明确四步蒸馏不等于整套系统实时生成。
- 未使用额外生成图片：四阶段视觉完全由 Canvas 绘制，可随步骤和状态实时变化，也避免引入新的外部素材授权问题。
- 原模块 1.1“四任务共享路径总图”顺延为模块 1.2，组件文件仍为 `src/modules/hy-mission-planner.tsx`。
- 模块 1.2 的生成支路现在只显示 HY-Pano 2.0、WorldNav、WorldStereo 2.0；重建支路只显示 Any-Modal 输入；两条支路右侧共同连接单一 `WorldMirror 2.0` 节点。
- 共享节点增加“生成路径 ↘ / 重建路径 ↗”双入口状态，当前任务对应入口高亮，另一入口弱化；切换文本、单图、多视图或视频时，共享节点始终保留，不再随未选支路灰显。
- 显式三维资产输出改为路径下方横向栏，按当前输入显示 3DGS、Mesh、视频、点图、深度、法线或相机等输出，并连接 WorldLens 实时渲染与漫游。
- 原模块 1.2“统一系统边界透视台”顺延为模块 1.3，组件与交互内容不变，仅调整编号以保持阅读顺序。
- 调整模块 10.2“模型能力进化图鉴”，组件文件为 `src/modules/hy-model-evolution.tsx`：范围按钮改为“对比其它谱系”，副标题改为“加入 GenEx、video2world 与 Marble 1.0”。
- 调整 `src/modules/registry.tsx`：注册新的四阶段组件；调整 `src/data/tutorial.ts`：新增 1.1 并更新 1.2、1.3 的标题、描述与组件映射。
- 调整 `src/styles/paper.css`：新增四阶段页签、Canvas、详情表、完成度栏、控制区及移动端响应式样式；重排共享任务图的四列两行布局、共享核心和下方输出栏。

### 证据边界

- 四阶段顺序对应论文世界生成路径：全景初始化、场景感知规划、关键帧世界扩展、前馈重建与三维合成。
- 多视图或视频世界重建不会执行前三个生成步骤，而是从 Any-Modal 输入直接进入同一个 WorldMirror 2.0；教程在动画末尾和共享路径图中均明确标注该条件。
- Canvas 中的房间、相机、折线路径、关键帧位置和几何轮廓是教学示意，不是论文真实中间输出或定量结果。
- 外部模型范围改名只改变比较语义，不修改任何模型能力状态、发表日期、来源链接或论文结论。

### 验证结果

- TypeScript 检查与 Vite 生产构建通过，65 个模块完成转换。
- 浏览器确认四个阶段可以按顺序前进，也可以直接点击页签；每步均同步更新 Canvas、阶段标题、四栏说明、完成度状态和下一步按钮。
- 浏览器确认第三步显示关键帧与跨轨迹连线，第四步显示 WorldMirror 2.0、几何轮廓和可漫游资产；“从头再看”能恢复第一步且清空已完成状态。
- 共享路径图中只有 1 个 `.mission-shared-core`，生成与重建支路内部不再包含重复的 WorldMirror 节点。
- 桌面端共享图 `scrollWidth=clientWidth=949px`，无需横向滚动；输出栏位于路径和共享核心下方，边界间距为 12px，无覆盖。
- 桌面页面 `clientWidth=1265px`、`scrollWidth=1265px`，无整页横向溢出。
- 模块 10.2 范围按钮已显示“只看 HY 谱系 / 对比其它谱系”。
- `paper-skill` 结构校验通过：10 章、17 个主动模块、5 个双模块章节、19 个组件全部注册。
- 仓库结构验证、目录生成、PR 范围检查和单篇构建全部通过；单篇构建转换 65 个模块。
- 响应式规则复核：小于 760px 时四阶段主画面切为单列，阶段页签与完成度栏切为两列；小于 520px 时四栏详情切为单列，下一步按钮独占整行，共享任务图仅在内部保留横向滚动。
- `git diff --check` 通过。

### 审查重点

- 模块 1.1 每次点击下一步都应在同一世界画面上增加一层能力，而不是把四步做成四张互不相关的静态图。
- 模块 1.2 的 WorldMirror 2.0 必须只出现一次；切换生成 / 重建任务时，双入口高亮应变化，但共享核心本身不能消失或重复。
- 共享资产输出栏应始终位于路径下方，不得遮挡任务卡、分流器、生成支路、重建支路或共享核心。
- “对比其它谱系”只用于打开外部参照列，不能暗示 GenEx、video2world 与 Marble 属于 HY-World 的历史版本。

---

## V0.40「同轴星环·代际共鸣启封」

日期：2026-08-16

### 修改内容

- 强化模块 10.2 的横向比较能力：选中任意模型能力单元格后，同时标出当前能力行与当前模型列，减少在宽矩阵中丢失比较坐标的问题。
- 新增“同轴对照带”，把当前能力在可见模型中的证据状态压缩为一排可切换模型卡；移动端不必反复横向滑回表头，也能连续比较同一功能。
- 保持证据边界不变：“未报告”仍为灰色且不可点击，状态计数只复述当前资料范围，不把不同模型目标压成统一排行榜。

### 模块变更明细

- 调整模块 10.2“模型能力进化图鉴”，组件文件为 `src/modules/hy-model-evolution.tsx`。
- 为当前模型表头新增列选中态，为当前能力行标题新增行选中态；同一能力行使用轻微底色，同一模型列使用细边框，交叉单元格继续使用原有蓝色强选中态。
- 新增动态状态汇总：随能力切换统计当前可见范围内“完整支持、局部支持、仅定性、未报告、闭源参照”的数量。
- 新增同轴模型按钮组：点击任一有证据模型，只切换模型、不改变当前能力；按钮立即更新详情面板、证据强度、用途、2.0 对应改进与来源链接。
- 同轴按钮继承矩阵证据语义：完整、局部、定性和闭源分别保留原颜色；未报告按钮禁用，并提供包含模型名、功能名和状态的读屏标签。
- 调整 `src/styles/paper.css`：新增行列聚焦、同轴对照带、七列 / 四列 / 两列响应式布局，以及长模型名自动换行规则。
- 调整 `src/data/tutorial.ts`：模块描述补充“沿同一能力轴切换历代与外部参照”的操作目标。

### 证据边界

- 本版本不修改任何模型能力判断、发表日期、论文指标或来源链接，只改变比较路径与视觉聚焦。
- 状态数量按当前“HY 谱系 / 全部参照”范围实时统计；数量不是模型排名，也不代表未报告能力不受支持。
- 同轴对照带与原矩阵读取相同数据，不新增教程推断或外部结论。

### 验证结果

- TypeScript 检查与 Vite 生产构建通过，64 个模块完成转换。
- `paper-skill` 结构校验通过：10 章、16 个主动模块、5 个双模块章节、18 个组件全部注册。
- 仓库结构验证、目录生成、PR 范围检查和单篇构建全部通过；目录命令未产生新的跟踪差异。
- 浏览器确认从“WorldMirror 1.0 × 多视图 / 视频重建”矩阵单元格进入后，选中能力行、模型列、同轴按钮和详情标题同步更新。
- 浏览器确认在同轴对照带点击 HY-World 2.0 时，只切换模型，能力仍保持“多视图 / 视频世界重建”，详情面板同步切换到 2.0。
- 浏览器确认切换“全部参照”并选择 Marble 1.0 后，对照带显示 7 个模型，状态计数为完整 2、局部 2、定性 1、未报告 2、闭源 0；两个未报告模型保持禁用。
- 手机视口下，同轴对照带切为两列，长模型名正常换行；页面 `clientWidth=375px`、`scrollWidth=375px`，无整页横向溢出。
- `git diff --check` 通过。

### 审查重点

- 点击任意矩阵单元格后，能力行、模型列、交叉单元格和同轴对照带必须指向同一状态。
- 点击同轴对照带中的模型只能切换模型，不应改变当前能力；详情面板和来源链接应同步更新。
- “未报告”在矩阵与同轴对照带中都必须保持灰色不可点击；范围切换不得改变底层能力判断。

---

## V0.39「三镜择星·无题沙盘开演」

日期：2026-08-16

### 修改内容

- 将模块 4.1 从“关键帧取景谜题”改造成“关键帧取景沙盘”，避免读者进入章节时先看到一个失败状态并继续寻找唯一正确答案。
- 默认展示一个稀疏跨视角组合，再提供连续三帧、中段密集、跨视角三个预设，方便直接比较不同采样结构的取舍。
- 新增任务说明栏，明确固定三帧只是教学对照条件，帧数、最小间隔和首尾跨度不是论文训练超参数。
- 保留自由选帧能力与论文相机误差证据，使预设只是探索入口，不会替代读者对 8 个候选视角的直接操作。

### 模块变更明细

- 调整模块 4.1“关键帧取景沙盘”，组件文件为 `src/modules/hy-keyframes.tsx`。
- 初始选帧由连续的 `F1 / F2 / F3` 改为跨视角的 `F1 / F4 / F8`，让首次进入时直接看到稀疏关键帧覆盖整段转弯的结构。
- 新增三个预设按钮：“连续三帧”选择 `F1 / F2 / F3`，“中段密集”选择 `F3 / F4 / F5`，“跨视角”选择 `F1 / F4 / F8`。
- 预设与自由选帧共享同一状态：点击任一帧后，如果不再匹配预设，三个预设都会取消选中；再次点击预设可恢复对应组合。
- 新增取景任务栏，分成任务、目标、观察三部分：固定 3 帧概括快速转弯，减少相邻重复并覆盖较大视角变化，同时注明诊断指标仅用于教学。
- 删除原来的“重置为连续三帧”按钮，避免把连续帧误写成默认或标准答案。
- 把“组合成立 / 失败”式反馈改为覆盖结构分析：相邻组合强调局部连续性和重复观察，局部稀疏组合提示首尾不足，跨视角组合说明其教学优势。
- 完整选满但覆盖不足时不再使用红色错误反馈；只有满足教学稀疏与跨度条件的状态用绿色突出，其余状态保持中性说明。
- 将术语抽屉中的“上方谜题规则”改为“教学沙盘的固定对照条件”，继续强调论文并未规定只允许三帧。
- 调整 `src/styles/paper.css`：新增三栏任务说明、三预设分段控制和移动端单列样式；移除不再使用的重置图标按钮样式。
- 调整 `src/data/tutorial.ts`：模块标题改为“关键帧取景沙盘”，描述改为预设切换、自由改选、覆盖结构与论文证据四层任务。

### 证据边界

- Keyframe-VAE 去除时间压缩、选择性冻结 Cross-Attn 与 FFN、相机误差变化来自论文 WorldStereo 2.0 章节及相关消融。
- `RotErr 0.762 → 0.492`、`ATE 2.141 → 1.768` 是论文配置比较，越低越好；不由上方三帧教学组合计算。
- 8 个候选视角、角度、固定三帧、最小间隔阈值、首尾跨度阈值和三个预设均为教程设计，不是论文规定的关键帧选择算法或训练参数。

### 验证结果

- TypeScript 检查与 Vite 生产构建通过，64 个模块完成转换。
- 浏览器确认默认“跨视角”预设选中 `F1 / F4 / F8`，诊断显示 3 帧已满、最小间隔 3 个候选位、首尾跨度 7 个候选位。
- 浏览器确认切换“连续三帧”后选中 `F1 / F2 / F3`，诊断同步更新为最小间隔 1、首尾跨度 2，并显示局部连续性与覆盖不足的中性说明。
- Canvas 初次进入时会在进入视口后完成绘制，桌面截图确认路线、相机朝向、三预设、8 个候选帧和诊断条均可见。
- 手机视口下，任务说明和三个预设切为单列，8 个候选帧切为两列；页面 `clientWidth=375px`、`scrollWidth=375px`，无横向溢出。
- 浏览器控制台无运行错误，`git diff --check` 通过。

### 审查重点

- 默认状态应为 `F1 / F4 / F8`，而不是连续三帧；首次进入不应出现红色失败反馈。
- 三个预设必须同步更新 Canvas、帧卡选中态、三项诊断、Keyframe-VAE 比较文案和反馈。
- 自由选帧仍应可用，且不能把预设或三帧限制表述为论文原始算法。
- 论文 RotErr / ATE 指标必须继续标记为独立证据，不可随教学选帧组合变化。

---

## V0.38「环幕炼金·双源暗房点灯」

日期：2026-08-16

### 修改内容

- 继续清理正文答题负担，将第 2 章两个连续判题模块分别改造成“修复前后扫描线”和“数据策展观察镜头”。
- 模块 2.1 不再要求读者猜测三种故障对应的修复机制，而是直接选层、拖动扫描线，在同一张图中比较修复前后。
- 模块 2.2 不再要求逐张作出收入 / 剔除裁决，而是按“全部、建议保留、建议过滤”三个镜头浏览样本、来源价值和污染规则。
- 两个模块仍保留论文证据边界：交互只解释机制和策展原则，不把教学样本数量、扫描比例或示意画面写成论文统计。

### 模块变更明细

- 调整模块 2.1“全景三层修复扫描仪”，组件文件为 `src/modules/hy-panorama.tsx`。
- 删除三道故障题的候选修复按钮、答对 / 答错状态、已归档计数、错误反馈与必须重新选择的流程。
- 保留三个故障层级页签：相机档案缺页、潜空间两端失联、解码边缘仍跳色；点击页签直接更新故障症状、观察提示和对应修复机制。
- 新增 `8%–92%` 修复前后扫描线：左侧显示修复后状态，右侧显示修复前状态，扫描线位置同步控制 Canvas 的裁切范围。
- 相机元数据切片对比“显式投影错位”与“条件图像 / 全景噪声在统一潜空间中学习对应”；潜空间切片对比“左右特征断开”与“周期邻接”；像素切片对比“解码后跳变”与“线性融合”。
- 新增三机制地图，始终同时展示隐式自适应映射、循环填充、线性像素融合所在层级；当前切片对应机制用绿色突出，其余机制保持可读但弱化。
- 第一次视觉复检发现扫描线会把修复前后中央文字拼接成一句，随后把状态标签分置到左右观察区，确保只混合图形，不混合说明文字。
- 保留论文表 4 的可展开原表与 `CLIP-I 0.831 → 0.844` 协议说明，强调该数字属于完整 HY-Pano 2.0，不由扫描线计算。
- 调整模块 2.2“全景数据双源策展镜头”，组件文件为 `src/modules/hy-panorama-curation.tsx`。
- 删除六张教学样本的裁决状态、正确计数、清空按钮、收入 / 剔除双按钮和答错提示。
- 新增“全部样本、建议保留、建议过滤”三个页签；切换后样本网格从 6 张筛选为对应的 3 张，并自动选择当前镜头中的第一张可见样本。
- 每张样本直接显示“保留”或“过滤”标签；详情面板同步解释其来源价值、污染线索、策展结论和不可外推边界。
- 保留双源样本架，继续说明真实拍摄提供自然光照与复杂纹理，UE 合成提供精确标签、想象场景和可控构型；来源不是保留 / 过滤的唯一标准。
- 调整 `src/styles/paper.css`：新增扫描线控制器、三机制地图、策展镜头页签、保留 / 过滤卡片状态和策展结论面板；删除旧修复答案架与裁决按钮样式。
- 调整 `src/data/tutorial.ts`：更新模块 2.1 与 2.2 的标题和描述，使操作说明从“匹配 / 裁决”转为“切换 / 扫描 / 浏览 / 比较”。

### 证据边界

- 隐式自适应映射、潜空间循环填充和解码后线性融合来自论文 Section 3.2。
- 真实高分辨率全景、UE 合成数据、接缝污染过滤与相机设备入镜过滤来自论文 Section 3.1。
- `CLIP-I 0.831 → 0.844` 来自论文 Table 4 的 I2P 定量比较，越高越好；不能归因给扫描线中的单一机制。
- 扫描线比例、R1-S3 样本、场景名称、缩略图和每类 3 张的数量均为教程构造；论文没有公开固定的真实 / 合成配比或这六张样本。

### 验证结果

- TypeScript 检查与 Vite 生产构建通过，64 个模块完成转换。
- paper-skill 结构校验通过：10 个章节、16 个主动模块、5 个双模块章节、18 个已注册组件。
- 浏览器确认模块 2.1 三个故障层可以切换，当前层会同步更新卷宗、机制地图和 Canvas；范围控件可见、启用，初始扫描位置为 `52%`。
- 浏览器确认模块 2.2 的“建议过滤”镜头将网格从 6 张缩为 3 张：夜间商业街、室内会客厅、峡谷训练场；三张均显示过滤原因。
- 手机视口下，三个故障卡、三个策展镜头和样本卡均切为单列；页面 `clientWidth=375px`、`scrollWidth=375px`，无横向溢出。
- 桌面 `1280×900` 视口下，页面 `clientWidth=1265px`、`scrollWidth=1265px`；浏览器控制台无运行错误。
- 正文关键词复查后，集中式判决 / 答题交互只剩第 10 章证据法庭；第 1、2、6 章均已改为探索式交互。
- `git diff --check` 通过。

### 审查重点

- 模块 2.1 的扫描线左侧必须始终表示修复后、右侧表示修复前；拖动时不应出现文字互相拼接或 Canvas 空白。
- 三个故障层必须各自绑定不同层级：输入到潜空间、潜空间周期边界、解码后像素边缘，不能把三种修复写成同一作用点。
- 模块 2.2 的“建议保留”应同时包含真实与合成样本；“建议过滤”也应跨来源过滤污染，不能把来源直接等同于质量。
- 六张样本和筛选数量必须继续明确标记为教学示意，不得写成论文数据集配比。

---

## V0.37「无问试炼·三阶星轨展开」

日期：2026-08-16

### 修改内容

- 继续减少正文中的问答负担：模块 1.2 不再要求连续完成五道成立 / 不成立判断，模块 6.1 不再要求为三个故障匹配训练处方。
- 将模块 1.2 改造成五切片系统边界阅读器，读者点击声明即可查看论文证据、适用边界和出处，同时让 Canvas 只高亮当前关系。
- 将模块 6.1 改造成三阶段训练能力时间轴，读者沿训练顺序观察前置条件、引入组件、能力增量、继承能力与顺序依赖。
- 保留第 10 章末尾的证据问答模块；本版本只移除正文中会打断连续阅读的密集判断题。

### 模块变更明细

- 调整模块 1.2“统一系统边界透视台”，组件文件为 `src/modules/hy-boundary-compare.tsx`。
- 删除五条声明各自的“成立 / 不成立”双按钮、作答状态、正确计数、完成条件和重置按钮。
- 新增五张阅读切片卡：前三张直接标记为“论文明确支持”，后两张标记为需要纠正的常见误读；点击卡片会同步更新证据面板、论文定位和 Canvas 高亮路径。
- 新增“下一条边界”按钮和 `01/05` 当前切片计数，支持按顺序浏览，也可直接跳到任意切片。
- Canvas 根据当前切片分别突出稀疏输入生成、丰富观测重建、重建回流、多个专用组件和离线生成 / 实时漫游边界；未涉及的路径使用灰色弱化。
- 修复实时切片中“712 秒离线生成”与“离线生成 → 实时漫游”两个标签的纵向重叠，分别移到画布 `y=62` 与 `y=92`。
- 当切换到第五条边界时继续显示论文 Table 10 的可展开证据，不把运行时实时漫游误写成完整生成实时化。
- 调整模块 6.1“三阶段训练能力时间轴”，组件文件为 `src/modules/hy-training-stages.tsx`。
- 删除三个故障案例、处方选择、答对 / 答错反馈、已诊断计数与“重新诊断”按钮。
- 新增三个阶段页签：领域适配、中段记忆训练、后蒸馏；初始状态为领域适配，点击页签或“下一阶段”更新全部联动内容。
- Canvas 从左到右累积显示“相机控制 → 跨轨迹一致性 → 四步生成器”，当前阶段用阶段色突出，已经获得的能力用绿色保留，尚未到达的阶段灰显。
- 每个阶段新增五类信息：前置能力、本段引入、能力增量、继承能力、为什么必须按此顺序训练；另增加对应故障症状说明，帮助读者把机制与实际问题对应起来。
- 明确“后蒸馏”只压缩已经成熟的 WorldStereo 2.0 生成器，不负责重新学习相机控制或跨轨迹记忆，也不能外推为完整世界生成只需四步。
- 调整 `src/styles/paper.css`：为边界阅读卡、支持 / 误读状态、展开证据提示、训练时间轴头部、阶段页签、能力详情网格和症状条增加桌面与移动端样式；彻底移除旧答题样式选择器。
- 调整 `src/data/tutorial.ts`：模块 1.2 与 6.1 的标题和描述改为阅读、浏览与能力累积导向，删除“判断”“诊断”“匹配处方”等答题式要求。

### 证据边界

- 生成 / 重建分流、WorldMirror 作为生成基础组件、多组件系统结构来自论文 Abstract、Introduction、Figure 2 与相关章节。
- 712 秒来自论文 Table 10 的完整生成管线；WorldLens 的实时性只对应生成后资产的运行时渲染和交互。
- 三阶段训练顺序、GGM、SSM++ 与 DMD 来自论文 WorldStereo 2.0 训练描述；时间轴、能力条和症状措辞属于教程信息设计。
- “能力累积”表示后续阶段依赖并保留前序训练目标的教学关系，不代表论文报告了每段独立冻结后所有能力的统一量化曲线。

### 验证结果

- TypeScript 检查与 Vite 生产构建通过，64 个模块完成转换。
- paper-skill 结构校验通过：10 个章节、16 个主动模块、5 个双模块章节、18 个已注册组件。
- 浏览器确认模块 1.2 第五张切片可切换为“常见误读”状态，证据内容更新为 WorldLens 与 712 秒边界，`aria-pressed` 同步为 `true`。
- 浏览器确认模块 6.1 可从领域适配切换到中段记忆训练和后蒸馏；每次切换均更新前置条件、引入组件、能力增量、继承能力和顺序原因。
- 手机视口下，训练页签与详情网格切为单列；页面 `clientWidth=375px`、`scrollWidth=375px`，无横向溢出。
- 桌面视口下，页面 `clientWidth` 与 `scrollWidth` 均为 `1265px`；浏览器控制台无运行错误。
- `git diff --check` 通过，旧的答题样式选择器已全部移除。

### 审查重点

- 模块 1.2 应表现为证据浏览器，不应再出现正确率、完成题数或必须回答后才能继续的状态。
- 点击五张边界卡时，Canvas、高亮状态、证据、边界和论文定位必须同步切换。
- 模块 6.1 的绿色能力条必须随阶段向右累积；后蒸馏阶段应同时显示控制、记忆与四步采样，不得把前三段画成互相替代。
- 正文阅读应比原版连续，只有第 10 章仍保留集中式问答与证据校验。

---

## V0.36「紫峰迁跃·七百二十毫秒炼成」

日期：2026-08-16

### 修改内容

- 为模块 6.2 增加真正连续的 Canvas 形变，噪声切换与 DMD 更新不再瞬间跳到新状态。
- 切换较低 / 中等 / 较高噪声时，教师与学生曲线宽度、探针位置和两条 score 箭头在 560ms 内同步渐变。
- 点击“执行一次更新”时，学生分布峰值在 720ms 内从当前视觉位置移动到新位置；连续点击会从尚未结束的中间状态继续，而不是先跳回旧终点。
- 新增过渡状态条，区分“噪声层级重塑”“学生分布沿分数差靠拢”和“恢复初始分布间距”。

### 模块变更明细

- 调整模块 6.2“DMD 分布校准仪”，组件文件为 `src/modules/hy-dmd-lab.tsx`。
- 重构局部 Canvas 包装器：由状态改变后单次重绘，改为按 `requestAnimationFrame` 驱动的三次缓出插值。
- 新增视觉状态 `sigma / gap`、过渡起点、目标、类型与序号；Canvas 每帧同步更新分布曲线、带噪样本、真实 / 伪 score 箭头和状态文字。
- DMD 更新时长为 720ms，噪声切换为 560ms，重置为 480ms；这些时长只用于教程动画，不是论文运行时间。
- 遵循系统 `prefers-reduced-motion` 设置：读者选择减少动态效果时直接绘制终态。
- 调整 `src/styles/paper.css`：增加 Canvas 过渡边框、三类进度条、移动端单列状态和减少动态效果规则。
- 调整 `src/data/tutorial.ts`：模块描述新增“连续形变”和“学生峰值平滑靠近”的操作说明。

### 证据边界

- DMD 的真实 / 伪 score、分数差和学生分布靠近教师的方向来自论文 Section 5.3。
- 560ms、720ms、480ms、三次缓出曲线和一维峰值移动均是教程视觉设计，不是 DMD 训练步数、采样耗时或论文优化轨迹。
- 噪声切换只改变教学曲线宽度与 score 示意，仍不展示论文未报告的具体扩散时间步。
- 四步 DiT 仍只属于 WorldStereo 2.0 生成器，完整世界生成边界保持 712 秒。

### 验证结果

- TypeScript 检查与 Vite 生产构建通过，64 个模块完成转换。
- 浏览器逐帧采样较高噪声切换：教学 `sigma` 从中等噪声连续逼近 `0.38`，中途保持 `running=true`，约 640ms 后到达终态并自动关闭运行状态。
- 浏览器逐帧采样第一次 DMD 更新：学生间距从 `0.7800 → 0.5363 → 0.4604 → 0.4524` 单调缩小，约 840ms 后显示 `1 次`并结束动画。
- 连续更新测试中，第二次点击前视觉间距为 `0.4954`，点击后继续经过 `0.3204 → 0.2729 → 0.2624`，没有回跳到旧终点；计数正确增加到 `2 次`。
- 初次进入模块时不再自动播放无意义动画，状态条显示“等待一次可观察的状态变化”，`running=false`。
- 手机 `390×844` 视口下，Canvas 为约 `284×151`、宽高比 `1.879`；状态条切为单列，页面 `clientWidth` 与 `scrollWidth` 均为 `375px`。
- `git diff --check` 通过，无空白错误。

### 审查重点

- 噪声切换时曲线宽度必须连续变化，不能闪现终态。
- 执行更新时紫色学生峰值必须沿横轴平滑靠近绿色教师峰值。
- 连续点击时新动画应从当前中间位置继续，不得回跳。
- 开启减少动态效果时应直接显示终态，并保持交互可用。

---

## V0.35「四门汇流·双径星图展开」

日期：2026-08-16

### 修改内容

- 将模块 1.1 原来一次只展示一张任务卡的布局，重做为四种输入与两条系统路径同时可见的共享大图。
- 文本、单图、多视图和视频四张输入卡固定排列在同一张图中；点击任一输入只切换阅读焦点，不再替换整张流程图。
- 生成支路、重建支路、共享 WorldMirror 2.0 与显式三维输出同时保留，未经过的输入和模块统一灰显。
- 在大图下方增加任务目标、分流原因和使用边界三栏说明，补充每种输入真正解决的问题。

### 模块变更明细

- 调整模块 1.1“四任务共享路径总图”，组件文件为 `src/modules/hy-mission-planner.tsx`。
- 四张输入卡分别显示输入丰富度、任务类型和主要风险；初始状态仍为“单图”。
- 文本与单图高亮 `HY-Pano 2.0 → WorldNav → WorldStereo 2.0 → WorldMirror 2.0` 生成主链。
- 多视图与视频高亮 `Any-Modal 输入 → WorldMirror 2.0` 重建路径，并灰显生成阶段。
- 两条路径共同连接“显式三维资产”与 WorldLens，强调它们不是四套彼此独立的模型。
- 调整 `src/styles/paper.css`：新增横向共享图、路径灰显、输入卡状态、任务说明三栏和移动端内部横向滚动提示。
- 调整 `src/data/tutorial.ts`：更新模块标题和描述，使章节入口直接说明“大图 + 路径切换 + 灰显”交互。

### 证据边界

- 四种输入到生成 / 重建路径的分流来自论文 Figure 2 与系统描述。
- 共享大图、输入卡位置、灰显规则和列布局属于教程信息设计，不是论文原始网络图。
- 文本与单图共享四阶段生成主链，多视图与视频直接进入 WorldMirror 2.0；灰显只表示当前路径不经过该模块，不表示模块被删除或模型不支持其它任务。
- 显式资产输出名称同时参考论文与官方项目页；实验数字仍需按各子任务协议核对。

### 验证结果

- TypeScript 检查与 Vite 生产构建通过，64 个模块完成转换。
- 浏览器确认初始“单图”状态下，四张输入卡同时存在，生成支路四个模块为高亮，重建支路两个模块为灰显。
- 点击“多视图”后，输入卡 `aria-pressed` 正确切换；生成支路整体灰显，Any-Modal 与 WorldMirror 2.0 重建支路转为高亮。
- 桌面 `1280×900` 视口下，大图容器 `clientWidth` 与 `scrollWidth` 均为 `949px`，完整系统图无需横向滚动。
- 手机 `390×844` 视口下，页面 `clientWidth` 与 `scrollWidth` 均为 `375px`；大图在 `279px` 内部容器中以 `985px` 内容宽度独立滚动，页面本身没有横向溢出。
- `git diff --check` 通过，无空白错误。

### 审查重点

- 四张输入任务卡必须同时可见，不能再一次只展示一张任务流程。
- 点击文本 / 单图时应高亮完整生成主链；点击多视图 / 视频时应高亮重建支路。
- 未选输入与未经过模块必须明显灰显，但文字仍应可辨认。
- WorldMirror 2.0 与显式三维资产必须保持为两条路径的共同落点。

## V0.34「双铭归位·无重叠结界」

日期：2026-08-16

### 修改内容

- 修复模块 6.2 在学生分布收敛后，两条分布标题随峰值靠近而可能互相覆盖的问题。
- 修复 Canvas 底部带噪样本 `x_t`、`s_real` 与 `s_fake` 标签在窄间距状态下的垂直重叠风险。
- 缩短右下角状态提示，使桌面和手机端都能在固定画布内完整显示。

### 模块变更明细

- 调整模块 6.2“DMD 分布校准仪”，组件文件为 `src/modules/hy-dmd-lab.tsx`。
- 教师铭牌固定到画布左上角并增加“实线”提示；学生铭牌固定到右上角并增加“虚线”提示，不再绑定动态峰值横坐标。
- 带噪样本标签移动到独立的 258px 高度层，`s_real` / `s_fake` 箭头分别下移到 288px / 316px。
- 右下角状态由长句缩短为“分数差推动学生靠近教师 / 分数方向已接近”，字号降为 9px，避免与第二条 score 箭头冲突。
- 调整 `src/styles/paper.css`：DMD Canvas 使用 `min(100%, 620px)` 与 `620 / 330` 固定宽高比，手机端不再保留 330px 固定高度而横向压扁。

### 证据边界

- 本版本只调整教学图形排版，不改变 DMD 公式、交互状态、收敛规则或论文结论。
- “实线 / 虚线”是教程视觉编码，不是论文对真实分布与伪分布的绘图规范。

### 验证结果

- TypeScript 检查与 Vite 生产构建通过，64 个模块完成转换；paper-skill 结构校验继续保持 10 章、16 个主动模块、5 个双模块章节与 18 个注册组件。
- 桌面端初始态截图确认教师 / 学生铭牌分别固定在左上与右上；4 次更新收敛后曲线靠近，但铭牌位置不变。
- 收敛态截图确认 `带噪样本 x_t`、`s_real`、`s_fake` 与右下状态提示分处不同垂直层，没有文字互相覆盖。
- 首次手机复测发现公共画布工具的内联 `height: 330px` 覆盖普通响应式高度；增加局部 `height: auto !important` 后，Canvas 从错误的 `283×329` 修正为约 `283×150`。
- 修正后手机端 Canvas 宽高比为 `1.887`，接近原始 `620 / 330`；浏览器内容区 `clientWidth` 与 `scrollWidth` 均为 `375px`，页面无横向溢出。
- 浏览器教程控制台无 warning 或 error。
- 仓库结构校验、PR 范围检查、总目录重新生成与 `build:paper` 单篇正式构建全部通过。

### 审查重点

- 收敛后左右铭牌必须保持分离，不能随曲线峰值靠近而重叠。
- `x_t`、`s_real`、`s_fake` 三层文字必须上下分开，且都在 330px 画布内。
- 手机端 Canvas 缩放后文字不得被边界裁切。

## V0.33「谱系折镜·七曜参照切换器」

日期：2026-08-16

### 修改内容

- 为模块 10.2 新增“只看 HY 谱系 / 查看全部参照”分段切换，先聚焦代际变化，再按需加入外部模型。
- 修正入口文案“点击任意单元格”与未报告灰格不可点击之间的矛盾，明确只有有证据状态才能展开。
- 范围切换只改变可见模型列，不改变能力状态、证据强度、发表时间和原有顺序。

### 模块变更明细

- 调整模块 10.2“模型能力进化图鉴”，组件文件为 `src/modules/hy-model-evolution.tsx`。
- 新增谱系视图：固定显示 HY-World 2.0、HY-World 1.5、HY-World 1.0、WorldMirror 1.0，目标模型仍处于第一列。
- 新增全部视图：在谱系四列之后追加 GenEx、video2world 与 Marble 1.0，保持原有外部参照顺序。
- 当读者在全部视图选中外部模型后切回谱系视图，详情自动回到 `HY-World 2.0 × 当前功能`，避免详情指向已经隐藏的模型。
- 矩阵 ARIA 标签随范围变化，分段按钮提供 `aria-pressed` 状态；灰色未报告按钮继续保持原生 `disabled`。
- 调整 `src/styles/paper.css`：新增两段范围控件、谱系四列矩阵宽度，以及手机端单列控件和 644px 内部滚动宽度。
- 调整 `REFERENCES.md`：说明范围控件仅改变可见列，不改变比较结论。

### 证据边界

- 谱系与全部范围属于教程阅读工具，不是论文定义的模型分类或排名。
- WorldMirror 1.0 是重建分支前代，不是 HY-World 完整系统的线性代际版本；界面仍保留“重建分支前代”标签。
- 外部模型被隐藏时，其数据与状态没有删除或重新计算；重新切回全部视图后保持原值。
- “未报告”仍不等于“不支持”，无论在哪个范围都不可点击。

### 验证结果

- TypeScript 检查与 Vite 生产构建通过，64 个模块完成转换。
- paper-skill 结构校验通过：保持 10 个章节、16 个主动模块、5 个双模块章节与 18 个已注册组件。
- 浏览器确认初始谱系表头依次为 `HY 2.0 / HY 1.5 / HY 1.0 / WM 1.0`，谱系范围有 7 个禁用“未报告”灰格。
- 切换全部参照后，右侧只追加 `GenEx / video2world / Marble 1.0`，共 7 列、15 个禁用灰格，前四列顺序不变。
- 选中 `Marble 1.0 × 文本 / 单图世界生成` 后切回谱系，详情自动回到 `HY-World 2.0 × 文本 / 单图世界生成`，当前功能轴保持不变。
- `390×844` 移动视口下，范围控件为单列；谱系矩阵宽 644px、全部矩阵宽 1016px，均在 283px 内部容器中独立滚动。
- 两种范围下浏览器内容区 `clientWidth` 与 `scrollWidth` 均为 `375px`，页面无横向溢出，教程控制台无 warning 或 error。
- 仓库结构校验、PR 范围检查、总目录重新生成与 `build:paper` 单篇正式构建全部通过。

### 审查重点

- 初始谱系视图表头必须依次为 HY 2.0、HY 1.5、HY 1.0、WM 1.0。
- 全部视图必须只在右侧追加 GenEx、video2world、Marble 1.0，不能改变前四列。
- 选中 Marble 后切回谱系，详情必须回到 HY-World 2.0 且保留当前功能轴。
- 两种视图中的未报告灰格都必须保持禁用；范围切换不能把灰格变成可点击。
- 手机端分段控件应单列，矩阵只能在自身内部横向滚动，页面不得溢出。

## V0.32「镜语秘匣·四枚术式解封」

日期：2026-08-16

### 修改内容

- 为模块 4.1 新增四个默认收起的专业术语暗格，补充 Keyframe-VAE、Plücker 射线、点图条件和选择性冻结的机制直觉。
- 增加灰色操作提示，让读者明确知道术语可以点击展开，同时保持主谜题与论文指标区紧凑。
- 所有解释均标注不可外推边界，不把三帧选择规则、几何条件或冻结策略改写成虚构的论文参数。

### 模块变更明细

- 调整模块 4.1“关键帧取景谜题”，组件文件为 `src/modules/hy-keyframes.tsx`。
- 新增“Keyframe-VAE 与 Video-VAE 差在哪？”暗格：解释时间压缩与关键帧保真的差别，并明确论文没有三帧限制。
- 新增“Plücker 射线在告诉模型什么？”暗格：解释相机中心、像素射线方向与真实深度之间的区别。
- 新增“点图条件为什么能补充射线？”暗格：解释逐像素三维位置与射线方向的互补性，不生成虚构综合分数。
- 新增“为什么选择性冻结会有取舍？”暗格：解释保留预训练先验、相机控制误差与视觉指标之间的消融权衡。
- 调整 `src/styles/paper.css`：新增术语标题、灰色提示、四列展开槽，以及平板两列、手机单列布局。
- 调整 `REFERENCES.md`：记录四个暗格与模块 4.1 原有证据链的关系和不可外推范围。

### 证据边界

- 术语内容基于论文关键帧潜空间、双重相机条件和选择性冻结消融；不新增实验数字。
- Plücker 射线提供观察几何，不等于真实深度；点图提供空间位置线索，也不保证所有输入几何无误差。
- 选择性冻结是论文报告的训练取舍，不能概括成“冻结越多越好”的通用规律。
- 上方谜题的 8 个候选、选择 3 帧、最小间隔 2 与首尾跨度 6 仍是教程规则，不是论文超参数。

### 验证结果

- TypeScript 检查与 Vite 生产构建通过，64 个模块完成转换。
- paper-skill 结构校验通过：保持 10 个章节、16 个主动模块、5 个双模块章节与 18 个已注册组件。
- 浏览器确认四个术语暗格初始 `open=0`；点击第一项后只有该项展开，正文完整显示且灰色操作提示在未展开时可见。
- `390×844` 移动视口下，术语区为单列，每个暗格 `clientWidth` 与 `scrollWidth` 均为 `283px`，浏览器内容区 `clientWidth` 与 `scrollWidth` 均为 `375px`。
- 手机截图确认最长标题自然换行，展开正文不遮挡后续条目；页面按钮无文字溢出，教程控制台无 warning 或 error。
- 仓库结构校验、PR 范围检查、总目录重新生成与 `build:paper` 单篇正式构建全部通过。

### 审查重点

- 四个术语默认必须收起，点击任一标题后只展开对应解释。
- 灰色提示必须在未展开时直接可见，不能让读者猜测交互方式。
- 手机端四个暗格必须单列，最长标题和正文不得撑宽页面。
- 术语解释不能改变 RotErr / ATE 数值，也不能把三帧谜题规则写成论文设置。

## V0.31「十景轮舞·类比幻灯万花筒」

日期：2026-08-16

### 修改内容

- 完全重做 10 个章节顶部的小型类比动画，删除 9 章重复使用“相机沿同一折线走向同一目标框”的换皮逻辑。
- 为每章分配不同的主动作：任务分流、环拍、避障、关键帧取景、记忆检索、四拍跳步、尺度伸缩、旋钮选输出、双片对齐、角色回访。
- 保留统一的空间摄影视觉语言、色彩语义和单主体动作约束，同时让读者在滚动章节时能立即感知机制差异。

### 模块变更明细

- 调整公共类比组件 `src/modules/hy-analogy.tsx`；不修改章节正文、论文数字或主动模块注册。
- 第 1 章“定任务”：相机在“稀疏线索 → 生成”和“丰富观察 → 重建”两条分支之间交替，目标与颜色同步变化。
- 第 2 章“转全景”：保留环绕主体的 360° 动作，增加中心物体与角度标签，使环拍目标更明确。
- 第 3 章“选路线”：相机沿上绕路径避开墙体并到达观察目标，突出规划不是直线穿墙。
- 第 4 章“拍关键帧”：相机在 K1 / K2 / K3 三个离散视角间跳转并触发快门光圈，强调少量跨视角观察。
- 第 5 章“查记忆”：一张紫色历史照片从三格照片架移动到目标槽，替代继续移动相机的旧动画。
- 第 6 章“压步数”：相机标记在四个学生推理拍点间跳步，背景八点只表示“多步教师”的教学对照。
- 第 7 章“换尺度”：画框从小分辨率伸展到大分辨率，绿色相对位置点保持同一归一化比例。
- 第 8 章“拨输出”：橙色旋钮在点图、深度、法线和 3DGS 四个输出方向间转动，表达共享骨干后的多头输出。
- 第 9 章“对齐”：第二张橙色深度图从旋转偏移状态滑入绿色参考图位置，直观呈现尺度与偏移校准。
- 第 10 章“交付”：橙色角色绕已经保存的世界资产回访，强调 WorldLens 交互发生在生成完成之后。

### 证据边界

- 本版本只改变教学类比动画，不新增论文事实、实验数值或能力结论。
- 第 1 章两条分支对应论文的稀疏输入生成与丰富观察重建；交替节奏不是实际路由时延。
- 第 6 章八点与四拍仅表达“教师多步 / 学生四步”的概念差异，不表示教师的真实采样步数。
- 第 8 章旋钮只切换阅读焦点，WorldMirror 2.0 的多类输出头在一次前馈中同时在线。
- 第 10 章角色回访表示生成后资产运行时，不表示生成模型会随角色动作实时重算完整世界。

### 验证结果

- TypeScript 检查与 Vite 生产构建通过，64 个模块完成转换。
- paper-skill 结构校验通过：保持 10 个章节、16 个主动模块、5 个双模块章节与 18 个已注册组件。
- 浏览器展开全部 10 章，确认 10 张类比 Canvas 均进入 `is-ready` 绘制状态。
- 桌面端截图抽查第 3 / 5 / 8 / 9 / 10 章：分别显示绕墙避障、照片入槽、旋钮选输出、双片对齐和角色绕资产回访，主要动作不再重复。
- 第 3 章路径绕过墙体后到达绿色观察框；第 9 章橙色深度图从旋转偏移状态向绿色参考图滑动；第 10 章角色在已保存世界外侧运动。
- `390×844` 移动视口下，10 张 Canvas 均保持 `244×130` 与 `1.877` 宽高比，浏览器内容区 `clientWidth` 与 `scrollWidth` 均为 `375px`。
- 页面按钮无文字溢出，浏览器控制台无 warning 或 error。
- 仓库结构校验、PR 范围检查、总目录重新生成与 `build:paper` 单篇正式构建全部通过。

### 审查重点

- 10 张动画的主要动作必须明显不同，不能只看标题才能分辨章节。
- 第 3 章相机不得穿过墙体；第 9 章橙色照片必须逐渐与绿色照片对齐。
- 第 6 章只能显示四个学生拍点，不能让八个背景点被误读为论文教师步数。
- 第 10 章角色必须绕已保存世界移动，不能表现成世界在角色动作后实时重新生成。
- 手机端 244×130 Canvas 应保持完整比例，无文字遮挡或裁切。

## V0.30「幻步炼金·四拍分布校准仪」

日期：2026-08-16

### 修改内容

- 在第 6 章新增 DMD 分布校准交互，把公式中的 `s_real`、`s_fake` 与 `J_θ` 从符号说明升级为可操作的分布匹配过程。
- 读者可切换三个教学噪声层级，并反复执行 DMD 更新，观察学生伪分布沿分数差方向靠近教师真实分布。
- 新增明确的生命周期边界：四步 DiT 只属于 WorldStereo 2.0 生成器，完整世界生成仍由 Table 10 的 712 秒约束。
- 在模块底部接入完整论文表 8，方便核查相机控制、记忆配置和后蒸馏行，而不把教学点击次数伪装成实验数据。

### 模块变更明细

- 新增模块 6.2“DMD 分布校准仪”，组件文件为 `src/modules/hy-dmd-lab.tsx`，并在 `src/modules/registry.tsx` 注册 `hy-dmd-lab`。
- 调整 `src/data/tutorial.ts`：第 6 章由单模块扩展为 6.1 训练故障诊断 + 6.2 DMD 分布匹配两个互补模块。
- Canvas 新增绿色教师分布、紫色学生分布、同一带噪样本 `x_t`、`s_real` / `s_fake` 双箭头与分数差反馈；每次更新后学生曲线位置和定性差距同步变化。
- 新增较低 / 中等 / 较高噪声三个标签页；切换时曲线宽度、箭头与解释同步更新，但不展示虚构的论文时间步数。
- 新增 DMD 更新控制：显示教学更新次数、收敛状态、禁用状态与一键重置；达到示意收敛后按钮改为“分布已靠拢”。
- 新增三格 score 账本、四步推理边界双栏、`J_θ` 与 score 解释扩展槽，以及可展开 Table 8。
- 调整 `src/styles/paper.css`：增加噪声标签、score 账本、更新控制、边界区、术语槽和移动端单列布局。
- 调整 `REFERENCES.md`：逐项记录一维曲线、噪声档位、点击次数与论文 DMD 目标之间的映射和不可外推边界。

### 证据边界

- DMD 公式、真实 / 伪 score、带噪样本 `x_t`、学生雅可比 `J_θ` 与四步 DiT 来自论文第 5.3 节。
- 一维钟形曲线、三档噪声、曲线初始偏移、每次缩小比例和收敛阈值均为教程设计，不是论文参数或训练曲线。
- Table 8 的配置 G 是后蒸馏结果；上方交互不会计算或改变表格指标，也不能从教学收敛推导 PSNR、SSIM 或相机误差。
- 四步只描述 WorldStereo 2.0 生成器推理，不能替代 Table 10 的端到端 712 秒。

### 验证结果

- TypeScript 检查与 Vite 生产构建通过，64 个模块完成转换。
- paper-skill 结构校验通过：10 个章节、16 个主动模块、5 个双模块章节、18 个已注册组件。
- 仓库结构校验与 PR 范围检查通过，敏感信息和本地绝对路径扫描无命中。
- 浏览器在默认中等噪声状态确认分数差初始为“较大”，执行一次更新后变为“正在缩小”；连续 4 次更新后变为“很小”，按钮改为“分布已靠拢”并禁用。
- 修复 Canvas 只在首次挂载时绘制的问题；前后截图确认紫色学生分布峰值会随 4 次更新从右侧实际移动到绿色教师分布附近，而不是只更新文字状态。
- 三档噪声标签可切换；较高噪声标签的 `aria-selected` 正确更新，教师 / 学生曲线宽度与 score 示意同步变化。
- 重置按钮恢复 0 次更新与初始分布间距；Table 8 可展开并显示 9 行完整消融，灰色展开与横向滑动提示可见。
- `390×844` 移动视口下，浏览器内容区 `clientWidth` 与 `scrollWidth` 均为 `375px`；噪声标签、score 账本、更新控制、边界区和术语槽均为单列。
- 页面按钮无文字溢出，DMD Canvas 已正确绘制，浏览器控制台无 warning 或 error。
- 总目录重新生成通过，仓库 `build:paper` 单篇正式构建通过，产物已更新到本地 `site/papers` 预览目录。

### 审查重点

- 连续执行更新时，紫色学生分布应向绿色教师分布靠近，分数差从“较大”变为“正在缩小”再变为“很小”。
- 三档噪声只应改变教学曲线宽度与 score 示意，不能出现论文未报告的数值时间步。
- 收敛后更新按钮必须禁用，重置按钮应恢复 0 次更新与初始分布间距。
- 模块必须始终同时显示“四步 WorldStereo”与“712 秒完整生成”的边界，避免再次制造实时生成误读。
- 手机端标签、账本、更新按钮、双栏边界和扩展槽应全部单列，不得撑宽页面。

## V0.29「时环纠偏·七百十二秒真名录」

日期：2026-08-16

### 修改内容

- 修复模块 1.2 条款 05 把完整生成耗时误定位到 Table 2 的证据错误；真正来源是论文 Table 10。
- 新增完整论文表 10，列出单个世界生成的五个阶段耗时与 `712s` 总计。
- 当读者查看“WorldLens 实时漫游不等于完整生成实时”条款时，自动显示可展开 Table 10，让局部运行时交互与端到端离线生成的区别可直接核查。
- 记录论文正文“10 minutes”与表格精确 `712s ≈ 11.9min` 的近似差异，教程统一使用表格精确值。

### 模块变更明细

- 调整模块 1.2“统一框架契约校验台”，组件文件为 `src/modules/hy-boundary-compare.tsx`。
- 条款 05 的证据定位由 `Abstract；Introduction；Table 2` 修正为 `Abstract；Introduction；Table 10；Figure 22`。
- 当当前条款为 05 时，在总反馈与术语槽之间渲染 `PaperTable tableId="table-10"`；切换其它条款时表 10 隐藏，避免无关证据占用空间。
- 调整 `src/modules/hy-paper-evidence.tsx`：扩展 `PaperTableId` 并新增完整 Table 10。
- Table 10 列出全景生成 `15s`、轨迹规划 `182s`、世界扩展 `286s`、重建与对齐 `102s`、3DGS `127s`、总计 `712s`，重点高亮总计列。
- 表格说明明确硬件为 NVIDIA H20，并区分端到端生成与 WorldLens 生成后资产实时运行时。
- 调整 `REFERENCES.md`：写明 Table 2 是三类视频生成范式比较，不能支持 712 秒结论；补充 Table 10 与 Figure 22 的正确证据链。

### 证据边界

- Table 10 的分项和总计均来自论文单个世界生成效率表，硬件条件为 NVIDIA H20。
- `712s` 是端到端世界生成，不是 WorldMirror 单步重建、WorldStereo 四步采样或 WorldLens 渲染帧率。
- Figure 22 只展示生成后世界中的实时碰撞和角色探索，不能替代生成耗时。
- 论文正文的 “10 minutes” 是近似概括；表格精确总计为 712 秒，教程不将二者伪装成完全相等。
- Table 2 比较 Native VDM、AR、WorldStereo 2.0 的感受野、轨迹、潜空间与质量属性，不用于运行时间结论。

### 验证结果

- TypeScript 检查与 Vite 生产构建通过，63 个模块完成转换。
- paper-skill 结构校验通过：10 个章节、15 个主动模块、4 个双模块章节、17 个已注册组件。
- 仓库结构校验与 PR 范围检查通过，敏感信息和本地绝对路径扫描无命中。
- 浏览器实测条款 05：证据定位显示 `Abstract；Introduction；Table 10；Figure 22`，Table 10 可展开，唯一数据行依次为 `15 / 182 / 286 / 102 / 127 / 712`。
- 浏览器切换到条款 01 后，Table 10 隐藏且定位恢复为 `Abstract；Introduction；Figure 2`。
- `390×844` 移动视口下，浏览器内容区 `clientWidth` 与 `scrollWidth` 均为 `375px`；表格在 `250px` 容器内以 `696px` 内容宽度独立横向滚动，页面无横向溢出。
- 页面按钮无文字溢出，浏览器控制台无 warning 或 error；灰色“点击展开/左右滑动”提示在折叠与展开状态均可见。
- 总目录重新生成通过，仓库 `build:paper` 单篇正式构建通过，产物已更新到本地 `site/papers` 预览目录。

### 审查重点

- 条款 05 必须显示 `Table 10` 定位，不能再出现 `Table 2`。
- 只有查看条款 05 时才显示论文表 10；切换条款 01-04 后表格应隐藏。
- 表 10 五个分项之和必须为 `712`，不得误写成 600 秒或把 5.60 秒混入总计。
- 手机端表 10 应在内部横向滚动，不得撑宽页面。

## V0.28「双表星门·尺度证据补全仪」

日期：2026-08-16

### 修改内容

- 修复模块 7.1 引用论文表 12、但只能展开表 11 的证据缺口。
- 新增完整论文表 12，将相机姿态 / 深度左子表与新视角合成右子表合并为一张可展开、可横向滑动的表。
- 在表 12 与表 11 前分别增加灰色提示与阅读顺序，明确两表任务、指标方向和低分辨率尺寸不同。
- 保留“未报告不等于不支持”：合并表中的 `-` 只表示原论文对应子表没有数值。

### 模块变更明细

- 调整模块 7.1“分辨率外推变插值”，组件文件为 `src/modules/hy-resolution.tsx`。
- 在交互反馈后新增“先看表 12”提示，说明它核查位置编码相关的相机姿态、深度与 NVS 跨分辨率结果。
- 新增 `PaperTable tableId="table-12"`，包含 Fast3R、CUT3R、FLARE、VGGT、π3、AnySplat、WorldMirror 1.0 L/M/H、WorldMirror 2.0 L/M/H 共 12 个合并行。
- 表 12 列包含 AUC@30、RTA@30、AbsRel、δ<1.25、NVS PSNR、NVS SSIM、NVS LPIPS，并保留各指标方向。
- 对只出现在左子表或右子表的方法使用 `-`，表格说明明确“未报告不等于不支持”。
- 高亮 WorldMirror 1.0 H 与 WorldMirror 2.0 H 两行，便于核对 AUC `66.29 → 86.89` 的高分辨率差异，同时保留其它指标的非单调变化。
- 在表 11 前新增“再看表 11”提示，说明它负责 7-Scenes 点图重建与全部先验端点。
- 协议提示继续区分 Table 12 的 L=`189×259` 与 Table 11 的 L=`182×252`；M/H 两处一致。
- 调整 `src/data/tutorial.ts`：模块描述新增展开表 12 / 表 11 核对不同协议。
- 调整 `src/styles/paper.css`：新增双表阅读提示样式，使用灰底、小字与蓝色左边界。
- 调整 `src/modules/hy-paper-evidence.tsx`：扩展 `PaperTableId` 并注册完整 Table 12 数据。
- 调整 `REFERENCES.md`：新增 Table 12 数据集、指标方向、跨分辨率证据与两表尺寸差异说明。

### 证据边界

- 相机姿态与深度在 RealEstate10K 上评估；NVS 是 RealEstate10K 与 DL3DV 平均，两侧不是同一个任务。
- 基线方法按 M 分辨率评估，教程不为它们补造 L/H 数值。
- WorldMirror 2.0 H 的 AUC 明显高于 1.0 H，但 NVS PSNR、LPIPS 等指标并非全部随分辨率单调改善。
- `-` 只表示原论文对应子表未报告，不可改写为“不支持”或“失败”。
- 表 12 与表 11 的低分辨率尺寸不同，教程不能把两表数字混成一个统一协议。

### 验证结果

- Vite 生产构建通过，63 个模块完成转换。
- paper-skill 结构校验通过，保持 10 个章节、15 个主动模块、4 个双模块章节与 17 个注册组件。
- 仓库 `catalog`、`validate`、`validate:pr -- main` 与 `build:paper -- hy_world_2_0` 全部通过，教程成功重新构建到 `site/papers`。
- 浏览器确认模块 7.1 内论文表格数量由 1 增加为 2，DOM 顺序为表 12 在前、表 11 在后，两段阅读提示顺序正确。
- 展开表 12 后包含 12 个方法行、8 个表头单元（方法 + 7 个指标）；`-`、AUC `66.29 / 86.89`、NVS PSNR `17.78 / 19.98` 均准确存在。
- 表 12 灰色提示明确显示“点击展开 / 收起、过宽时左右滑动”，右侧范围说明显示“完整左右子表合并；缺少对应任务报告时记为 -”。
- 桌面截图确认 12 行数据、两条高分辨率高亮行、来源说明和原论文页码链接清晰，无列标题或数字重叠。
- 390×844 手机端页面内容宽度与滚动宽度均为 375px；表 12 / 表 11 的内部容器均为 250px，表格内容宽度分别为 788px / 626px，可独立横向滑动。
- 手机截图确认表 12 首列与 AUC 列可读、底部横向滚动条可见，“再看表 11”提示紧随其后；页面日志无错误或警告。

### 审查重点

- 模块 7.1 应按“交互 → 表 12 → 表 11”顺序出现，两张表都带明显灰色展开提示。
- 表 12 应包含 12 个合并方法行与 7 个指标列；FLARE 同时有左右两侧数字，AnySplat 只有 NVS 数字。
- WorldMirror 1.0 H / 2.0 H 的 AUC 应为 `66.29 / 86.89`，NVS PSNR 应为 `17.78 / 19.98`。
- 手机端两张表必须使用各自内部横向滚动，不得撑宽页面。

## V0.27「双源铭文·全景创新补完章」

日期：2026-08-16

### 修改内容

- 完成新增 2.2 后的跨章节一致性修正：第 2 章洞见、章节总结和 8.2 创新证据星图都纳入 HY-Pano 2.0 的数据策展贡献。
- 将 HY-Pano 创新从单纯“隐式映射 + 接缝修复”扩展为“数据策展 + 隐式映射 + 双层边界修复”三部分。
- 明确表 4 是完整 HY-Pano 2.0 结果，论文没有公开真实 / 合成固定配比或数据策展独立消融，避免新增模块后出现过度归因。

### 模块变更明细

- 调整模块 8.2“创新证据星图”，组件文件为 `src/modules/hy-innovation-map.tsx`。
- HY-Pano 节点标题改为“双源数据策展、隐式映射与环形修复”，贡献类型改为“数据与生成方法改进”。
- HY-Pano 节点新增 `training` 标签，因此它会同时出现在“核心方法”和“训练与效率”筛选中；训练筛选计数随之增加。
- 旧问题新增“高质量全景数据稀缺”；机制新增高分辨率真实全景、UE 合成资产与明显污染过滤。
- 效果说明拆分为数据侧与模型侧：数据侧拓宽真实质感、精确标签和想象场景分布，模型侧降低相机元数据依赖并改善环形连续性。
- 证据定位从“HY-Pano 2.0、表 4”细化为 `Section 3.1-3.2、Figure 3、Table 4`。
- 边界新增“无固定双源配比、无数据独立消融、表 4 不能归因给单一机制”。
- 关键词新增“双源数据”，保留隐式映射、ERP 接缝和循环填充。
- 调整第 2 章 `insight`：从只讲模型机制改为同时概括双源数据、质量过滤、隐式映射和双层接缝修复。
- 调整第 2 章第一条 takeaway：由泛化的“全局初始化”改为“双源策展”，明确真实 / 合成互补与污染过滤。
- 调整 `REFERENCES.md` 创新证据索引中的 HY-Pano 行，补充 Section 3.1-3.2 及不可外推边界。

### 证据边界

- 双源数据与过滤策略来自论文 Section 3.1；隐式映射、循环填充和像素融合来自 Section 3.2 与 Figure 3。
- Table 4 数字来自完整 HY-Pano 2.0 的 T2P / I2P 评测，论文没有给出数据策展、隐式映射或两类接缝机制的独立贡献数字。
- 将数据贡献加入 `training` 筛选是教程的信息组织方式，不表示论文把它命名为训练算法。
- 章节 takeaway 只总结论文已说明的双源价值与重点污染，不增加数据规模、比例或过滤阈值。

### 验证结果

- Vite 生产构建通过，63 个模块完成转换。
- paper-skill 结构校验通过，保持 10 个章节、15 个主动模块、4 个双模块章节与 17 个注册组件。
- 仓库 `catalog`、`validate`、`validate:pr -- main` 与 `build:paper -- hy_world_2_0` 全部通过，教程成功重新构建到 `site/papers`。
- 浏览器逐章打开到第 8 章，确认第 2 章 takeaway 已显示“双源策展”，创新星图显示新标题“双源数据策展、隐式映射与环形修复”。
- “训练与效率”筛选计数由 3 项增加为 4 项；HY-Pano、WorldStereo、WorldMirror、3DGS + WorldLens 四个节点均正常显示。
- 在“训练与效率”筛选中点击 HY-Pano 节点，详情区正确显示高分辨率真实全景、UE 合成资产、质量过滤、隐式映射和双层接缝修复。
- 详情区明确显示“没有公开真实 / 合成固定配比或数据策展独立消融”，证据定位为 `Section 3.1-3.2、Figure 3、Table 4`。
- 桌面截图确认四个筛选、四个训练节点、三段创新解释、证据 / 边界双栏和关键词均无重叠；页面日志无错误或警告。

### 审查重点

- HY-Pano 节点应同时出现在“核心方法”和“训练与效率”筛选中，且两处内容一致。
- 节点必须显示双源数据、隐式映射和环形修复三类贡献，不能把表 4 数字归因给其中一项。
- 第 2 章第一条 takeaway 应显示“双源策展”，原“全局初始化”不再重复占用总结位。

## V0.26「双源炼景·星砂样本裁决场」

日期：2026-08-16

### 修改内容

- 新增模块 2.2，补上原教程只讲 HY-Pano 2.0 模型、不讲第 3.1 节数据策展的问题。
- 新增六张全景教学样本裁决：真实拍摄与 UE 合成各三张，读者需要保留高质量双源数据并剔除明显接缝或拍摄设备污染。
- 新增双源样本架、逐样本理由、错误反馈、清空操作和四个术语扩展槽，使数据质量与域差距成为可操作的学习内容。
- 明确六张画面、场景名与数量均为教程构造，不伪装成论文原图、真实数据配比或自动过滤器输出。

### 模块变更明细

- 新增模块 2.2“全景数据策展暗房”，组件文件为 `src/modules/hy-panorama-curation.tsx`，并在 `src/modules/registry.tsx` 注册 `hy-panorama-curation`。
- 新增 R1“玻璃庭院酒店”：真实高分辨率样本，无明显污染，正确裁决为收入训练集。
- 新增 S1“浮空遗迹”：UE 合成样本，提供精确标签与想象场景，正确裁决为收入训练集。
- 新增 R2“夜间商业街”：明显左右接缝，正确裁决为剔除。
- 新增 R3“室内会客厅”：全景相机设备入镜，正确裁决为剔除。
- 新增 S2“星港控制舱”：高质量 UE 合成样本，扩展语义分布，正确裁决为收入训练集。
- 新增 S3“峡谷训练场”：边界拼接破损，正确裁决为剔除；强调质量过滤不因数据来源而豁免。
- 六张样本卡显示来源、污染线索和当前裁决；选择样本后，检查器同步更新大图、论文理由、收入 / 剔除控制和即时反馈。
- 新增“已收入样本架”，分别统计当前教学状态中收入的真实与合成样本，并明确计数不代表论文配比。
- 六张全部正确时进入绿色完成状态；全部作答但存在错误时，反馈提示“来源不是唯一标准”。
- 新增清空裁决按钮，一次恢复六张待检状态与 R1 焦点。
- 新增四个扩展槽：真实数据价值、合成数据价值、domain gap、设备入镜污染。
- 调整 `src/data/tutorial.ts`：第 2 章由单模块升级为双模块，新增 2.2 的标题、描述与组件映射。
- 调整 `src/styles/paper.css`：新增六卡样本网格、纯 CSS 全景示意、污染标记、检查器、双源样本架、证据边界和响应式布局。
- 调整 `REFERENCES.md`：新增 Section 3.1 的双源数据、接缝过滤、设备过滤与域差距证据映射。

### 证据边界

- 论文明确使用高分辨率真实全景与高质量引擎合成资产两类来源，并说明它们分别提供真实外观先验与精确标签 / 多样场景。
- 论文明确过滤低质量样本，特别是明显拼接痕迹和暴露拍摄设备的样本。
- R1-S3 的画面、场景、污染位置、六题数量和正确计数均为教程构造，不是论文公开数据样例或比例。
- 教程只判断论文明确描述的两类重点污染，没有虚构清晰度分数、过滤阈值、数据规模或真实 / 合成固定比例。
- “domain gap 减轻”来自论文对混合数据策略的定性说明，教程不生成未报告的数值。

### 验证结果

- Vite 生产构建通过，63 个模块完成转换。
- paper-skill 结构校验通过，教程升级为 10 个章节、15 个主动模块、4 个双模块章节与 17 个注册组件。
- 仓库 `catalog`、`validate`、`validate:pr -- main` 与 `build:paper -- hy_world_2_0` 全部通过，教程成功重新构建到 `site/papers`。
- 浏览器初始状态显示六张待检样本、`0/6` 和真实 / 合成样本架 `0 / 0`。
- 将六张全部裁决为“收入训练集”后，R1、S1、S2 正确，R2、R3、S3 冲突，总反馈准确报告 3 张错误。
- 将 R2、R3、S3 改为“从暗房剔除”后进入 `6/6` 完成状态，样本架显示真实拍摄 1、UE 合成 2。
- 当前 S3 检查器显示 UE 合成来源、边界拼接破损、红色接缝示意和“无论来源为何都需过滤”的正确理由。
- 点击清空裁决后，六张全部恢复待检，计数回到 `0/6`，双源样本架归零，焦点回到 R1。
- 桌面截图确认六卡网格、纯 CSS 样本画面、检查器、双源样本架和完成反馈没有重叠。
- 390×844 手机端页面内容宽度与滚动宽度均为 375px；模块宽度 284px，样本卡 `scrollWidth=clientWidth=283px`，动作按钮组为 257px，无页面级横向溢出。
- 手机端六张样本、检查器、样本架和扩展槽按单列排列；页面日志无错误或警告。

### 审查重点

- 六张正确裁决必须依次为“收入 / 收入 / 剔除 / 剔除 / 收入 / 剔除”。
- S1、S2 不得因为“合成”来源被默认判为低质量；S3 必须因明显接缝被剔除。
- R2、R3 的污染示意和理由应分别对应 stitching artifacts 与 capturing equipment。
- 六张全部正确后应显示 `6/6`；清空后全部恢复待检，双源样本架计数归零。
- 手机端六张样本、检查器、双源样本架和扩展槽不得撑宽页面。

## V0.25「双界誓约·万象系统合议庭」

日期：2026-08-16

### 修改内容

- 完全重做模块 1.2，删除点击一次后自动播放“传统割裂 / 条件化统一”双相机折线路径的被动动画。
- 新增五条系统契约判断题，让读者主动辨认稀疏输入生成、丰富观测重建、重建回流、系统级统一和运行时交互之间的关系。
- 新增实时技术管线拼图、逐条论文证据卡、错误状态与重置操作，专门纠正“统一就是一个万能网络”和“可实时漫游就是可实时生成”两类误读。
- 新增四个概念扩展槽，补充系统级统一、重建支撑生成、离线生成与实时漫游、论文首创范围。

### 模块变更明细

- 调整模块 1.2，标题由“传统割裂与统一框架”改为“统一框架契约校验台”，真实注册组件文件为 `src/modules/hy-boundary-compare.tsx`。
- 新增条款 01：文本 / 单图输入需要生成未见区域，正确答案为“成立”。
- 新增条款 02：多视图 / 视频输入进入世界重建，正确答案为“成立”。
- 新增条款 03：WorldMirror 2.0 重建能力也支撑生成管线的最终三维合成，正确答案为“成立”。
- 新增条款 04：统一表示所有任务由一个单体网络完成，正确答案为“不成立”。
- 新增条款 05：WorldLens 可实时漫游，所以完整世界生成也是实时的，正确答案为“不成立”。
- 每条条款提供“成立 / 不成立”二元控制，选择后同步更新条款颜色、正确状态、当前证据、不可外推边界和技术管线 Canvas。
- Canvas 显示稀疏条件经全景 / 规划、世界扩展和 WorldMirror 形成 3DGS；丰富观测直接进入 WorldMirror；条款 03 正确时两条路径在重建节点汇合。
- 条款 04 错选“成立”时，Canvas 显示红色“一个万能网络”误读框；条款 05 错选“成立”时显示红色“实时生成？”警告。
- 五条全部正确后进入绿色“条件分流 + 重建回流 + 显式资产”状态；全部作答但仍有错误时，反馈给出剩余冲突数量。
- 新增重置按钮，一次恢复五条未判断状态与首条证据焦点。
- 新增四个扩展槽：“统一系统 ≠ 单一网络”“重建为何是生成的基础组件”“离线生成与实时漫游如何共存”“第一结论的范围是什么”。
- 调整 `src/data/tutorial.ts`：更新模块标题与描述，明确系统声明判断和两类误读纠正。
- 调整 `src/styles/paper.css`：新增契约头部、双栏工作台、五条判断卡、二元控制、证据状态、术语槽和手机端布局。
- 调整 `REFERENCES.md`：新增 Abstract、Introduction、Figure 2、Section 2 的五条契约证据矩阵与首创范围说明。

### 证据边界

- 五条正确答案依据论文 Abstract、Introduction、Figure 2 与 Section 2；它们是论文结构与定位判断，不是定量指标。
- “统一”指生成与重建被组织进同一个多阶段系统，论文并未声称由单一端到端网络完成全部任务。
- WorldMirror 2.0 既可独立执行多视图 / 视频重建，也在生成管线中承担世界合成前的三维重建；这不表示两条路径完全相同。
- WorldLens 的实时渲染和角色交互发生在资产生成之后，不能据此把完整世界生成称为实时闭环。
- 论文的首创表述带有“开源、系统化、多模态、离线三维范式”限定，教程不删除这些限定。

### 验证结果

- Vite 生产构建通过，62 个模块完成转换。
- paper-skill 结构校验通过，保持 10 个章节、14 个主动模块、3 个双模块章节与 16 个注册组件。
- 仓库 `catalog`、`validate`、`validate:pr -- main` 与 `build:paper -- hy_world_2_0` 全部通过，教程成功重新构建到 `site/papers`。
- 浏览器初始状态包含 5 条“尚未判断”，反馈显示已完成 `0/5`，技术管线使用灰色未决状态。
- 将五条全部选择“成立”后，前三条判断正确，第 4、5 条显示“与论文边界冲突”，总反馈准确报告 2 条冲突。
- 将第 4、5 条改为“不成立”后，五条全部变绿，计数更新为 `5/5`，Canvas 显示两类输入在 WorldMirror 汇合并输出 3DGS / 几何。
- 点击重置后，五条全部恢复“尚未判断”，完成反馈消失，首条证据重新成为当前焦点。
- 桌面截图确认 Canvas、当前条款证据和五条判断卡没有重叠；成立 / 不成立按钮颜色与正确状态一致。
- 390×844 手机端页面内容宽度与滚动宽度均为 375px；模块宽度 284px，判断卡 `scrollWidth=clientWidth=279px`，二元按钮组为 256px，无页面级横向溢出。
- 手机端标题、计数与重置按钮按两行排列，Canvas、证据卡、判断卡和术语槽依次单列；页面日志无错误或警告。

### 审查重点

- 五条正确答案必须依次为“成立 / 成立 / 成立 / 不成立 / 不成立”。
- 条款 04 与 05 错选“成立”时必须产生明确红色误读状态，不能把错误答案当成替代路线。
- 条款 03 成立时，Canvas 应显示稀疏生成路径与丰富观测路径都进入 WorldMirror，但不画成单体网络。
- 五条全部正确后应显示 `5/5` 和绿色总结合约；重置后全部恢复“尚未判断”。
- 手机端五条判断卡、二元按钮、Canvas 与术语槽不得撑宽页面。

## V0.24「环界侦探·全景裂缝取证所」

日期：2026-08-16

### 修改内容

- 完全重做模块 2.1，删除“显式投影 / 隐式映射 / 接缝修复”三个按钮只切换图示和指标条的被动观察交互。
- 新增三案式全景故障取证：读者需要根据相机元数据、潜空间周期边界和解码后像素边缘的症状，选择作用层级正确的修复机制。
- 新增错误机制反馈、逐案归档进度、技术管线 Canvas 和四个术语扩展槽，让每次选择同时更新图形、层级、判断和解释。
- 将表 4 从交互状态中彻底分离，明确它是完整 HY-Pano 2.0 的系统结果，不由上方选择器计算，也不能归因给单个机制。

### 模块变更明细

- 调整模块 2.1，标题由“全景初始化三步观察”改为“全景故障取证台”，真实注册组件文件为 `src/modules/hy-panorama.tsx`。
- 新增案件 A“相机档案缺页”：显式投影因焦距 / FoV 缺失或不准而错位，正确机制为 MMDiT 隐式自适应映射。
- 新增案件 B“潜空间两端失联”：ERP 左右特征在去噪阶段无法周期通信，正确机制为潜空间循环填充。
- 新增案件 C“解码边缘仍跳色”：最终像素边缘出现亮度或纹理突变，正确机制为解码后的线性像素融合。
- 新增三种修复卡。读者把任一修复应用到当前案件后，Canvas 会同步显示故障层、连线状态、左右边缘状态和“匹配 / 不匹配”判定；错误选择明确指出机制所在层级。
- 新增逐案归档状态：每个案件首次正确匹配后显示“已解”，完成全部三案后反馈总结“映射、潜空间边界、像素边缘缺一不可”。
- 新增“当前卷宗”信息区，逐案显示症状、诊断问题和提示，避免交互退化为按顺序点亮步骤。
- 新增论文证据区，固定显示 Section 3.2 与 Table 4 边界，并用 `CLIP-I 0.831 → 0.844` 作为完整系统锚点；该数字不随案件或按钮变化。
- 新增 ERP、MMDiT、循环填充与像素融合、生成补全四个扩展槽。
- 保留论文表 4 的完整 I2P 子表和灰色展开提示，继续支持表格内部横向滚动。
- 调整 `src/data/tutorial.ts`：更新模块标题与描述，明确“检查故障 → 匹配机制 → 原表核查”的学习顺序。
- 调整 `src/styles/paper.css`：新增案件网格、取证工作台、修复卡、论文边界区、术语扩展槽及手机端单列布局。
- 调整 `REFERENCES.md`：新增 Section 3.2、Figure 3 与 Table 4 的逐案证据映射和不可外推边界。

### 证据边界

- 论文明确说明隐式自适应映射通过 MMDiT 在统一潜空间学习透视图到 ERP 的对应，以减少对精确相机内参的依赖。
- 论文把循环填充和线性像素融合作为组合式接缝修复策略：前者位于潜空间去噪阶段，后者位于解码后的像素空间。
- 三个案件、逐案归档和错误选项均为教程设计，不是论文真实推理接口、训练超参数或消融配置。
- 表 4 没有报告三个机制的独立消融；`0.831 → 0.844` 只表示 HY-World 1.0 与完整 HY-Pano 2.0 在 I2P CLIP-I 上的比较。
- 隐式映射会根据生成先验补全未见区域，教程不把这些内容描述为真实测量或确定几何。

### 验证结果

- Vite 生产构建通过，62 个模块完成转换。
- paper-skill 结构校验通过，保持 10 个章节、14 个主动模块、3 个双模块章节与 16 个注册组件。
- 仓库 `catalog`、`validate`、`validate:pr -- main` 与 `build:paper -- hy_world_2_0` 全部通过，教程成功重新构建到 `site/papers`。
- 浏览器初始案件 A 选择“循环填充”时保持 `0/3`，反馈明确说明机制位于潜空间，不能直接处理相机内参依赖。
- 依次完成 A=隐式自适应映射、B=循环填充、C=线性像素融合后，三个案件均保留“已解”标记，进度更新为 `3/3`，总反馈显示三类层级缺一不可。
- 桌面截图确认案件网格、Canvas、当前卷宗、修复卡与论文证据区没有重叠；完成态 Canvas 正确高亮 ERP 输出边缘与像素融合。
- 论文表 4 展开后包含 5 行 I2P 指标，浏览器确认最后一项 Q-Align 美学（等距柱状）与 `4.056` 数值完整存在。
- 390×844 手机端页面内容宽度与滚动宽度均为 375px；模块宽度 284px，案件按钮 `scrollWidth=clientWidth=283px`，修复按钮 `279px`，无文字或控件撑宽页面。
- 手机端表 4 使用 250px 内部容器承载 543px 表格内容，可左右滑动且不造成页面级横向溢出。
- Canvas 已进入可见绘制状态；本地页面日志只有 Vite 连接与 React 开发提示，没有教程错误或警告。

### 审查重点

- 三个案件的正确机制应依次为“隐式自适应映射 / 循环填充 / 线性像素融合”，错误选择必须保持红色层级不匹配反馈。
- 解开一个案件后切换到其它案件，已解标记应保留；三案完成后应显示 3/3 和总结合格反馈。
- Canvas 的故障高亮应分别落在透视输入到潜空间、潜空间 ERP 边界、最终 ERP 像素边缘三个位置。
- 表 4 数字不得随上方选择变化，也不得出现单机制贡献归因。
- 手机端案件、修复卡、证据区和扩展槽不得撑宽页面。

## V0.23「星屑裁决·高斯预算炼金阵」

日期：2026-08-16

### 修改内容

- 完全重做模块 9.1，删除点击五个配置后仅替换点云密度和三条指标条的单选交互。
- 新增二维高斯预算实验：读者调节最大高斯数量与最低 PSNR，Canvas 即时生成可行域并筛选论文表 9 的五个真实配置。
- 新增帕累托前沿、最低数量推荐、无可行解反馈和配置点检查器，使数量与画质取舍从被动读数变成主动资源决策。
- 新增“完整配置不是无损压缩”的论文结论锚点，同时展示数量、PSNR 和 LPIPS 的真实变化。
- 新增体素降采样、自适应增密、MaskGaussian 三个机制扩展槽。

### 模块变更明细

- 调整模块 9.1，标题由“高斯数量与画质取舍”改为“高斯预算帕累托实验”，真实注册组件文件为 `src/modules/hy-composition.tsx`。
- Canvas 改为散点决策图：横轴为高斯数量 M、越低越轻；纵轴为 PSNR、越高越好；B/V/D/M/F 五个点严格使用表 9 数值。
- 新增绿色可行域，范围由橙色“最大高斯预算”竖线和“最低 PSNR”横线共同决定；不满足约束的点降低透明度。
- 新增教学帕累托前沿，连接 V、F、D、B 四个二目标非支配点；M 点标记为被 F 轻微支配，因为 F 的数量更少且 PSNR 更高。
- 新增“轻量交付”“画质审查”“极限压缩”三个教学情境预设，并明确阈值不是论文部署标准。
- 新增两个范围滑块：最大高斯预算 `1.0M-6.0M`，最低 PSNR `24.50-25.18`；每个滑块同时提供减号 / 加号步进按钮，避免触屏或浏览器拖动不稳定时失去操作入口。
- 新增三项诊断：“可行配置数量”“最低数量推荐”“当前检查点是否满足约束”。
- 推荐规则固定为“在可行点中优先选择高斯数量最少者，数量相同时 PSNR 更高者优先”，并明确不是论文自动部署策略。
- 新增五个论文配置坐标按钮；每个按钮显示配置代号、数量、PSNR、可行状态和是否被支配，点击后展开组件组合、SSIM、LPIPS 与机制解释。
- 新增论文结论锚点：`6.000M → 1.381M`、`25.176 → 25.023`、`0.209 → 0.215`，避免把约 77% 数量下降写成无损压缩。
- 保留论文表 9 可展开原表，以及轻量 Mesh / 角色交互两个官方 GIF；GIF 继续只说明官方演示能力。
- 调整 `src/data/tutorial.ts`：更新模块标题与描述，写明阈值、可行点、推荐点和原表核查顺序。
- 调整 `src/styles/paper.css`：新增情境按钮、双滑块、散点诊断、配置检查器、结论锚点、术语扩展和手机端单列布局。
- 调整 `REFERENCES.md`：新增 Section 7.2、Table 9 与帕累托教学分析的证据映射和边界。

### 证据边界

- 五个配置的高斯数量、PSNR、SSIM、LPIPS 均来自 Table 9；评估为 10 个场景平均、每场景 20 个验证视角。
- 三个情境预设、两个滑块范围、当前可行域和推荐规则是教程设计，不是论文训练参数、产品档位或硬件标准。
- 帕累托前沿只使用“高斯数量越低越好、PSNR 越高越好”两个目标；不包含 LPIPS、SSIM、渲染帧率、显存和场景难度。
- M 被 F 轻微支配是对表 9 数字的教学分析，不是论文作者给出的统计显著性结论。
- 完整配置的 PSNR 与 LPIPS 相对基线仍有小幅变化，教程不使用“零损失”或“无损”表述。

### 验证结果

- Vite 生产构建通过，62 个模块完成转换。
- paper-skill 结构校验通过，保持 10 个章节、14 个主动模块、3 个双模块章节与 16 个注册组件。
- 仓库 `catalog`、`validate`、`validate:pr -- main` 与 `build:paper -- hy_world_2_0` 全部通过；为避免 Windows 锁住 `esbuild.exe`，单篇构建前停止预览，构建后恢复 `4174`。
- 浏览器确认初始轻量交付状态为预算 1.5M、PSNR 25.00；可行配置恰为 M 与 F，最低数量推荐为 F=1.381M。
- “画质审查”状态为预算 6.0M、PSNR 25.15，可行配置恰为 B 与 D，推荐 D=5.254M。
- “极限压缩”状态为预算 1.1M、PSNR 24.50，可行配置仅 V=1.000M。
- 原生 range 在浏览器控制层中不能稳定响应拖动后，新增的减号 / 加号步进按钮实测可用；从极限压缩连续提高 PSNR 10 次后到达 25.00，正确进入 0/5 无可行配置状态。
- 点击 M 配置后，检查器显示 1.383M、PSNR 25.017、SSIM 0.747、LPIPS 0.216，并明确提示它在数量与 PSNR 两目标上被 F 轻微支配。
- 桌面截图发现预算标签、前沿说明与 M/F 标签初版有轻微拥挤；调整文字位置后复核，数据点坐标未改变且标签不再重叠。
- 390×844 手机端页面内容宽度与滚动宽度均为 375px，无页面级横向溢出；情境按钮、双滑块、诊断、五个配置点和论文结论锚点均切换为单列。
- 手机端两个步进器 `scrollWidth=clientWidth=259px`；五个配置按钮 `scrollWidth=clientWidth=283px` 且 `scrollHeight=clientHeight=73px`，未发现文字裁切。
- 论文表 9 展开后包含 5 行，250px 容器内横向滚动内容宽度 733px；Mesh GIF 为 520×293，交互 GIF 为 640×360，均完整加载。
- Canvas 进入 `is-ready` 状态，桌面与手机截图无控件重叠；本地页面日志无错误或警告。

### 审查重点

- 初始“轻量交付”状态应为预算 1.5M、PSNR 25.00，可行点为 M 与 F，最低数量推荐为 F=1.381M。
- “画质审查”应筛出 B 与 D；“极限压缩”应只筛出 V。
- 将预算压到 1.0M 且最低 PSNR 提高到 25.00 时应显示无可行配置，不能自动修改阈值或伪造新点。
- 点击 M 时应显示 1.383M / 25.017 / 0.747 / 0.216，并提示它在数量与 PSNR 上被 F 轻微支配。
- 手机端滑块、五个配置按钮、论文结论锚点和表 9 内部滚动不得撑宽页面。

## V0.22「万象插槽·五首镜像机关城」

日期：2026-08-16

### 修改内容

- 完全重做模块 8.1，删除从“图像”到“输出”依次点亮节点的线性路径浏览器。
- 新增 Any-Modal 架构配线盘：多视图图像固定为必需输入，位姿、内参和深度作为三个可独立接入的先验。
- 新增五个输出头检查器，分别解释点图、相机、深度与有效掩码、法线、3DGS 的职责与 2.0 相关改进。
- 新增表 11 高分辨率端点证据：仅图像与全部先验两种真实配置会随配线状态高亮，部分先验不生成虚构指标。
- 新增 Any-Modal、DPT 输出头、两阶段 3DGS 课程三个术语扩展槽。

### 模块变更明细

- 调整模块 8.1，标题由“WorldMirror 2.0 路径地图”改为“Any-Modal 架构配线盘”，真实注册组件文件为 `src/modules/hy-architecture.tsx`。
- Canvas 重构为技术配线图：左侧三张多视图图像始终接入；位姿、内参、深度 token 随复选框状态改变颜色和连线；统一 token 序列进入共享 Transformer 后同时连接五个输出头。
- 新增三个原生复选框，允许 0-3 类先验的全部合法组合；状态文字同步显示“仅图像”“图像 + 部分先验”或“图像 + 全部先验”。
- 新增五个输出头标签页；所有端口始终显示在线，当前标签只用橙色标记阅读焦点，避免误导成一次只预测一个任务。
- 点图焦点解释归一化位置编码；相机焦点解释可选位姿条件不等于相机头开关；深度焦点解释 depth-to-normal 与有效掩码；法线焦点解释 normal-only 伪标签；3DGS 焦点解释两阶段课程。
- 新增三项诊断：“多视图图像已接入”“先验状态”“5 个 DPT 头在线”。
- 新增论文证据端点：无先验时高亮 H 分辨率 Acc. 均值 0.037 / Comp. 均值 0.040；全部先验时高亮 0.012 / 0.016。
- 部分先验状态显示“表 11 没有单独一行”，并提示 Figure 27 另行比较单类先验，不能用端点插值。
- 保留官方重建 GIF，但将它放在架构配线、输出头和证据解释之后，继续只承担流程展示职责。
- 调整 `src/data/tutorial.ts`：更新模块标题与描述，明确必需输入、可选先验、同时输出和真实端点。
- 调整 `src/styles/paper.css`：新增先验复选器、输出头标签、职责详情、诊断、端点证据、术语扩展与手机端两列 / 单列布局。
- 调整 `REFERENCES.md`：新增 Figure 12、Section 6.1-6.2、Section 8.2.2 与 Table 11 的机制映射和边界。

### 证据边界

- Any-Modal 只覆盖论文定义的图像、相机位姿、相机内参和深度四类输入；教程不把它解释为任意传感器或任意格式。
- 多视图图像是必需输入，三个几何先验可选；训练时每种先验独立以 0.5 概率丢弃。
- 五个任务头在一次前馈中同时预测；标签页只切换讲解焦点，不表示模型执行单头推理。
- 表 11 端点限定于 7-Scenes、WorldMirror 2.0、H=`756×1036`；Acc. 与 Comp. 均为误差均值、越低越好。
- 部分先验组合不显示插值指标；Figure 27 与 Table 11 的聚合方式不同，教程不混算。

### 验证结果

- Vite 生产构建通过，62 个模块完成转换。
- paper-skill 结构校验通过，保持 10 个章节、14 个主动模块、3 个双模块章节与 16 个注册组件。
- 仓库 `catalog`、`validate`、`validate:pr -- main` 与 `build:paper -- hy_world_2_0` 全部通过；单篇构建首次因运行中的 Vite 预览锁住 Windows `esbuild.exe` 而失败，停止预览后重跑成功，并恢复 `4174` 预览。
- 浏览器确认初始状态为 0/3 先验、仅图像，诊断显示多视图图像已接入和 5 个 DPT 头在线；证据区高亮 0.037 / 0.040 端点。
- 仅接入相机位姿时，状态显示“图像 + 相机位姿”，保持合法输入；两个表 11 端点均不高亮，并显示不能插值部分先验指标。
- 三类先验全部接入后，状态显示“图像 + 全部先验”，绿色反馈成立，证据区高亮 0.012 / 0.016 端点。
- 浏览器逐一切换 Pts3D、Camera、Depth、Normal、3DGS 五个输出头；每次均只有一个 `aria-selected` 阅读焦点，同时保留 5 个绿色在线端口，并更新对应职责和 2.0 改进说明。
- Any-Modal 术语扩展槽可正常展开；官方重建 GIF 加载完成，原始显示尺寸为 640×360；Canvas 进入 `is-ready` 状态。
- 桌面截图确认三类先验连线、共享 Transformer、五头分支、复选器和输出详情没有重叠。
- 390×844 手机端页面内容宽度与滚动宽度均为 375px，无页面级横向溢出；输出头标签切换为两列，先验控件和证据端点切换为单列。
- 手机端五个输出按钮的 `scrollWidth` 等于 `clientWidth`，三条先验控件的 `scrollHeight` 等于 `clientHeight`，未发现文字裁切；本地页面日志无错误或警告。

### 审查重点

- 初始状态必须是仅图像、0/3 先验，并高亮表 11 的 0.037 / 0.040 端点。
- 任意单类或双类先验组合都应保持合法，但证据区不能出现推算数字。
- 三类先验全部接入时应显示绿色反馈，并高亮 0.012 / 0.016 端点。
- 点击五个输出头时，其余四个端口仍应显示在线；Canvas、职责说明和 2.0 改进必须同步切换。
- 手机端不应出现输出标签文字挤压、先验说明溢出或页面级横向滚动。

## V0.21「星忆司书·双层回声调度局」

日期：2026-08-16

### 修改内容

- 完全重做模块 5.1，删除只切换四个预设配置并替换 Canvas 指标条的单选交互。
- 新增记忆调度实验：读者需要分别处理 GGM 全局点云、SSM++ 关键帧检索和空间 / 时间拼接三类决策。
- 新增论文表 8 完整可展开表格，覆盖相机控制基线、A-G、A* 共 9 行以及 8 个定量指标。
- 新增 GGM、SSM++、空间拼接三个术语扩展槽，让读者自行展开论文机制细节。
- 将教程状态与论文数据分离：自定义组合不再冒充消融配置，只有与 A 或 A* 接近的状态才显示对应证据行。

### 模块变更明细

- 调整模块 5.1，标题由“组合记忆消融”改为“记忆调度局：为目标视角配对”，真实注册组件文件为 `src/modules/hy-memory.tsx`。
- 新增 GGM 二元开关；关闭时 Canvas 中全景点云退为灰色并明确提示缺少跨轨迹粗结构约束，开启后显示 `Ppan 已加载`。
- 新增三个历史视角候选 R1-R3，每张候选卡展示相机朝向、视野重合教学线索和局部对应说明；R2 是与目标转角最相关的候选。
- 新增空间拼接 / 时间拼接分段控件：空间拼接显示宽度 `2W` 与共享时间索引 `tk`，时间拼接显示 `R·tk / T·tk+1` 的身份错位。
- 新增三项同步诊断：“全局骨架”“局部对应”“时间身份”；只有 GGM 开启、R2 命中、空间拼接三项同时成立时进入绿色成功状态。
- 新增论文证据聚光区，固定展示基线、配置 A、配置 A* 和配置 F；选择空间或时间拼接时只高亮最接近的已报告消融行。
- 新增边界提示：配置 F 还包含可训练 FFN、两类增强、相机嵌入和 batch size 64，不能把最终数字归因于选对检索帧。
- 调整 `src/modules/hy-paper-evidence.tsx`：新增 `table-8`，完整搬入论文 Table 8，并保留指标方向、warp mask 条件和最终记忆 / 蒸馏模型区别。
- 调整 `src/data/tutorial.ts`：更新模块标题和描述，明确三类操作与展开论文表 8 的阅读顺序。
- 调整 `src/styles/paper.css`：新增开关、分段控件、候选视图、诊断条、证据卡、术语扩展槽和手机端单列布局。
- 调整 `REFERENCES.md`：新增模块与 Figure 7 / 10 / 11、Section 5.2、Table 8 的证据映射和教程阈值边界。

### 证据边界

- 三个候选视角、相机角度和 `43% / 78% / 12%` 重合率是教学谜题线索，不是论文模型输出、检索阈值或数据统计。
- 论文只说明推理时按 3D FoV 相关性检索，并将最大检索数量限制为 `Tr < Tkf`；教程不虚构具体 `Tr`。
- 三项诊断用于解释互补机制，不计算 PSNR、SSIM、PSNRm 或相机误差。
- 配置 A、A*、F 的指标直接来自论文表 8；自定义组合显示“论文表 8 没有对应行”。
- A* 的退化只支持“在该消融协议中空间拼接优于时间拼接”，不能外推为所有视频模型都禁止时间拼接。

### 验证结果

- Vite 生产构建通过，62 个模块完成转换。
- paper-skill 结构校验通过，保持 10 个章节、14 个主动模块、3 个双模块章节与 16 个注册组件。
- 仓库 `catalog`、`validate`、`validate:pr -- main` 与 `build:paper -- hy_world_2_0` 全部通过，教程成功重新构建到 `site/papers`。
- 浏览器确认初始状态为 GGM 关闭、R1、时间拼接；三项诊断依次为“缺失 / 继续检索 / 被错开”，证据区明确显示论文表 8 没有对应行。
- 开启 GGM 并选择 R2 后，时间拼接状态正确高亮配置 A*，显示 PSNR 19.83、SSIM 0.581、PSNRm 29.77 和红色退化反馈。
- 切换为空间拼接后，Canvas 显示宽度 2W 与共享时间索引 `tk`，三项诊断全部通过，证据区高亮配置 A 的 PSNR 20.94、SSIM 0.640、PSNRm 30.27。
- 论文表 8 可展开，浏览器确认包含 9 行配置；SSM++ 术语扩展槽可独立展开。
- 390×844 手机端页面内容宽度与滚动宽度均为 375px，无页面级横向溢出；候选视角、诊断、证据卡与术语槽均切换为单列。
- 手机端论文表格容器宽度 250px、内容宽度 880px，并启用内部横向滚动，不会撑宽页面。
- Canvas 已进入 `is-ready` 状态，桌面与手机截图确认控件、候选卡、反馈和表格没有重叠；本地页面日志无错误或警告。

### 审查重点

- 初始状态应为 GGM 关闭、R1、时间拼接，三项诊断均未通过。
- 只开启 GGM 不应出现论文配置 A；切换 R2 后若仍为时间拼接，应高亮 A* 并显示红色退化反馈。
- 选择 GGM + R2 + 空间拼接后，Canvas 应显示 2W 与共享 `tk`，三项诊断全部变绿，并高亮配置 A。
- R1/R3 的重合率必须继续标记为教程线索，不能被解释为论文检索器输出。
- 展开论文表 8 后应看到 9 行完整配置；A*、F、G 的含义和指标方向必须准确。

## V0.20「稀帧魔眼·转角定格试炼」

日期：2026-08-16

### 修改内容

- 完全重做模块 4.1，删除“点击开始后两台相机同时沿直线播放”的被动对比动画。
- 新增关键帧取景谜题：读者需要从快速转弯轨迹的 8 个候选视角中亲手选择 3 帧。
- 将“帧数是否选满”“是否存在相邻重复”“首尾视角是否覆盖整段转弯”拆成三个即时诊断状态。
- 将教学选择器与论文指标分离：RotErr、ATE 和选择性冻结配置独立放入论文证据区，避免让读者误以为谜题会计算论文指标。

### 模块变更明细

- 调整模块 4.1，标题由“Video-VAE 与 Keyframe-VAE 同轨比较”改为“关键帧取景谜题”，真实注册组件文件为 `src/modules/hy-keyframes.tsx`。
- 新增 8 个候选视角按钮 F1-F8，每个按钮显示视角编号、示意画面和相机角度；最多同时选择 3 帧。
- 初始状态选择连续三帧 F1-F3，用红色反馈说明相邻帧重复过多；选满 3 帧后其它候选会暂时禁用，必须先撤下一帧。
- 新增 Canvas 转弯轨迹：所有候选点固定显示，选中帧出现相机和朝向射线，反馈文案随帧间隔与首尾跨度同步变化。
- 新增三项诊断：“帧数约束”“最小间隔”“首尾跨度”；三项均满足时才进入绿色“关键帧组合成立”状态。
- 新增 Video-VAE 与 Keyframe-VAE 的文字化编码对照，解释连续时空压缩与稀疏关键帧保真的差别，不再依赖两台相机自动移动。
- 新增重置按钮，可一键恢复连续三帧的失败起点，方便重复审查不同组合。
- 新增论文证据区，明确展示 RotErr `0.762 → 0.492`、ATE `2.141 → 1.768` 且两项均为越低越好。
- 调整 `src/data/tutorial.ts`：更新模块标题与描述，写明 8 选 3、重复诊断、跨度诊断和论文证据阅读顺序。
- 调整 `src/styles/paper.css`：新增候选帧网格、缩略视图、诊断条、编码方式对照、指标条和手机端两列/单列布局。
- 调整 `REFERENCES.md`：新增关键帧谜题的论文映射与阈值边界。

### 证据边界

- 8 个候选视角、最多选择 3 帧、最小间隔 2 个候选位和首尾跨度 6 个候选位均为教程设计的谜题规则，不是论文训练参数或采样算法。
- 谜题用于解释“减少相邻重复、保留更大视角变化”的直觉，不声称手动选择 F1/F4/F8 就能复现论文模型。
- RotErr 与 ATE 数字来自论文的选择性冻结配置比较，不由上方选择器计算，也不能归因于某一组手动关键帧。
- 全量训练的部分视觉指标更高，但相机控制与泛化存在取舍；教程不把单一相机指标解释为模型总质量。

### 验证结果

- Vite 生产构建通过，62 个模块完成转换。
- 浏览器确认模块使用真实注册组件 `hy-keyframes.tsx`；页面生成 8 个候选帧按钮、3 个诊断状态和 1 个独立论文证据区。
- 初始 F1/F2/F3 状态选中 3 帧并禁用其余 5 帧，反馈为“相邻候选重复过多”。
- F1/F3/F5 状态满足最小间隔但首尾跨度不足，界面只点亮前两项诊断并提示快速转弯覆盖不足。
- F1/F4/F8 状态选中 3 帧、最小间隔 3、首尾跨度 7，进入绿色成功状态，Keyframe-VAE 对照同步显示“稀疏关键帧覆盖整段转弯”。
- 桌面端截图确认轨迹、候选帧、诊断条、编码对照和反馈没有重叠；390×844 手机端候选帧切换为两列，诊断与编码对照切换为单列。
- 390×844 手机端页面内容宽度与滚动宽度均为 375px，无页面级横向溢出；本地教程页面日志无错误或警告。
- paper-skill 结构校验通过，保持 10 个章节、14 个主动模块、3 个双模块章节与 16 个注册组件。
- 仓库 `catalog`、`validate`、`validate:pr -- main` 与 `build:paper -- hy_world_2_0` 全部通过，教程成功重新构建到 `site/papers`。

### 审查重点

- 初始连续三帧必须是失败状态；撤下一帧后其它候选应重新启用。
- F1/F3/F5 只能通过间隔诊断，不能错误进入最终成功状态；F1/F4/F8 应同时通过三项诊断。
- 候选帧选择必须同步更新 Canvas 相机位置、诊断数字、编码对照和反馈颜色。
- RotErr 与 ATE 必须继续放在“论文证据，不由上方选择器计算”区域，不能随谜题选择变化。

## V0.19「盲区猎场·双轨勘景契约」

日期：2026-08-16

### 修改内容

- 完全重做模块 3.1，删除“点击五类路线，只看一条固定路径与一句说明”的单选交互。
- 新增盲区勘景任务：读者先选择物体背面、走廊远端或俯视盲区，再从五类 WorldNav 路线中组合最多两类策略。
- 将路线是否触达目标、是否形成互补观察、执行条件与论文表 1 数量上限放入同一操作闭环。
- 新增错误组合和预算上限状态：路线未覆盖目标时明确指出缺口，两类策略选满后禁用其它路线并要求先撤下一条。

### 模块变更明细

- 调整模块 3.1，标题由“五类轨迹覆盖实验”改为“盲区勘景任务台”，组件文件为 `src/modules/hy-trajectory.tsx`。
- 新增三个盲区标签页：“物体背面”“走廊远端”“俯视盲区”；切换目标时自动装入一条能触达目标的起始策略。
- 为常规、环绕、重建感知、漫游和航拍路线补充覆盖对象、执行条件、论文表 1 最大数量与独立 Canvas 路径。
- 新增最多两类策略的组合选择；选满两类后，其余未选路线进入禁用状态，避免无上限堆叠所有路线。
- Canvas 同时绘制五类路线的灰色候选路径，只把当前选中路线着色，并在路线终点显示相机；目标框会随是否覆盖切换红/绿状态。
- 新增任务判定区，区分“未覆盖”“单一路线已触达”“推荐互补组合完成”三种状态，不使用虚构覆盖率或路线总分。
- 为三个盲区分别设置教学推荐组合：物体背面使用环绕 + 重建感知，走廊远端使用漫游 + 常规，俯视盲区使用航拍 + 重建感知。
- 调整 `src/data/tutorial.ts`：更新模块标题与描述，明确先选盲区、再组合路线的操作方式。
- 调整 `src/styles/paper.css`：新增目标标签、任务工作台、路线策略按钮、三态反馈、禁用状态与手机端单列布局。
- 调整 `REFERENCES.md`：新增路线组合教学映射，说明推荐组合来自路线用途的教学转译，不是论文报告的全局最优策略。

### 证据边界

- 五类路线的最大数量、对象绑定与迭代执行条件来自论文表 1；教程中的“两类策略上限”只是为了形成可审查的组合操作，不是论文预算参数。
- 三组推荐组合依据路线用途进行教学映射，不代表论文报告了这三组组合的统一覆盖率、胜率或全局最优性。
- WorldNav 仍是场景解析、NavMesh、对象检测、碰撞约束与启发式路线的系统组合，不能写成端到端学习的最优相机规划器。
- “单一路线已触达”不等于路线无效；界面只提示观察类型仍然单一，需要另一类策略补充证据。

### 验证结果

- Vite 生产构建通过，62 个模块完成转换。
- 浏览器实测三个盲区：起始单路线均进入“已触达”状态，加入对应第二条路线后均进入“互补完成”状态。
- 每个推荐组合选满两类策略后，剩余三条路线均正确禁用；撤下一条后可以重新选择。
- 物体背面目标下选择“常规 + 漫游”时，任务保持红色“未覆盖”状态，并提示撤下一条路线再选择能覆盖目标的策略。
- 桌面端截图确认多条候选路线、已选路线、相机终点、目标框、任务判定与五张路线条件卡没有重叠。
- 390×844 手机端页面内容宽度与滚动宽度均为 375px，无页面级横向溢出；目标、工作台与路线卡均计算为单列布局。
- paper-skill 结构校验通过，保持 10 个章节、14 个主动模块、3 个双模块章节与 16 个注册组件。
- 仓库 `catalog`、`validate`、`validate:pr -- main` 与 `build:paper -- hy_world_2_0` 全部通过，教程成功重新构建到 `site/papers`。
- 浏览器桌面端与手机端日志均无错误或警告。

### 审查重点

- 三个盲区的目标标题、推荐组合、Canvas 目标框与任务反馈必须同步切换。
- 任意时刻最多只能选两类路线；选满后其它路线应禁用，不能无条件把五类路线全部叠加。
- 错误组合必须保持“未覆盖”状态；单路线触达与双路线互补完成必须使用不同反馈。
- 路线卡中的“最大数量”必须继续标为表 1 启发式上限，不能被误读为本任务的预算成本或质量分数。

## V0.18「记忆回廊·世界状态压力试炼」

日期：2026-08-16

### 修改内容

- 将模块 1.0 从“四个标签切换四组说明”升级为可交叉操作的世界状态实验，新增遮挡、回访与动作三项压力测试。
- 为视频生成、动作条件视频世界、显式三维重建和 HY-World 2.0 编写 12 组独立反馈，不再用同一套动画只替换少量文案。
- 新增房间状态示意：相机位置、遮挡区表达与椅子位置会随测试和模型范式变化，帮助读者区分“生成新画面”“恢复观测几何”“保存显式资产”和“运行时动作响应”。
- 新增三栏状态账本，同步展示每个范式对未见区域、离开后回访和动作反馈的处理方式。

### 模块变更明细

- 调整模块 1.0“世界模型概念实验室”，组件文件为 `src/modules/hy-world-model-basics.tsx`。
- 新增三项压力测试按钮：“绕到遮挡背面”“离开后再回访”“推动场景物体”；按钮与四个范式标签形成 4×3 状态矩阵。
- 每个组合都定义独立的结论强度、结论句、解释、未见区域策略、回访方式和动作响应，共 12 条不同判断。
- 新增房间示意区：遮挡测试改变相机朝向，动作测试在动作视频世界与 HY-World 2.0 下移动椅子，四种范式使用不同的遮挡区状态色。
- 新增结论反馈区，使用绿色、橙色和红色分别表示“该范式的主要优势”“能力有条件”“不是该范式主要目标”，不使用虚构分数。
- 新增状态账本，三列分别显示“未见区域”“离开再回访”“动作响应”，并在手机端转为单列。
- 调整 `src/data/tutorial.ts`：模块描述改为明确说明三项压力测试与四类比较维度。
- 调整 `src/styles/paper.css`：新增测试按钮、房间示意、状态反馈、状态账本与响应式布局。
- 调整 `REFERENCES.md`：新增状态持久性实验的资料映射与教学边界，说明该实验不是统一数据集排行榜。

### 证据边界

- 三项压力测试是基于世界模型范式与 HY-World 2.0 官方“Why 3D”说明设计的教学实验，不是论文报告的 12 组消融或用户实验。
- “动作视频世界”用于解释动作条件像素 rollout 的范式特征，不代表所有在线视频世界模型都具有相同长期记忆能力。
- HY-World 2.0 的“推动物体”判断只覆盖 WorldLens 运行时、角色、碰撞与资产交互的系统能力；完整世界生成仍然在运行前离线完成。
- 稀疏输入生成得到的遮挡背面来自生成先验，不能升级为对真实场景的测量或数字孪生证据。

### 验证结果

- Vite 生产构建通过，62 个模块完成转换。
- 浏览器遍历 4 个范式与 3 项测试共 12 种组合，12 组均有非空状态、结论与三项账本输出，且 12 条结论互不重复。
- 桌面端截图确认测试按钮、房间示意、结论反馈和账本在同一视口内形成清晰的操作—反馈关系。
- 390×844 手机端页面内容宽度与滚动宽度均为 375px，无页面级横向溢出；测试按钮、场景/结论和状态账本均切换为单列。
- paper-skill 结构校验通过，保持 10 个章节、14 个主动模块、3 个双模块章节与 16 个注册组件。
- 仓库 `catalog`、`validate`、`validate:pr -- main` 与 `build:paper -- hy_world_2_0` 全部通过，教程成功重新构建到 `site/papers`。
- 浏览器桌面端与手机端日志均无错误或警告。

### 审查重点

- 依次切换四个范式和三项测试，确认结论、状态强度和三项账本同步变化，而不是只改变标题。
- 在“推动场景物体”中，动作视频世界与 HY-World 2.0 的椅子应产生位移；普通视频生成和纯重建不应伪装成在线动作模型。
- HY-World 2.0 的遮挡测试必须保留“生成先验，不是真实测量”的边界，动作测试必须保留“运行时交互，不是在线再生成”的边界。
- 手机端三项测试按钮、房间示意和长结论文字不得重叠或产生页面级横向滚动。

## V0.17「终焉校准仪·效果对照解放」

日期：2026-08-16

### 修改内容

- 对论文第 32-34 页进行文本与页面渲染双重核对，确认 WorldMirror 的低分辨率在不同协议中确实存在 `189×259` 与 `182×252` 两种写法。
- 在模块 7.1 新增协议差异提示：相机姿态 AUC 使用正文/表 12 的 L=189×259，点图重建表 11 单独使用 L=182×252；中、高档一致。
- 强化模块 10.2 的效果比较：新增“当前模型 → HY-World 2.0”左右对照和“读者真正能感知的变化”，不再只堆叠两段抽象说明。
- 对第 4、5、6、7、9 章全部公式符号进行可点击性审查，并修复第 6 章第三个 `x_t` 显示为可点击却不产生解释的问题。
- 汇总全站交互形式，确认新增版本已经覆盖范式切换、任务分流、动画对照、路线选择、消融切换、诊断匹配、分辨率探针、架构路径、创新筛选、证据判决和能力矩阵等不同操作。

### 模块变更明细

- 调整模块 7.1“分辨率外推变插值”，组件文件为 `src/modules/hy-resolution.tsx`。
- 在三项读数与反馈之间新增灰色协议提示，不改变 L/M/H 按钮、位置探针或 AUC 数值。
- 提示明确区分表 12 相机姿态/深度/NVS 使用的 L=189×259 与表 11 点图重建脚注的 L=182×252，避免把论文内部的任务协议差异误判为教程录入错误。
- 调整模块 10.2“模型能力进化图鉴”，组件文件为 `src/modules/hy-model-evolution.tsx`。
- 为 7 条功能轴新增 `readerImpact` 字段，分别解释生成、重建、全景、扩展、资产、交互和开放复现对用户实际工作流的影响与代价。
- 详情区新增左右效果对照：左侧显示当前选中模型在该功能上的证据状态与实现形态，右侧显示 HY-World 2.0 的对应升级；中间箭头只表示阅读对照，不表示所有模型都是线性前代。
- 新增绿色“读者真正能感知的变化”区，强调持久资产、跨轨迹稳定、运行时漫游、复现门槛等实际结果。
- 原四字段详情改为“功能用途、代际/外部比较、当前证据强度、证据与适用边界”，删除与左右对照重复的说明。
- 调整 `src/styles/paper.css`：新增协议提示、左右效果对照、用户影响区及手机端单列布局。
- 调整第 6 章 DMD 公式数据，文件为 `src/data/tutorial.ts`：将重复三次的 `x_t` 导数项等价缩写为 `J_θ`，并在符号解释中保留 `J_θ = dx_t/dθ` 的含义，避免框架对第三个同名节点产生空反馈。

### 证据边界

- PDF 第 32 页正文将三档推理分辨率写为 189×259、378×518、756×1036；第 34 页表 11 脚注将点图任务低档写为 182×252。教程保留两者并标注任务来源。
- 左右效果对照不是统一实验排行榜；对于 HY-World 1.5、GenEx、Marble 等不同范式模型，只比较当前资料明确覆盖的功能和输出形态。
- “读者感知变化”是对论文与官方系统能力的教学转译，不增加新的性能数字，也不替代证据边界。
- `J_θ` 是教程为了交互稳定性引入的导数项缩写，不是新增训练机制；它仍表示原式中的 `dx_t/dθ`。

### 验证结果

- Vite 生产构建通过，62 个模块完成转换；paper-skill 结构校验保持 10 个章节、14 个主动模块、3 个双模块章节与 16 个注册组件。
- 仓库 `catalog`、`validate`、`validate:pr -- main` 与 `build:paper -- hy_world_2_0` 全部通过，教程成功重新构建到 `site/papers`。
- 浏览器逐项点击第 4、5、6、7、9 章共 22 个公式高亮节点，所有节点均显示非空解释；第 6 章两个 `x_t` 与 `J_θ` 均能正确反馈。
- 在 10.2 分别选择 HY-World 1.5、WorldMirror 1.0、GenEx 与 Marble 1.0 的可用单元格，模型标题、左右效果对照和读者影响区均同步更新。
- 10.2 共 15 个“未报告”单元格，浏览器确认 15 个全部处于禁用状态，不会展开伪造详情。
- 390×844 手机端视口下，页面内容宽度与滚动宽度均为 375px，无页面级横向溢出；协议提示、效果对照与读者影响区均计算为单列布局。
- 手机端截图复核 7.1 的 L/M/H 按钮、Canvas 与说明文字没有重叠；浏览器日志无错误或警告。

### 审查重点

- 模块 7.1 必须同时显示两个低档尺寸及各自来源，不能把它们合并为一个数字。
- 在 10.2 依次点击 HY-World 1.5、WorldMirror 1.0、GenEx 和 Marble 的可用状态，确认左右对照与用户影响同步变化。
- “未报告”15 个单元格继续保持禁用，左右对照不能为它们生成伪造详情。
- 第 4、5、6、7、9 章公式中的所有高亮符号都应可点击并出现解释；第 6 章的 `J_θ` 应解释为 `dx_t/dθ`。

## V0.16「六芒创新阵·证据星核点火」

日期：2026-08-16

### 修改内容

- 新增模块 8.2“创新证据星图”，把论文亮点从章节叙述中抽出，形成可主动筛选和逐项审查的创新地图。
- 将贡献拆为“核心方法、训练与效率、系统集成、运行时能力”四类，避免把模型机制、训练工程、完整管线和官方演示统一包装成算法创新。
- 收录 6 个创新节点：任务分流统一框架、HY-Pano 2.0、WorldNav、WorldStereo 2.0、WorldMirror 2.0、3DGS + WorldLens。
- 每个节点均固定呈现“旧问题 → 新机制 → 带来什么”的因果链，并附论文证据、证据定位、关键词和不能外推的边界。
- 在 `REFERENCES.md` 增加创新证据索引，集中记录各子系统的问题、机制、证据与限制。

### 模块变更明细

- 新增模块 8.2，组件文件为 `src/modules/hy-innovation-map.tsx`，组件 ID 为 `hy-innovation-map`。
- 主操作一是切换四类贡献筛选器；每次切换会改变当前审查问题、可选创新节点和默认详情。
- 主操作二是点击当前分类下的节点；详情区同步更新贡献类型、子系统、问题、机制、效果、论文数字、协议限制与关键词。
- “核心方法”覆盖 HY-Pano、WorldNav、WorldStereo、WorldMirror；“训练与效率”覆盖 WorldStereo、WorldMirror、3DGS；“系统集成”覆盖任务分流、WorldNav 与资产运行时；“运行时能力”只保留 3DGS + WorldLens。
- HY-Pano 节点使用表 4 I2P；WorldNav 使用表 1；WorldMirror 使用表 11 与表 14；3DGS 使用表 9；WorldStereo 使用教程已核对的关键帧和记忆消融数字。
- WorldNav 详情明确标注“启发式组合，不是端到端全局最优规划”；WorldLens 详情明确标注“官方 GIF 不是物理真实性或统一帧率基准”。
- 修改 `src/modules/registry.tsx` 注册新组件；修改 `src/data/tutorial.ts` 将模块 8.2 加入第 8 章，并更新章标题、桥接、洞察和 takeaway。
- 调整 `src/styles/paper.css`：新增贡献筛选、创新节点、三段因果链、证据/边界双栏、关键词和三档响应式布局。

### 证据边界

- 模块不计算“创新分数”，也不根据节点数量判断哪个子系统更重要。
- 同一节点可以同时属于多个贡献类型；分类用于说明证据性质，不代表论文官方章节划分。
- 系统集成和运行时能力是论文整体贡献的一部分，但不能自动等同于新的网络结构或统一实验领先。
- 所有定量数字继续附带数据集、指标方向、分辨率、硬件或子协议边界。

### 验证结果

- Vite 生产构建通过，62 个模块完成转换；paper-skill 结构校验升级为 10 个章节、14 个主动模块、3 个双模块章节与 16 个已注册组件。
- 仓库 `validate`、`catalog`、`validate:pr -- main` 与 `build:paper -- hy_world_2_0` 全部通过，单篇教程成功重新构建到发布目录。
- 浏览器逐一切换四类筛选器，节点数分别为核心方法 4、训练与效率 3、系统集成 3、运行时能力 1；每次切换都自动选择该分类中的有效节点和审查问题。
- WorldNav 详情明确显示五类轨迹的点云/语义/NavMesh/碰撞机制，并保留“启发式组合、不是端到端全局最优”的限制。
- WorldMirror 详情正确显示表 11 的 `0.079 → 0.037` 为越低越好的 Acc. 误差，并将表 14 的 `5.60 秒` 限制在 H20 四卡、128 视图重建步骤。
- 3DGS + WorldLens 详情同时显示 `6.000M → 1.381M` 与 `PSNR 25.176 → 25.023`，并说明高斯减少不是无损、官方 GIF 不是物理真实性或统一帧率基准。
- 390×844 手机端页面宽度为 375px，无页面级横向溢出；筛选器、节点、三段因果链和证据/边界区全部变为单列，训练类筛选仍正确显示 3 个节点。
- 浏览器桌面端与手机端日志均无错误或警告。

### 审查重点

- 切换四类筛选器时，节点数量应分别为 4、3、3、1，且当前详情必须自动落到该分类中的有效节点。
- 点击 WorldNav，确认界面没有把五类路线说成学习得到的最优策略。
- 点击 WorldMirror，确认 0.079→0.037 标为越低越好的误差，5.60 秒保留 H20 四卡与 128 视图条件。
- 点击 3DGS + WorldLens，确认 6.000M→1.381M 同时显示 PSNR 变化，且官方演示没有升级为定量物理结论。

## V0.15「世界观测镜·概念星图展开」

日期：2026-08-16

### 修改内容

- 将第 1 章升级为“先理解世界模型：一套系统，两种任务”，在进入 HY-World 管线前先回答“世界模型到底是什么”。
- 新增模块 1.0“世界模型概念实验室”，对比普通视频生成、动作条件视频世界、显式三维重建与 HY-World 2.0 四种范式。
- 新增可切换的系统剖面，逐项展示输入线索、内部世界状态、输出形式、动作反馈闭环、擅长能力和不可混写的边界。
- 新增六个术语抽屉：世界模型、生成/重建/模拟、显式三维表示、3DGS、数字孪生和持久世界。
- 新增四阶段发展史暗格，从 2018 年环境动力学世界模型、2024 年生成式交互环境、2025 年在线像素与显式三维路线分流，讲到 2026 年 HY-World 2.0 的系统连接方式。
- 补充 `REFERENCES.md` 的世界模型概念资料，加入《World Models》、Genie 2 官方介绍与 HY-World 2.0 官方“Why 3D World Models?”说明。

### 模块变更明细

- 新增模块 1.0，组件文件为 `src/modules/hy-world-model-basics.tsx`，组件 ID 为 `hy-world-model-basics`。
- 主操作是切换四个观察镜头；每次切换会同步改变系统问题、输入、内部状态、输出、反馈闭环、四项能力判定、优势和边界。
- 四项能力轴固定为“补出未见内容、忠实恢复真实几何、持久三维资产、动作反馈闭环”，状态使用“明确具备 / 部分覆盖 / 不是主要目标”，避免用红叉制造单一排行榜。
- HY-World 2.0 初始状态明确显示文本/单图生成、多视图/视频重建、显式三维资产和 WorldLens 交互，同时保留“完整生成分钟级、生成区域不是测量”的限制。
- 术语与发展史使用原生可展开区域，摘要下方均显示灰色点击提示；展开内容不会改变主交互状态。
- 新增三条一手延伸链接，并在 `REFERENCES.md` 记录资料用途和不能外推的结论。
- 修改 `src/modules/registry.tsx` 注册新组件；修改 `src/data/tutorial.ts` 把模块 1.0 放在任务调度台之前，并更新第 1 章标题、桥接、洞察和 takeaway。
- 调整 `src/styles/paper.css`：新增四镜头导航、系统剖面、能力轴、术语抽屉、发展史暗格和桌面/平板/手机三档响应式布局。

### 证据边界

- “世界模型”在不同文献中的定义并不完全相同；模块使用的是帮助阅读的广义教学定义，不宣称存在唯一标准。
- 2018-2026 时间线只选择与本教程最相关的代表性路线，不是完整领域综述，也不按年份断言路线彼此替代。
- 普通视频生成、动作条件世界、三维重建和 HY-World 2.0 的四项能力状态是范式解释，不是统一数据集上的实验评分。
- HY-World 2.0 的具体方法、数字与限制仍以论文为准；官方 README 和产业博客只负责补充概念背景。

### 验证结果

- Vite 生产构建通过，61 个模块完成转换；paper-skill 结构校验升级为 10 个章节、13 个主动模块、2 个双模块章节与 15 个已注册组件。
- 仓库 `validate`、`catalog`、`validate:pr -- main` 与 `build:paper -- hy_world_2_0` 全部通过，单篇教程成功重新构建到发布目录。
- 浏览器逐一切换四个观察镜头，内部状态分别更新为视频潜变量、动作条件视频状态、显式几何，以及 HY-World 2.0 的全景/轨迹/记忆/显式几何组合；四项能力判定和边界同步变化。
- 10 个扩展槽均可展开：6 个术语抽屉和 4 个发展史暗格；“数字孪生”展开后明确显示文本生成世界不能自动称为真实场景数字孪生，“2018”暗格正确说明经典环境动力学路线。
- 两组扩展槽在收起状态即可看到灰色点击提示；三条延伸来源链接分别指向 World Models、Genie 2 官方介绍和 HY-World 2.0 官方 Why 3D 章节。
- 联网核验 World Models arXiv、Genie 2 官方页面和 HY-World 2.0 官方 README 均返回成功状态。
- 390×844 手机端页面内容宽度为 375px，无页面级横向溢出；四镜头、系统剖面、四能力轴、判断区和扩展槽全部变为单列，手机端切换状态正常。
- 浏览器桌面端与手机端日志均无错误或警告。

### 审查重点

- 四个观察镜头切换时，内部世界状态和四项能力必须真正变化，不能只改标题。
- “动作条件视频世界”不得被描述为天然拥有显式三维资产；“三维重建”不得被描述为可以任意想象未见空间。
- 打开术语“数字孪生”，确认文本明确限制在真实多视图/视频重建语境。
- 手机端四镜头、四能力轴、系统剖面、术语和发展史均应变为单列，页面本身不得横向溢出。

## V0.14「三相炼成阵·训练处方觉醒」

日期：2026-08-16

### 修改内容

- 完全重做模块 6.1，删除只会依次高亮三个阶段的“上一步 / 下一步”交互。
- 新增训练故障诊断玩法：学习者需要把“相机不听指令”“跨轨迹空间漂移”“质量足够但推理过慢”分别匹配到领域适配、中段记忆训练和后蒸馏。
- 错误处方不再只是显示错误颜色，而会说明被选阶段真正解决的能力，以及为什么正确阶段必须先于或后于其它阶段。
- 新增三项课程进度：相机控制、跨轨迹一致性和四步生成器；只有三个故障均诊断正确，才显示完整训练顺序及“四步并不等于整套系统实时”的边界。
- 修复第 6 章公式中的不可点击符号：原清单包含正文未出现的 `G_θ`，现改为正文实际出现的 `L_DMD`、`x_t` 与 `θ`。

### 模块变更明细

- 调整模块 6.1，组件文件为 `src/modules/hy-training-stages.tsx`，标题改为“训练诊断台：给故障开对处方”。
- 新增三个案例标签页，每个标签显示案号、故障名和“待诊断 / 已诊断”状态；已解决案例使用绿色顶边，当前案例使用蓝色顶边。
- 新增三张训练处方按钮：领域适配、记忆训练、后蒸馏。选择后 Canvas 中对应阶段即时变色，并显示“处方命中”或“顺序不匹配”。
- 相机控制案例强调关键帧潜空间与相机适配器；跨轨迹案例强调 GGM 与 SSM++；速度案例强调 DMD 只在生成器能力已经学好后进行四步蒸馏。
- Canvas 底部新增三条定性能力条，不使用虚构分数；能力条只在对应案例诊断成功后点亮。
- 新增课程依赖区，固定解释“领域适配 → 记忆训练 → 后蒸馏”，并提供“重新诊断”按钮恢复初始状态。
- 调整 `src/data/tutorial.ts` 的模块标题、描述和第 6 章公式符号表；没有修改论文公式正文。
- 调整 `src/styles/paper.css`：新增案例标签、处方状态、正确/错误反馈、课程依赖和移动端单列布局。

### 证据边界

- 三个案例是对论文训练流程的教学化故障映射，不是论文报告的独立消融实验或故障率统计。
- 能力条只表示课程依赖是否被学习者正确识别，不表示性能百分比或训练完成度。
- 四步推理只对应 WorldStereo 2.0 的 DMD 蒸馏生成器；完整世界生成仍是包含全景、规划、扩展、重建和资产处理的多阶段管线。

### 验证结果

- Vite 生产构建通过，60 个模块完成转换；paper-skill 结构校验保持 10 个章节、12 个主动模块、2 个双模块章节与 14 个注册组件。
- 仓库 `validate`、`catalog`、`validate:pr -- main` 与 `build:paper -- hy_world_2_0` 全部通过，单篇教程成功重新构建到发布目录。
- 浏览器故意为案 1 选择“中段记忆训练”时，界面显示错误状态，并解释该阶段解决跨轨迹一致性、案 1 应先建立关键帧潜空间与相机控制。
- 三案依次选择领域适配、记忆训练、后蒸馏后，三个标签均变为“已诊断”，课程依赖区显示完整顺序和“四步只属于 WorldStereo 2.0 生成器”的边界；切换案例不会丢失状态。
- 点击“重新诊断”后，三案全部恢复为“待诊断”；Canvas、处方按钮和反馈同步回到初始状态。
- 第 6 章公式生成 7 个点击节点，对应 5 类独立符号 `L_DMD`、`s_real`、`s_fake`、`x_t`、`θ`；逐类点击均显示正确解释。
- 390×844 手机端页面宽度为 375px，无页面级横向溢出；案例、处方和课程依赖均切换为单列，手机端正确处方可正常提交。
- 浏览器桌面端和手机端日志均无错误或警告。

### 审查重点

- 对任一案例先故意选择错误处方，确认反馈能说清该阶段真正解决什么，以及正确阶段为何不同。
- 三案全部选对后，确认三个能力条均点亮，课程依赖区显示完整顺序和四步生成器边界。
- 切换已诊断案例时不得丢失完成状态；点击“重新诊断”应清空三案状态。
- 第 6 章公式中的 `L_DMD`、`s_real`、`s_fake`、`x_t` 与 `θ` 应可以分别点击。

## V0.13「归一星尺·像素探针复苏」

日期：2026-08-16

### 修改内容

- 重做模块 7.1 的分辨率交互，删除在部分设备上难以准确切档的连续滑杆，改为可直接点击的 L/M/H 三档按钮。
- 新增“靠近起点 / 网格中心 / 靠近终点”位置探针，让学习者观察同一相对位置在原始整数索引与归一化坐标中的差异。
- 修复第 7 章公式无法点击的问题：原符号清单使用 `i, j`、`H_p, W_p` 等组合字符串，公式正文中没有对应的连续文本，因此框架无法生成点击节点；现已拆为六个实际出现的独立符号。
- 调整模块 10.2 的模型顺序为 HY-World 2.0、HY-World 1.5、HY-World 1.0、WorldMirror 1.0、GenEx、video2world、Marble 1.0。
- 删除模块 10.2 详情区中的 GIF 与相关数据字段；官方 GIF 仍保留在第 8、9 章的机制与资产模块中，不再重复占用模型比较区域。

### 模块变更明细

- 调整模块 7.1“分辨率外推变插值”，组件文件为 `src/modules/hy-resolution.tsx`。
- 分辨率控制从一个 `range` 滑杆改为三个显式按钮；按钮同时显示档位和像素尺寸，选中状态使用蓝色边框、背景和 `aria-pressed`。
- 新增第二组位置探针按钮。切换探针后，Canvas 中两侧橙色标记同步移动，读数区同时更新标准索引范围、归一化坐标和 WorldMirror 2.0 相对 1.0 的 AUC@30 差值。
- Canvas 左侧展示“标准 RoPE 的索引范围随网格扩张”，右侧展示“归一化 RoPE 始终落在 [-1,1]”；示意线明确注明不代表真实 patch 数，避免把教学采样密度误当论文结构参数。
- L/M/H 三个反馈状态分别解释训练附近、中档对照和训练外高分辨率；高分辨率状态保留论文表 11 的 66.29 与 86.89，并明确结论只覆盖对应协议与先验条件。
- 调整第 7 章公式数据 `src/data/tutorial.ts`：`x̂_i`、`ŷ_j`、`H_p`、`W_p`、`i`、`j` 均可直接点击并显示各自含义。
- 调整模块 10.2“模型能力进化图鉴”，组件文件为 `src/modules/hy-model-evolution.tsx`。
- 模型矩阵顺序改为“目标模型 → 在线视频前代 1.5 → 系统级前代 1.0 → 重建分支前代 → 外部参照”；所有 15 个“未报告”状态继续保持禁用。
- 删除 10.2 的 `OfficialGif` 引用、功能 GIF 字段与详情区媒体渲染，比较模块现在只保留功能用途、当前实现、2.0 改进点、证据边界和来源链接。
- 调整 `src/styles/paper.css`：新增三档按钮、探针按钮、三项读数与移动端单列布局；没有修改框架样式文件。

### 证据边界

- 7.1 的 AUC@30 数字来自论文表 12，档位尺寸按论文第 32 页正文的 189×259、378×518、756×1036；下方表 11 点图任务的低档另为 182×252。Canvas 中 6/9/13 条示意线只表达“分辨率越高，采样越密”，不是论文报告的真实 patch 网格数量。本行由 V0.17 完成证据更正。
- 归一化读数由章节公式 `(2i+1)/H_p-1` 的一维示意计算得到，用于解释固定区间映射，不新增论文结论。
- 10.2 的排序只改变阅读优先级，不改变各模型能力状态、发表时间、比较结论或“未报告不等于不支持”的规则。

### 验证结果

- Vite 生产构建通过，60 个模块完成转换；paper-skill 结构校验保持 10 个章节、12 个主动模块、2 个双模块章节与 14 个已注册组件。
- 仓库 `validate`、`catalog`、`validate:pr -- main` 和 `build:paper -- hy_world_2_0` 全部通过，单篇教程成功重新构建到发布目录。
- 桌面端实测 L/M/H 三档和三个位置探针均可点击；高分辨率状态显示 WorldMirror 2.0 相对 1.0 的 AUC@30 差值 `+20.60`，探针从起点移动到终点时归一化读数由 `-0.77` 变为 `0.77`。
- 第 7 章公式生成 6 个可点击节点：`x̂_i`、`i`、`H_p`、`ŷ_j`、`j`、`W_p`；点击后显示对应解释，再次点击同一符号可正常收起。
- 模块 10.2 的表头顺序实测为 HY 2.0、HY 1.5、HY 1.0、WM 1.0、GenEx、video2world、Marble 1.0；49 个能力状态中仍有 15 个“未报告”禁用项，详情区 GIF 数量为 0。
- 390×844 手机端页面内容宽度为 375px，无页面级横向溢出；7.1 的档位与读数自动变为单列，模型矩阵继续只在自身 283px 容器内横向滚动。
- 浏览器桌面端与手机端日志均无错误或警告。

### 审查重点

- 依次点击 L、M、H，确认档位无需拖动即可切换，Canvas 标题、两条 AUC 和反馈文字同步更新。
- 在任意分辨率下切换三个位置探针，确认橙色标记和归一化坐标同步变化。
- 点击第 7 章公式中的六类符号，确认解释框出现；再次点击同一符号应收起。
- 模块 10.2 的前三列必须为 HY-World 2.0、HY-World 1.5、HY-World 1.0，详情区不再出现 GIF。

## V0.12「时序领航灯·灰格静默结界」

日期：2026-08-16

### 修改内容

- 调整模块 10.2 的模型顺序：HY-World 2.0 移到第一列，其后依次为系统前代 HY-World 1.0、在线视频前代 HY-World 1.5、重建分支前代 WorldMirror 1.0，最后排列 GenEx、video2world 与 Marble 1.0 三个外部参照。
- 在每个模型表头新增时间标记。论文模型优先显示 arXiv 首次公开日期；Marble 显示论文比较版本日期；video2world 因 HY-World 2.0 论文没有单列版本日期，明确显示“论文未单列日期”。
- 将 15 个“未报告”状态改为不可点击，保留灰色中性显示；其余 34 个有证据内容的状态继续支持点击展开。
- 图例中的“未报告”增加“不可展开”提示，矩阵下方灰色说明同步解释“未报告不等于不支持”。

### 模块变更明细

- 调整模块 10.2“模型能力进化图鉴”，组件文件为 `src/modules/hy-model-evolution.tsx`。
- 新增固定模型顺序数组 `modelOrder`，保证所有功能行都按“目标模型 → 历代模型 → 外部模型”排列，初始可见区域直接展示 HY-World 2.0。
- 模型表头从两行扩展为三行：模型简称、模型类型、发表/比较时间。HY-World 2.0 标记 2026-04-15，HY-World 1.0 标记 2025-07-29，HY-World 1.5 标记 2025-12-16，WorldMirror 1.0 标记 2025-10-12，GenEx 标记 2024-12-12。
- “未报告”按钮增加原生 `disabled` 状态、不可展开的 ARIA 标签和悬停提示；点击、键盘激活和自动详情切换均被阻止。
- 调整 `src/styles/paper.css`：新增时间行样式、禁用光标与灰色背景，并让 hover 选中效果只作用于可点击单元格。

### 修改依据

- HY-World 2.0：arXiv 2604.14268，首次公开于 2026-04-15。
- HY-World 1.0：arXiv 2507.21809，首次公开于 2025-07-29。
- HY-World 1.5 / WorldPlay：arXiv 2512.14614，首次公开于 2025-12-16；官方仓库新闻于 2025-12-17 发布技术报告与开源模型。
- WorldMirror 1.0：arXiv 2510.10726，首次公开于 2025-10-12。
- GenEx：arXiv 2412.09624，首次公开于 2024-12-12。
- Marble 1.0：沿用论文注明的比较版本截止日期 2026-03-30。

### 验证结果

- Vite 生产构建通过，60 个模块完成转换；paper-skill 结构校验继续保持 10 个章节、12 个主动模块、2 个双模块章节与 14 个已注册组件。
- 仓库 `validate`、`catalog`、`validate:pr -- main` 和 `build:paper -- hy_world_2_0` 全部通过。
- 浏览器确认七列顺序为 HY-World 2.0、HY-World 1.0、HY-World 1.5、WorldMirror 1.0、GenEx、video2world、Marble 1.0；七个表头均显示对应时间或明确的日期边界提示。
- 49 个状态中，15 个“未报告”按钮均为禁用状态，34 个有证据状态保持可点击；对禁用按钮执行强制点击测试后，详情仍停留在原模型与功能，没有触发状态切换。
- 390×844 手机端页面内容宽度为 375px，无页面级横向溢出；矩阵仍在 283px 容器内独立滚动，HY-World 2.0 的 124px 表头能完整容纳模型名、类型和时间三行信息。
- 浏览器页面日志无错误或警告。

### 审查重点

- 首个模型列必须是 HY-World 2.0，之后依次为 HY-World 1.0、HY-World 1.5、WorldMirror 1.0、GenEx、video2world、Marble 1.0。
- 检查七个表头均显示时间信息，且 video2world 不出现伪造日期。
- 点击灰色“未报告”单元格时详情区必须保持不变；键盘也不能激活该状态。
- 点击“完整支持”“局部支持”“仅定性”和“闭源参照”单元格时，详情区仍应正常同步。

## V0.11「七曜世界谱·进化星阵全开」

日期：2026-08-16

### 修改内容

- 在第 10 章新增大型交互模块“模型能力进化图鉴”，将 HY-World 历代、关键重建前代与外部模型放入同一能力坐标系。
- 比较对象扩展为 7 个：HY-World 1.0、HY-World 1.5、WorldMirror 1.0、GenEx、video2world、Marble 1.0、HY-World 2.0。
- 设计 7 条功能轴：文本/单图世界生成、多视图/视频重建、360° 全景初始化、场景感知规划与世界扩展、3DGS/Mesh/点云资产、可漫游与角色物理交互、代码权重与复现入口。
- 新增 7×7 共 49 个可点击能力单元格。每次点击会同步更新当前模型、当前功能、证据状态、功能用途、模型实现、2.0 改进点、证据边界、来源链接和可用的官方 GIF。
- 建立“完整支持 / 局部支持 / 仅定性 / 论文未报告 / 闭源参照”五类状态；未报告和闭源使用中性灰色，不再用红叉暗示能力失败。
- 将 HY-World 1.5 明确标记为在线视频前代：它强调动作驱动的实时像素视频，2.0 强调离线构建后可持久保存、编辑和实时渲染的显式三维资产，二者不是简单线性替代。
- 将 Marble 1.0 的全部结论限制在论文截至 2026-03-30 的定性案例，不生成虚构分数；将 GenEx 的定量比较限制在表 4 的 I2P 子协议；将 video2world 的加速限制在论文报告的生成场景对齐流程。
- 扩充 `REFERENCES.md` 的比较资料部分，逐项记录 7 个模型的来源、可支持结论和不可外推内容。

### 模块变更明细

- 新增模块 10.2“模型能力进化图鉴”，组件文件为 `src/modules/hy-model-evolution.tsx`，组件 ID 为 `hy-model-evolution`。
- 模块 10.2 的唯一主操作是点击矩阵单元格；初始状态选择“HY-World 2.0 × 文本/单图世界生成”，让用户先看到目标模型的完整链路。
- 矩阵顶部固定显示 7 个模型和类型标签，左侧固定显示 7 条功能轴；目标模型 HY-World 2.0 使用绿色顶边提示，但没有把其它模型渲染成失败项。
- 点击任一单元格后，详情标题即时切换为“当前模型 × 当前功能”，右上角状态标签同步改变颜色与文字。
- 详情区新增“这个功能有什么用”“当前模型如何实现”“HY-World 2.0 的改进点”“证据与适用边界”四个固定审查字段，保证 49 个状态都能按同一结构检查。
- 模型来源链接随选择切换；外部模型同时显示 HY-World 2.0 论文比较依据，避免只给二手结论而没有一手入口。
- 稀疏输入生成和持久三维资产状态复用 `official-mesh.gif`；多视图/视频重建复用 `official-reconstruction.gif`；可漫游与角色物理交互复用 `official-interactive.gif`。
- GIF 图注明确写出“只展示 HY-World 2.0 对应能力，不代表与当前选择模型完成同协议动态对照”，防止把演示素材误读成比较实验。
- 修改 `src/modules/registry.tsx`，注册 `hy-model-evolution`；修改 `src/data/tutorial.ts`，把 10.2 挂载到第 10 章，并更新本章 insight 与第三条 takeaway。
- 修改 `src/styles/paper.css`，新增矩阵内部横向滚动、首列固定、五类状态配色、键盘焦点、点击选中边框、自动定位滚动余量、四字段详情和移动端单列布局；390px 下将功能列收窄为 148px、模型列固定为 124px，确保至少一列模型标题完整可见。

### 使用素材与内容边界

- HY-World 1.0、1.5 与 2.0 的范式描述以 HY-World 2.0 论文代际概述和腾讯官方仓库为准。
- WorldMirror 1.0→2.0 的改进依据包括 Normalized RoPE、深度-法线耦合、Depth mask head、token-budget dynamic sampling、SP/BF16/FSDP 与三阶段课程；高分辨率和效率数字沿用教程已核对的表 11、表 14。
- GenEx 只在 I2P 全景协议中显示 0.831→0.844 与 2.917→4.026 等论文数字，不据此判断其全部世界探索任务。
- video2world 只陈述论文给出的约 5 小时/场景与少于 2 分钟线性对齐，不把它写成通用视频重建速度结论。
- Marble 1.0 只显示“仅定性”或“闭源参照”，不出现伪造定量分数。
- 三个 GIF 均复用 V0.9 已落库的腾讯官方素材，本版本没有新增外部媒体文件。

### 验证结果

- Vite 生产构建通过，60 个模块完成转换；paper-skill 结构校验通过：10 个章节、12 个主动模块、2 个双模块章节、14 个已注册组件。
- 仓库 `validate`、`catalog`、`validate:pr -- main` 与 `build:paper -- hy_world_2_0` 全部通过；总目录生成后没有出现非预期作品变更。
- 浏览器在桌面端和 390×844 手机端遍历 49 个能力单元格，选中状态、详情标题、功能用途、模型实现、2.0 改进和证据边界均正确同步。
- Marble 1.0 的单图生成详情显示“仅定性”，没有虚构定量分数；GenEx 全景详情保留 I2P 与 0.831→0.844 边界；video2world 详情同时显示约 5 小时、少于 2 分钟和生成场景流程限制。
- 三个官方 GIF 均完整加载：重建与交互素材为 640×360，Mesh 素材为 520×293；切换全景或开放复现功能时不会误配 GIF。
- 390×844 下页面内容宽度为 375px，无页面级横向溢出；矩阵在 283px 容器内独立滚动，内部宽度 1016px，详情区自动变为单列，Marble 1.0 标题与状态完整可见。
- 浏览器页面日志无错误或警告。

### 审查重点

- 依次点击 HY-World 1.5 的“可漫游与角色物理交互”和“持久三维资产”，确认界面能说明在线视频交互与显式三维资产是不同范式。
- 点击 GenEx 的“全景初始化”，确认只引用 I2P 子任务数字；点击其它功能时不得把表 4 外推成全系统领先。
- 点击 video2world 的“密集输入重建”和“持久三维资产”，确认约 5 小时与少于 2 分钟的比较始终带有生成场景流程边界。
- 点击 Marble 1.0 任意橙色单元格，确认详情显示“仅定性”，且不存在虚构分数或红叉。
- 点击任意灰色“未报告”单元格，确认提示明确说明“未报告不等于不支持”。
- 在 390×844 移动端确认只在矩阵内部横向滚动，页面本身不得溢出；点击后四个详情字段与 GIF 不得遮挡或改变页面宽度。

## V0.10「灰羽引导灵·折叠咒文显现」

日期：2026-08-16

### 修改内容

- 为 V0.9 新增的所有可折叠论文表格补充统一操作提示，解决用户不容易发现表格可以点击展开的问题。
- 提示文案为“点击此处展开/收起论文原表；表格过宽时可左右滑动查看完整数据”。
- 提示采用灰色 11px 小字，位于表格标题、表格范围说明下方；桌面端保持单行，移动端允许自然换行。

### 模块变更明细

- 调整共享组件 `src/modules/hy-paper-evidence.tsx` 的 `PaperTable` 摘要区域，新增 `paper-table-hint` 提示文本；所有表格通过同一组件自动获得提示，无需在各章节重复维护。
- 模块 2.1“全景初始化三步观察”的论文表 4 新增展开/收起与横向滑动提示。
- 模块 3.1“五类轨迹覆盖实验”的论文表 1 新增展开/收起与横向滑动提示。
- 模块 7.1“分辨率外推变插值”的论文表 11 新增展开/收起与横向滑动提示。
- 模块 9.1“高斯数量与画质取舍”的论文表 9 新增展开/收起与横向滑动提示。
- 模块 10.1“结论审判庭：八案证据挑战”中答题后解锁的表 4、表 11、表 9、表 14 同步新增提示。
- 调整 `src/styles/paper.css`：提示横跨摘要全部列，使用 `#8792a5`、11px、500 字重与正常字形，不改变原有表格标题、范围说明、折叠状态和数据布局。

### 验证结果

- Vite 生产构建通过，59 个模块完成转换。
- 桌面端检查确认提示为灰色 11px 单行文本，位于折叠表标题下方且可直接看见。
- 390×844 移动端检查确认提示自然折为两行，表格摘要没有文字遮挡，页面宽度保持 375 px，无横向溢出。
- 浏览器控制台无错误或警告。

### 审查重点

- 在表格未展开时即可看到操作提示，不需要先点击才能发现说明。
- 点击摘要任意位置仍能正常展开和收起，新增提示不能拦截点击。
- 移动端提示应说明表格内部可以左右滑动，但页面本身不得产生横向滚动。

## V0.9「幻彩证据馆·原表召唤术」

日期：2026-08-16

### 修改内容

- 从论文 PDF 逐页核对表 1、表 4、表 9、表 11 和表 14，将教程中原本只有“见论文表 X”的数字说明升级为可直接展开审查的原表证据层。
- 新增统一组件 `src/modules/hy-paper-evidence.tsx`，集中管理表格数据、指标方向、实验条件、论文页码、重点行列和官方动态素材说明。
- 从腾讯混元官方 GitHub 采集 `recon_en.gif`、`mesh_en.gif`、`interactive.gif`，压缩为本地素材后接入教程，避免运行时依赖外部图片地址。
- 在 README 与 `REFERENCES.md` 中逐项补充 GIF 原始链接、教程用途、压缩处理、许可证入口和“官方演示不等于论文定量证据”的边界。
- 新增响应式证据样式：论文表默认收起，展开后桌面端完整显示，移动端只在表格内部横向滚动；GIF 在桌面端双列、移动端单列排列。

### 模块变更明细

- 调整模块 2.1“全景初始化三步观察”，组件文件为 `src/modules/hy-panorama.tsx`。
- 模块 2.1 在交互反馈下新增论文表 4 的 I2P 子表，包含 CubeDiff、GenEx、HY-World 1.0、HY-Pano 2.0 在 CLIP-I 与四项 Q-Align 指标上的完整 I2P 数值；HY-Pano 2.0 列使用重点色标记。
- 调整模块 3.1“五类轨迹覆盖实验”，组件文件为 `src/modules/hy-trajectory.tsx`。
- 模块 3.1 新增论文表 1 完整表格，展示五类轨迹的最大数量、是否绑定对象、是否迭代执行，并保留“环绕/重建感知数量取决于检测对象数”的条件说明。
- 调整模块 7.1“分辨率外推变插值”，组件文件为 `src/modules/hy-resolution.tsx`。
- 模块 7.1 新增论文表 11 的 7-Scenes 子表，搬入 WorldMirror 1.0/2.0 在 L/M/H、是否提供全部先验共 12 行结果；重点标出无额外先验的高分辨率 0.079 与 0.037 行，避免把 Acc. 误读成准确率。
- 调整模块 8.1“WorldMirror 2.0 路径地图”，组件文件为 `src/modules/hy-architecture.tsx`。
- 模块 8.1 在点击式网络路径之后新增“多图与视频重建演示”GIF，显示官方仓库中的重建流程，并在图注中明确它是流程演示而非统一实验协议。
- 调整模块 9.1“高斯数量与画质取舍”，组件文件为 `src/modules/hy-composition.tsx`。
- 模块 9.1 新增论文表 9 完整五组消融，重点标出非天空增密 + MaskGaussian 完整配置；同时新增“轻量 Mesh 漫游”和“角色交互与物理反馈”两个官方 GIF，让高斯压缩、碰撞代理和可交互世界之间形成直观联系。
- 调整模块 10.1“结论审判庭：八案证据挑战”，组件文件为 `src/modules/hy-evidence-court.tsx`。
- 模块 10.1 的前四个定量案卷新增 `tableId` 关联：答题后除解释、指标卡和来源链接外，还会解锁表 4、表 11、表 9 或表 14；效率案搬入表 14 的 6 种配置、32/64/128/256 视图显存与时间，共 10 列完整数据。
- 新增共享证据组件 `src/modules/hy-paper-evidence.tsx`：`PaperTable` 统一负责折叠、横向滚动、重点标注、页码与协议注释；`OfficialGif` 统一负责本地图片、中文说明和官方来源链接。
- 调整 `src/styles/paper.css`：新增折叠摘要、表格固定首列、重点行列、键盘焦点、GIF 图注、双列媒体和两档移动端布局。

### 使用素材与内容边界

- 表 1 和表 9 为完整表；表 4 只搬入与第 2 章直接对应的 I2P 子表；表 11 只搬入与第 7 章直接对应的 7-Scenes、WorldMirror 1.0/2.0 子表；表 14 为完整表。
- 表格数据以 `2604.14268v1.pdf` 第 8、23、29、34、37 页为准，并通过文本提取与页面渲染进行双重核对。
- 三个 GIF 均来自腾讯混元官方仓库，压缩后总大小约 8.2 MB；未增加、裁改或重排演示内容。
- GIF 只说明官方仓库当前展示的重建、Mesh 漫游和角色交互能力，不用于证明论文指标、泛化性或所有输入的稳定效果。

### 验证结果

- 项目 Vite 生产构建通过，59 个模块完成转换，三个 GIF 均进入构建产物。
- paper-skill 结构校验通过：10 个章节、11 个主动模块、1 个双模块章节、13 个已注册组件。
- 仓库 `validate`、`catalog`、`validate:pr -- main` 和 `build:paper -- hy_world_2_0` 全部通过；目录索引重新生成后无额外变更。
- 浏览器确认三个 GIF 均完整加载：重建与交互素材为 640×360，Mesh 素材为 520×293，控制台无错误或警告。
- 桌面端展开表 9 后，五行消融、重点配置、指标方向和页码均可直接阅读。
- 390×844 移动端检查确认页面宽度无溢出；表 9 在 250 px 容器内横向滚动，表 14 在 209 px 容器内横向滚动，页面本身保持 375 px 宽。
- 第 10 章实测前四案正确判决，累计分数为 4/4；第四案解锁的表 14 包含 6 行、10 列，并正确显示 128 视图 18.00 s 与 5.60 s 的硬件条件。

### 审查重点

- 模块 2.1：展开表 4，确认教程只标注 I2P 子表，没有把 T2P 数值混入图像条件结论。
- 模块 3.1：展开表 1，确认航拍类别说明、对象绑定与迭代条件和论文一致。
- 模块 7.1：重点核对高分辨率 H 行的 Acc. 0.079 → 0.037，确认界面始终解释为越低越好的误差。
- 模块 9.1：切换五种配置后展开表 9，确认交互数值与原表对应；检查两个 GIF 图注没有把官方演示升级为定量结论。
- 模块 10.1：依次完成前四案，确认每案只解锁自己的对应表；表 14 的 5.60 秒必须同时显示 H20、4 卡、SP + BF16 + FSDP 和 128 视图条件。

## V0.8「终末审判庭·证据之眼觉醒」

日期：2026-08-16

### 修改内容

- 利用论文结果、腾讯项目主页、官方 GitHub、模型平台、登录页和社区许可证资料，重新设计第 1 章与第 10 章交互。
- 将单纯的输入分支图升级为可联动输入、执行路径和输出类型的任务调度组件。
- 将“指标赛跑”和“资料链接导航”合并为需要主动判断的八案证据挑战，减少重复说明并提高参与感。
- 删除三个被替代的旧组件文件，统一组件注册和章节数据。

### 模块变更明细

- 调整模块 1.1：由“输入决定任务”升级为“世界任务调度台”，新组件为 `src/modules/hy-mission-planner.tsx`，旧组件 `src/modules/hy-input-mode.tsx` 删除。
- 模块 1.1 保留“选择输入”这一主操作，但状态从简单的生成/重建二分扩展为 4 张任务卡：文本、单图、多视图、视频。
- 文本与单图状态展示 HY-Pano 2.0、WorldNav、WorldStereo、WorldMirror 2.0 四阶段生成路径，并同步显示 3DGS、Mesh、视频输出及“生成补全不等于真实测量”的警告。
- 多视图与视频状态展示 Any-Modal 输入到 WorldMirror 2.0 的重建路径，并同步显示点图/点云、深度、法线、相机和 3DGS 输出及覆盖条件。
- 合并原模块 10.1“兼容协议结果赛”和模块 10.2“资料可信度与使用前检查”，新模块仍使用编号 10.1，名称为“结论审判庭：八案证据挑战”，组件为 `src/modules/hy-evidence-court.tsx`。
- 删除旧组件 `src/modules/hy-results.tsx` 和 `src/modules/hy-resources.tsx`，并从 `src/modules/registry.tsx` 移除旧注册项。
- 新模块 10.1 包含 8 个案卷：I2P CLIP-I、7-Scenes 高分辨率误差、3DGS 数量与画质取舍、5.60 秒与 712 秒效率边界、Marble 定性比较、GitHub 当前开放内容、在线体验登录要求、社区许可证性质。
- 每案提供“成立 / 有条件 / 不成立”三个判决；选择后锁定答案、标出正确判决，并解锁解释、指标对照、三条检查条件和一手资料链接。
- 增加累计得分、下一案、重新审理、八案结算和四档称号；满分状态显示“首席证据审判官”，并提供论文、GitHub 和许可证复盘入口。
- 调整 `src/data/tutorial.ts`：替换模块 1.1 的标题与组件 ID，将第 10 章两个模块合并为一个，并更新第三条 takeaway。
- 重写 `src/styles/paper.css` 中本轮交互样式，增加任务路径、输出标签、判决按钮、证据解锁、指标对照、结算页和移动端布局。

### 使用素材与内容边界

- 论文表 4、表 9、表 11、表 14 和效率汇总用于四个定量案卷。
- 官方 GitHub 与中文文档用于 Pipeline、单卡/多卡、Gradio、权重下载和输入数量约束。
- 腾讯项目主页用于输入形式、四阶段生成路径与输出格式，但这些产品表述不替代论文实验。
- 腾讯登录页用于“需要登录”判断；社区许可证全文用于地域、百万月活和分发限制判断。
- Marble 仍只按论文中的定性比较处理，知乎文章不作为任何案卷的最终证据源。

### 验证结果

- Vite 生产构建通过，58 个模块完成转换。
- paper-skill 结构校验通过：10 个章节、11 个主动模块、1 个双模块章节、13 个已注册组件。
- 浏览器实测文本与视频任务卡，路径、输出和警告均随输入正确更新。
- 浏览器完成八案全流程测试：错误判决会显示纠正提示，正确判决累计得分，最终 8/8 进入“首席证据审判官”结算页。
- 桌面截图确认两个新模块无文字重叠，浏览器控制台无错误或警告。

### 审查重点

- 模块 1.1：依次切换文本、单图、多视图和视频，确认生成路径与重建路径没有混写。
- 模块 10.1：第三案必须判为“有条件”；第四、第五、第七、第八案必须判为“不成立”。
- 完成第八案后应进入结算页，得分不得循环累加超过 8 分。
- 外部链接只在证据解锁或结算复盘时出现，不应代替模块内部解释。

## V0.7「审查显微镜·模块真名解放」

日期：2026-08-16

### 修改内容

- 建立强制性的模块级修改记录规范。
- 回填 V0.1 至 V0.6 的模块编号、组件文件和具体调整内容。
- 将“新增了什么功能”细化为“哪个模块、哪种状态、哪段反馈发生了变化”，方便逐项审查。

### 模块变更明细

- 无运行模块变更。
- 本版本只调整 `TUTORIAL_CHANGELOG.md` 的审查结构，不修改 React 组件、教程数据或样式。
- 今后的版本若涉及模块修改，必须同时记录新增入口、控制状态、画面变化、反馈文案和证据边界。

### 验证结果

- V0.1 至 V0.7 均已包含模块变更说明。
- 模块编号与当前 `src/data/tutorial.ts`、`src/modules/registry.tsx` 的注册关系完成核对。

## V0.6「别名进化论·萌力全开」

日期：2026-08-16

### 修改内容

- 为 V0.1 至 V0.5 统一设计更萌、更中二的版本别名。
- 保持“空间勘景冒险团”的连续世界观，同时让名称能够提示每个版本的核心改动。
- 仅调整修改记录中的版本标题，不改变教程内容、交互行为或历史描述。

### 新版别名

- V0.1「星野勘景团·初次觉醒」
- V0.2「折线追光术·相机禁止瞬移」
- V0.3「秘典归档阵·入库封印完成」
- V0.4「全知资料喵·罗盘启动」
- V0.5「七曜开源舰·情报全装填」

### 模块变更明细

- 无运行模块变更。
- 仅修改 `TUTORIAL_CHANGELOG.md` 中 V0.1 至 V0.5 的版本标题；所有 React 模块、控制状态和反馈文案保持不变。

### 验证结果

- 本次仅修改 Markdown 文档，不影响 React 项目构建与运行结果。
- 所有已记录版本均保留唯一编号、日期和完整修改内容。

## V0.5「七曜开源舰·情报全装填」

日期：2026-08-16

### 修改内容

- 联网核验腾讯官方项目主页、GitHub、中文 README、中文使用文档、在线体验页和社区许可证。
- 在 `REFERENCES.md` 中加入官方开放时间线、Hugging Face 与 ModelScope 权重入口、复现前硬件检查和许可证重点条款。
- 核实在线体验页当前需要登录，避免把它描述为可匿名直接使用的服务。
- 核验两篇知乎专栏的真实链接、标题、作者和编辑日期，并逐篇标注适用范围与事实核验风险。
- 将第 10 章资料模块升级为七类可切换来源，增加证据层级、官方开放进度、使用约束与下载前检查。
- 统一教程中的判断口径：数字看论文，开放状态看 GitHub，产品能力看官网，使用边界看许可证，第三方文章只补充理解。

### 模块变更明细

- 调整模块 10.2“资料可信度与使用前检查”，组件文件为 `src/modules/hy-resources.tsx`。
- 入口从 V0.4 的 4 类扩展为 7 类：论文证据、项目主页、代码文档、模型权重、在线体验、许可证、中文解读。
- 新增 `evidence`、`official`、`warning`、`thirdparty` 四种证据层级；切换入口时同步改变层级标签、说明背景、三条核验事实和外部链接。
- 新增官方开放时间线，显示 2026-04-16、05-11、05-18 三次主要开放节点。
- 在线体验状态改为“需要登录”，许可证状态增加地域、百万月活和分发约束，知乎状态增加作者与第三方证据边界。
- 调整 `src/data/tutorial.ts` 中模块 10.2 的标题、描述、第 10 章洞察和第三条 takeaway，使模块反馈与联网核验结果一致。
- 调整 `src/styles/paper.css`：增加七入口横向导航、四类证据配色、多链接区、开放时间线和移动端单列布局。
- 审查时重点点击“许可证”和“中文解读”，确认警示信息不会被误读为论文结论。

### 修改依据

- 官方 GitHub README 的 News、模型表、WorldMirrorPipeline、多 GPU 与 Gradio 说明。
- 腾讯混元项目主页对输入形式、四阶段管线、输出格式和引擎兼容性的当前描述。
- `License.txt` 中的适用地域、百万月活额外许可、分发通知、输出权利和模型用途条款。
- Hugging Face、ModelScope 官方模型入口和腾讯混元 3D 登录页的当前状态。
- 知乎专栏《HY-World 2.0：生成辅助重建的完整开源》和《HY-World 2.0：完整的 3D 物理世界生成与模拟系统》。

### 验证结果

- 项目 `npm install` 与 Vite 生产构建通过，59 个模块完成转换。
- paper-skill 结构校验通过：10 个章节、12 个主动模块、14 个已注册组件。
- 仓库结构校验、目录重新生成、PR 范围检查和单篇教程构建全部通过。
- 在本地浏览器展开全部章节并实测资料切换；许可证与知乎内容可正确更新，页面无运行错误。
- 本版本计划以 `Expand HY-World 2.0 reference guide` 作为独立提交，便于逐版本审查。

### 审查重点

- 许可证摘要仅用于提示，不构成法律意见；实际部署和分发必须阅读协议全文。
- 2026 年 7 月的 HY World 2.1 是后续产品更新，教程没有将其混写为 2.0 论文贡献。
- 知乎文章中的体验判断和宣传性措辞未被用作论文事实依据。

## V0.4「全知资料喵·罗盘启动」

日期：2026-08-15

### 修改内容

- 新建 `REFERENCES.md`，按一手资料、论文相关比较和中文讨论入口整理外部资料。
- 从论文 PDF 首页链接中核验腾讯官方体验页、官方 GitHub、项目主页和 arXiv 地址。
- 在第 10 章新增“从论文结论走向官方入口”交互模块，学习者可在论文、项目页、GitHub 和在线体验之间切换。
- 明确不同资料的用途：论文用于事实核对，项目页用于案例浏览，GitHub 用于复现准备，体验页用于观察产品流程。
- 补全 `paper.json` 的作者团队、年份、发表平台和主题元数据。
- 在 README 中增加参考资料和修改记录入口。

### 模块变更明细

- 新增模块 10.2“从论文结论走向官方入口”，组件文件为 `src/modules/hy-resources.tsx`。
- 初版提供 4 个切换入口：arXiv 论文、腾讯项目主页、官方 GitHub、腾讯在线体验。
- 每次切换会更新资料来源、用途说明、使用边界和打开链接；初始状态为“论文”。
- 在 `src/modules/registry.tsx` 注册 `hy-resources`，并在 `src/data/tutorial.ts` 将其挂载到第 10 章。
- 在 `src/styles/paper.css` 新增入口按钮、选中状态、详情面板和移动端横向滚动样式。
- 该版本尚未联网核验当前网页状态，因此模块只提供一手入口，知乎仅保留站内搜索，不展示具体文章。

### 修改依据

- `2604.14268v1.pdf` 首页的可点击链接和 PDF 元数据。
- 论文关于开放模型权重、代码和技术细节的声明。
- 论文对 Marble 仅进行定性比较，因此教程继续保留协议边界提示。

### 验证限制

- 当前执行环境拒绝访问公共互联网，无法核验 GitHub README、腾讯网页当前状态和具体知乎文章正文。
- 为避免捏造，知乎部分只记录站内检索入口，具体文章需在可联网环境中二次审核后加入。

### 审查重点

- 检查四个官方入口是否仍可访问，以及 GitHub 当前实际开放的模型和权重范围。
- 检查在线体验页的账号权限与产品流程是否发生变化。

## V0.3「秘典归档阵·入库封印完成」

日期：2026-08-15

### 修改内容

- 将最终教程导入 `html_output/hy_world_2_0/`，补充仓库要求的 `paper.json` 和 `package-lock.json`。
- 在项目 README 中补充论文链接，以及 Figure 2、Figure 12 的素材来源与用途说明。
- 生成并更新仓库总目录 `catalog/papers.json`。
- 新建本修改记录，作为后续所有人工优化的统一审查入口。

### 模块变更明细

- 无运行模块变更。
- 本版本只将已有教程完整导入 `html_output/hy_world_2_0/`，补充 `paper.json`、依赖锁文件、README 素材说明和总目录记录。
- 导入前后的模块编号、组件注册和交互行为保持一致。

### 验证结果

- 仓库结构验证通过。
- 总目录生成通过。
- Pull Request 范围检查通过。
- 导入前相同教程源码已通过 TypeScript 检查和 Vite 生产构建。
- 仓库单篇构建脚本受本机 npm 沙箱权限限制，失败点不在教程源码。

### 审查重点

- `paper.json` 当前状态为 `review`，正式发布前仍需内容审核。
- 最终提交范围应只包含本作品目录和 `catalog/papers.json`。

## V0.2「折线追光术·相机禁止瞬移」

日期：2026-08-15

### 修改内容

- 修复首屏新旧方法对比中的相机运动：相机按照折线各段的实际长度移动，并经过全部拐点。
- 修复章节“空间摄影勘景”类比动画中的相机路径。
- 修复“传统割裂 / 条件化统一”对比动画中的双相机路径。
- 新增折线路径采样函数，按累计路程确定当前线段，再在线段内部插值。

### 模块变更明细

- 调整首屏对比组件 `src/modules/hy-hero.tsx`：相机从起终点直线插值改为按折线累计长度采样，移动时经过每个拐点。
- 调整公共类比组件 `src/modules/hy-analogy.tsx`：章节中的勘景相机复用折线路径采样，动画主体与画出的路线保持一致。
- 调整模块 1.2“传统割裂与统一框架”，组件文件为 `src/modules/hy-boundary-compare.tsx`：左右两侧相机分别沿各自折线移动，不再穿越路径内部。
- 三个组件共享同一类路径计算逻辑：先计算各段长度和总长度，再根据动画进度定位当前线段并做段内插值。
- 未修改模块 3.1“场景感知轨迹”的路线选择规则；本次只修复相机如何沿已画出的路线运动。
- 审查时重点观察折线拐角：相机应到达拐点后再转向，不能从起点直接滑向终点。

### 修改原因

原动画只对起点和终点坐标做线性插值，导致相机穿过折线内部，与画出的路径不一致，削弱了轨迹规划类比的可信度。

### 验证结果

- TypeScript 检查通过。
- Vite 生产构建通过。
- 教程结构校验通过。

## V0.1「星野勘景团·初次觉醒」

日期：2026-08-15

### 修改内容

- 根据 HY-World 2.0 论文生成完整中文交互式教程。
- 设计 10 个渐进章节和 11 个主动交互模块。
- 采用“空间摄影勘景”作为统一教学类比。
- 引入输入模式、全景初始化、场景感知轨迹、关键帧扩展、跨轨迹记忆、四步蒸馏、多尺度重建、Any-Modal 架构、3DGS 合成和实验结果等交互。
- 加入论文 Figure 2 和 Figure 12，辅助讲解系统架构与 WorldMirror 2.0。
- 修正全景、关键帧、记忆和 3DGS 指标条靠近 Canvas 底部时被裁切的问题。

### 模块变更明细

| 模块 | 组件文件 | 初版交互内容 |
| --- | --- | --- |
| 1.1“输入决定任务” | `src/modules/hy-input-mode.tsx` | 切换文本、单图、多视图和视频，显示生成或重建分支及原因 |
| 1.2“传统割裂与统一框架” | `src/modules/hy-boundary-compare.tsx` | 启动双侧对比动画，比较传统割裂方案与条件化统一框架 |
| 2.1“全景初始化三步观察” | `src/modules/hy-panorama.tsx` | 逐步查看显式投影、隐式映射和 ERP 接缝修复 |
| 3.1“五类轨迹覆盖实验” | `src/modules/hy-trajectory.tsx` | 选择常规、环绕、重建感知、漫游和航拍路线，比较覆盖条件 |
| 4.1“Video-VAE 与 Keyframe-VAE 同轨比较” | `src/modules/hy-keyframes.tsx` | 同轨播放两种编码方式，比较重复模糊帧与稀疏关键帧 |
| 5.1“组合记忆消融” | `src/modules/hy-memory.tsx` | 切换无记忆、组合记忆及替代拼接方案，观察一致性指标 |
| 6.1“三阶段能力阶梯” | `src/modules/hy-training-stages.tsx` | 逐段启用领域适配、记忆训练和后蒸馏，展示新增能力 |
| 7.1“分辨率外推变插值” | `src/modules/hy-resolution.tsx` | 拖动分辨率档位，对比标准 RoPE 与归一化 RoPE |
| 8.1“WorldMirror 2.0 路径地图” | `src/modules/hy-architecture.tsx` | 点击输入、先验、Token、骨干、任务头和输出，追踪激活路径 |
| 9.1“高斯数量与画质取舍” | `src/modules/hy-composition.tsx` | 切换五种 3DGS 配置，比较数量、PSNR、LPIPS 和漂浮物风险 |
| 10.1“兼容协议结果赛” | `src/modules/hy-results.tsx` | 选择兼容指标后启动比较，按高优或低优方向统一反馈 |

- 新增首屏组件 `src/modules/hy-hero.tsx`，展示传统方法与本文方法的动态对比。
- 新增公共类比组件 `src/modules/hy-analogy.tsx`，为 10 章提供统一的“空间摄影勘景”生活化动画。
- 调整模块 2.1、4.1、5.1、9.1 的 Canvas 底部留白和指标位置，避免状态条被边界裁切。
- 模块 1.2 引用论文 Figure 2，模块 8.1 引用论文 Figure 12；图像只作为模块内的论文证据补充，不替代主动交互。

### 验证结果

- 10 个章节、11 个主动模块和 13 个注册组件通过结构校验。
- TypeScript 检查和 Vite 生产构建通过。
- 两张论文图片的本地资源响应正常。
